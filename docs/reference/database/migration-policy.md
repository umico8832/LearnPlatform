# Flyway 迁移策略

迁移文件与版本顺序以[迁移目录](../../../backend/src/main/resources/db/migration/)为准。
新增迁移前核对该目录及目标表的既有 SQL；变更背景按需查阅[开发日志](../../project/changelog/index.md)。

## 编写规则

1. 已进入共享历史的迁移文件不可修改或重排。
2. 新迁移使用下一个连续版本，并采用可读名称。
3. 对存量表增加非空字段时，先设计默认值和存量数据回填。
4. 唯一约束上线前先确认存量数据没有冲突。
5. 迁移必须能在空数据库从 V1 连续执行，也必须能从上一个版本升级。
6. Java 实体、Mapper、DTO、测试和数据库文档在同一变更中同步。

## 外键策略

项目主要使用逻辑外键，原因是本地学习项目需要较低的迁移和演示环境维护成本。代价是删除保护、引用校验和跨表一致性必须在 Service 事务与集成测试中明确实现。

数据库唯一索引仍用于保护不能只依赖应用校验的并发不变量。

## 验证

按[测试策略](../../development/testing.md#2-分层验证l1--l2--l3)选择受影响验证，命令见
[运行命令](../../development/testing.md#10-运行命令)。真实 MySQL 的 Testcontainers 验证应覆盖
空库迁移、从上一版本升级、关键唯一约束、幂等操作和事务边界。
