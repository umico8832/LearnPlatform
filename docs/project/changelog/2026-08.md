# 2026-08 开发日志

## Round 277：拆分题目学习资产 Prompt 模板

### 完成内容

- 新增 260 行的无状态 `QuestionAssetPromptFactory`，集中维护标准解析、小白解析、步骤拆解、
  错误选项、常见误区、结构化变式题和可视化互动七类 system / user Prompt；工厂不读取 Mapper、
  不调用 AI Provider，也不注册为 Spring 组件。
- `QuestionLearningAssetService` 从 635 行收敛为 393 行，仍负责题目上下文读取、同步 / 流式生成、
  配额与审计、普通 / 变式题缓存、访问控制和反馈；Prompt 文本、换行和上下文拼接顺序保持不变。
- 原服务的五组可视化 Prompt 断言迁为工厂直接测试，新增七类 user Prompt 精确拼接与 system
  Prompt SHA-256 快照回归；服务测试继续覆盖缓存命中、Provider 调用、流式输出、缓存失败降级、
  变式题私有答案和完整题目上下文。

### 验证

- `QuestionAssetPromptFactoryTest`、`QuestionLearningAssetServiceTest`、`AiControllerTest` 与
  `AiAssetViewControllerTest` 共 42 个测试通过。
- `mvn clean verify -B`：后端全量 578 个测试通过；ArchUnit 5 个规则、生产 / 测试双
  Checkstyle、SpotBugs 和 JaCoCo 门禁全部通过。

### 遗留问题

- 题目上下文仍由主服务通过五个 Mapper 装配；它可以形成后续独立读取边界，但本轮不与纯模板迁移
  混合，也不拆分与流式异常、审计和缓存降级紧密耦合的 Provider 调用。

## Round 276：拆分重复题检测算法

### 完成内容

- 新增 186 行的无状态 `QuestionDuplicateDetector`，集中承担题目文本归一化、同课程同题型分桶、
  Levenshtein 相似度计算和并查集重复组聚合，不注册为 Spring 组件，也不访问 Mapper。
- `QuestionService` 从 577 行收敛为 427 行，公开题查询、题型 / 课程筛选、阈值与最大组数钳制、
  完整 `QuestionVO` 组装仍留在原服务；管理端 URL、权限、缓存、事务和数据库契约不变。
- 算法测试独立覆盖精确 / 相似匹配、跨课程隔离、短文本忽略、并查集传递分组，以及相似度和组大小
  排序；服务测试继续覆盖完整课程、选项、知识点填充，并新增阈值与 limit 边界回归。

### 验证

- `QuestionDuplicateDetectorTest`、`QuestionServiceTest` 与 `AdminQuestionControllerTest` 共 23 个
  测试通过。
- `mvn clean verify -B`：后端全量 576 个测试通过；ArchUnit 5 个规则、生产 / 测试双
  Checkstyle、SpotBugs 和 JaCoCo 门禁全部通过。

### 遗留问题

- 重复题检测仍沿用既有内存两两比较，单个分桶的最坏复杂度为 O(n²)；本轮是职责迁移，不将未经
  性能基线验证的索引、分词或向量检索混入结构重构。

## Round 275：拆分个人学习报告与通用统计

### 完成内容

- 新增 350 行的 `LearningReportService`，集中聚合本月 / 上月练习、错题转化、考试成绩、
  每日趋势、课程、题型、间隔复习和可解释学习效果指标。
- `StatisticsService` 从 584 行收敛为 277 行，移除报告专属的考试记录与复习计划依赖，只保留
  用户概览、每日趋势、课程统计和管理端平台概览。
- `StatisticsController` 的学习报告端点直接依赖新服务；其他统计端点和管理端控制器保持原有
  依赖。`learningReport` 缓存名 / 用户键、缓存清理、查询顺序、日期边界、计算公式、既有 N+1
  和无事务语义不变。
- 学习报告复习统计测试、控制器测试和真实 MySQL 集成测试调用方同步迁移；URL、认证用户绑定、
  DTO 和数据库结构不变。

### 验证

- `LearningReportReviewStatsTest` 与 `StatisticsControllerTest` 共 8 个测试通过。
- `mvn clean verify -B`：后端全量 572 个测试通过；ArchUnit 5 个规则、生产 / 测试双
  Checkstyle、SpotBugs 和 JaCoCo 门禁全部通过。
- `StatisticsServiceIntegrationTest` 已尝试执行，但本机 Docker socket 不存在，Testcontainers
  在 MySQL 容器启动前失败；该集成测试未记为通过，仍需可用 Docker 环境或远端 CI 确认。

### 遗留问题

- 报告内逐题加载课程和题型的 N+1 查询属于既有行为；本轮为职责迁移，没有顺带改写查询，后续
  若优化需单独定义性能基线和结果等价测试。

## Round 274：拆分 AI 学习资产交互与效果统计

### 完成内容

- 新增 106 行的 `AiAssetEngagementService`，集中承担学习资产查看按日原子聚合、当前缓存版本
  的变式训练开始 / 幂等完成，以及向 `AiVariantQuestionService` 委托结构化变式题首次判分。
- `AiLearningEffectService` 从 615 行收敛为 535 行，删除用户侧写事务与判分委托，只保留资产
  查看、反馈、后续练习、跨题迁移和变式训练结果的观察性统计。
- 学习端 `AiController` 改为直接依赖交互服务；管理端 `AdminAiUsageController` 继续只依赖
  效果统计服务。既有 URL、认证用户绑定、DTO、异常和事务边界保持不变。
- 原服务测试中的 5 个交互用例迁入 `AiAssetEngagementServiceTest`，统计测试保留在
  `AiLearningEffectServiceTest`；控制器和真实 MySQL 集成测试调用方同步迁移。

### 验证

- 交互、统计与两个控制器聚焦 4 个测试类共 20 个测试通过。
- `mvn clean verify -B`：后端全量 572 个测试通过；ArchUnit 5 个规则、生产 / 测试双
  Checkstyle、SpotBugs 和 JaCoCo 门禁全部通过。
- `AiVariantTrainingIntegrationTest` 尝试执行，但本机 Docker daemon 未运行，Testcontainers
  无法连接 Docker socket；该真实 MySQL 测试未记为通过，远端集成测试仍需验证。

### 遗留问题

- 本轮只迁移既有按日 upsert 和变式训练事务，不顺带改变并发、表结构或判分规则；真实 MySQL
  路径需由可用 Docker 环境或远端 CI 补充确认。

## Round 273：拆分 AI 调用治理职责

### 完成内容

- 新增 166 行的 `AiCallGovernanceService`，集中承担用户每日配额、当日调用量、调用日志、
  Token / 成本、Trace ID、提示词哈希和模型配置指纹；审计写入异常继续只记录日志，不覆盖
  原始 AI 业务结果。
- `AiService` 从 656 行收敛为 502 行，只保留同步 / 流式 AI 调用、提示词和业务结果编排，
  并将调用前配额与调用后审计委托给治理服务。
- 学习诊断建议、私有试卷草稿、题目学习资产、错题建议、投稿质检、难度评估和知识点标注
  7 个服务改为直接依赖治理边界；`AiController` 的用量查询同样不再通过宽泛的 `AiService`。
- 原 `AiServiceLoggingTest` 迁移为治理服务直接测试，并补充审计存储失败不抛出异常的回归用例；
  用户配额覆盖、全局配额继承、非正数不限额、成功 / 失败 Token 记录与指纹脱敏语义保持不变。

### 验证

- 11 个治理相关测试类共 136 个测试通过。
- `mvn clean verify -B`：后端全量 570 个测试通过；ArchUnit 5 个规则、生产 / 测试双
  Checkstyle、SpotBugs 和 JaCoCo 门禁全部通过。
- 本轮不改变 API 与数据库契约。

### 遗留问题

- `AiProvider#getLastTokenUsage` 的最近调用用量读取契约仍由 Provider 实现保证；本轮没有扩大
  Provider 的并发语义，也不在职责拆分中引入新的请求上下文协议。

## Round 272：拆分私有试卷草稿复核职责

### 完成内容

- 新增 252 行的 `components/exam/PrivateExamDraftReview.vue`，集中承担草稿原文件下载、AI 答案与
  解析建议生成、人工答案 / 解析表单、题型约束校验和逐题复核。
- `PrivateExamImportDialog.vue` 从 783 行收敛为 599 行，继续拥有 Markdown / 文本 / PDF /
  DOCX 统一预览与哈希确认、草稿创建 / 删除和最终试卷启用；复核组件只发出更新后的草稿，
  不自行确认启用或发出导入完成事件。
- 父组件收到复核结果后继续更新活动草稿和草稿列表；服务端接口、AI 建议与判分答案隔离、
  单选 / 判断唯一答案、多选至少两个答案和人工解析必填规则保持不变。

### 验证

- 新增 2 个复核组件测试，覆盖 AI 建议生成只上报草稿更新，以及从 AI 建议初始化人工表单
  后显式提交复核；与来源、记录和页面测试合计 4 个文件 17 个测试通过。
- `npm run coverage`：69 个测试文件、331 个测试通过；覆盖率为语句 42.00%、分支
  36.12%、函数 34.31%、行 43.54%。
- `npm run lint`、`npx vue-tsc --noEmit` 和学习端 / 管理端双生产构建通过；构建保留既有
  VueUse 纯注解位置与 Mermaid / 大 chunk 非阻塞警告。

### 遗留问题

- 导入组件剩余 599 行由统一表单、格式分派、预览确认、草稿列表和样式构成；当前未发现
  不复制 PDF / DOCX / 文本流程即可继续拆出的独立状态闭包，不再为行数继续拆分。

## Round 271：拆分私有试卷来源管理职责

### 完成内容

- 新增 `components/exam/PrivateExamSourceManager.vue`，集中承担已确认私有试卷来源查看、
  原文件清单分页、试卷 / 草稿原文件下载、关联业务内容删除和两个对话框的响应式展示。
- `ExamListView.vue` 从 707 行收敛为 454 行，不再持有来源或存储清单状态；页面只发出
  `openPaperSource` / `openStorage` 命令，并在组件发出 `contentDeleted` 后刷新试卷列表和
  导入草稿，跨组件协作保持显式。
- 试卷卡片本身的私有试卷删除仍留在页面列表闭包，没有为复用一条 API 强行转移；接口、
  删除确认文案、文件媒体类型、分页参数与移动端 640px 布局保持不变。

### 验证

- 新增 2 个组件测试，覆盖存储元数据分页、草稿关联删除后的清单刷新与父事件、来源正文
  读取和按响应媒体类型下载；与页面及记录测试合计 3 个文件 15 个测试通过。
- `npm run coverage`：68 个测试文件、329 个测试通过；覆盖率为语句 41.97%、分支
  36.13%、函数 34.29%、行 43.52%。
- `npm run lint`、`npx vue-tsc --noEmit` 和学习端 / 管理端双生产构建通过；构建保留既有
  VueUse 纯注解位置与 Mermaid / 大 chunk 非阻塞警告。

### 遗留问题

- `PrivateExamImportDialog` 仍有 783 行，但其文本 / PDF / DOCX 预览、答案草稿和人工复核
  状态共享同一导入生命周期；后续应先识别可独立测试的状态机或子闭包，不能按格式机械拆成
  三套重复流程。

## Round 270：拆分考试记录列表职责

### 完成内容

- 新增 `components/exam/ExamRecordList.vue`，集中承担考试记录接口加载、分页、状态 / 分数
  映射、桌面表格与移动卡片，以及继续考试和查看结果导航。
- `ExamListView.vue` 从 954 行收敛为 707 行，只保留试卷列表、考试发起、私有试卷导入和
  来源 / 存储管理；页面通过 `totalChange` 维护总览数量，通过组件暴露的 `reload` 在切换
  记录页签时刷新，没有建立第二套记录状态。
- 完整迁移原有样式并恢复 860px 响应式断点；API、路由名称、状态文案、设计变量和交互
  语义不变。

### 验证

- 新增 2 个组件测试，覆盖分页请求与总数事件、四种记录状态、暂定分数以及继续考试 /
  结果页导航；与既有页面测试合计 2 个文件 13 个测试通过。
- `npm run coverage`：67 个测试文件、327 个测试通过；覆盖率为语句 41.89%、分支
  36.07%、函数 34.23%、行 43.44%。
- `npm run lint`、`npx vue-tsc --noEmit` 和学习端 / 管理端双生产构建通过；构建保留既有
  VueUse 纯注解位置与 Mermaid / 大 chunk 非阻塞警告。

### 遗留问题

- `ExamListView` 仍同时维护私有试卷来源和原文件存储两个对话框；二者共享来源生命周期，
  下一轮按完整业务闭包评估是否提取，不能仅为降低行数拆成无状态包装组件。

## Round 269：拆分复习计划查询与写事务

### 完成内容

- 新增 `ReviewScheduleQueryService`，集中承担到期卡片的课程 / 题目 / 知识点筛选、全部卡片
  查询、统计分类、连续复习天数、卡片 VO 批量组装和 AI 复习上下文。
- `SpacedRepetitionService` 仅保留计划新增 / 同步 / 删除 / 重置、复习答题事务、错题同步、
  练习事实写入和 SM-2 算法，从 741 行收敛为 380 行；查询服务 366 行，没有引入接口—实现
  样板层。
- `ReviewController` 按命令与查询职责分别注入两个 Service，所有 `/api/review/**` URL、
  请求参数、响应 DTO、SSE 行为和认证边界保持不变；生产类删除为测试空依赖保留的重载
  构造器，测试改为面向实际职责实例化。
- 查询测试同步更名为 `ReviewScheduleQueryServiceTest`，新增统计分类 / 连续天数语义和
  Controller 查询委派测试。

### 验证

- 复习域聚焦测试：4 个测试类、23 个测试通过；生产与测试导入 Checkstyle 均为 0 条违规。
- `mvn clean verify -B`：567 个测试通过，0 失败；双 Checkstyle 0 条违规、SpotBugs 0 个
  问题，JaCoCo 覆盖率门槛满足，ArchUnit 分层规则通过。

### 遗留问题

- SM-2 算法仍与复习写事务同类维护，现有 380 行已形成单一写侧业务闭包；只有在算法需要
  独立版本化、时钟注入或复用时再提取策略，不为继续降行数机械拆分。

## Round 268：移除无入口学习计划后端残留

### 完成内容

- 核对前端历史后确认学习计划卡片、API 封装和旧首页已在 `a8095a4` 因课程空间 IA 收敛
  删除；当前 PRD、README、前端、测试和其他后端服务均不依赖 `/api/learning-plan`。
- 删除孤立的 `LearningPlanController`、`LearningPlanService`、请求 / 响应 DTO、Entity 和
  Mapper，并同步移除 API 参考中的两个旧端点；该实现原本存在 GET 请求隐式写库、连续
  天数最多 365 次查询和直接依赖系统日期的问题，不再为无入口功能保留维护面。
- 保留 `learning_plan` 数据表、V1 基线和 V3 幂等迁移，不执行破坏性迁移，不删除既有
  用户数据；数据库文档继续如实记录历史表。

### 验证

- `mvn clean verify -B`：565 个测试通过，0 失败；生产与测试导入 Checkstyle 均为 0 条
  违规，SpotBugs 0 个问题，JaCoCo 覆盖率门槛满足。
- 源码、前端和 API 文档复核已无 `learning-plan` 运行时入口或类型引用。
- `python3 scripts/check-docs.py` 通过，59 个 Markdown 文件与仓库 Skills 校验成功。

### 遗留问题

- `learning_plan` 表作为历史兼容数据保留；未来若决定物理删除，必须单独确认数据保留策略
  并通过显式迁移处理，不能在代码清理中顺带删表。

## Round 267：收口后端测试源码导入规范

### 完成内容

- 展开 12 个测试文件中的 22 条 Entity、Mapper、DTO 与 JUnit 普通星号导入，删除一个
  实际未使用的 Entity 星号导入；全部 Java 源码当前不存在普通包级星号导入。
- 借由导入门禁清理 8 条原有重复或无用导入，覆盖重复 `List` / `Tag`、未使用 DTO、时间、
  MyBatis 和 Mockito 匹配器，不改变测试逻辑。
- 新增 `checkstyle-test-imports.xml` 及独立 Maven verify 执行，仅门禁测试源码的星号、重复
  和无用导入；允许 JUnit 断言、Mockito 与 MockMvc DSL 的静态星号导入，避免把生产命名与
  行长规则无关地强加给 97 个既有测试源文件。

### 验证

- `mvn clean verify -B`：565 个测试通过，0 失败；生产 Checkstyle 与测试导入 Checkstyle
  均为 0 条违规，SpotBugs 0 个问题，JaCoCo 覆盖率门槛满足。
- `rg` 复核生产与测试源码普通星号导入为 0；测试保留 71 条静态 DSL 星号导入。

### 遗留问题

- 测试方法的下划线场景命名和部分测试数据长行属于既有测试表达风格，不纳入本轮导入
  卫生门禁；若未来统一测试风格，应另立范围并评估可读性收益。

## Round 266：收口后端生产源码导入规范

### 完成内容

- 展开 65 个生产 Java 文件中的 97 条包级星号导入，改为只显式声明实际使用的 Entity、
  Mapper、DTO、Spring MVC、MyBatis、JJWT 和 JDK 类型；API、数据库和运行时逻辑不变。
- 清理自动展开时由示例文本误判产生的 2 条多余 `HashMap` 导入，生产源码当前不存在
  星号导入、冗余导入或无用导入。
- 在 Checkstyle 中启用 `AvoidStarImport`，与既有 `RedundantImport`、`UnusedImports`
  共同阻止生产代码重新引入模糊依赖；工程规则同步记录该约束。

### 验证

- `mvn clean verify -B`：565 个测试通过，0 失败；Checkstyle 0 条违规，SpotBugs 0 个问题，
  JaCoCo 覆盖率门槛满足，生产构建成功。
- `rg` 复核 `backend/src/main` 星号导入为 0。

### 遗留问题

- 测试源码仍有 22 条普通星号导入和 71 条 JUnit / Mockito 等静态星号导入；普通导入
  后续显式展开，测试静态导入按测试可读性单独决策，不机械照搬生产规则。

## Round 265：统一题目管理领域目录

### 完成内容

- 将 `QuestionGovernanceDrawers.vue`、`questionManagePresentation.ts` 和
  `useQuestionImportExport.ts` 从 `admin/views/` 根目录迁入 `admin/views/question/`；
  题目编辑、导入导出、治理和展示映射现位于同一领域目录。
- 更新 `QuestionManage.vue` 与 `QuestionImportExport.vue` 导入路径，源码内容和运行时
  行为不变；管理端页面根目录只保留路由页面及尚未形成同域目录的少量页面级文件。
- 扩展仓库结构门禁，精确拒绝三个题目辅助文件回到旧位置，并在工程规则中记录管理端
  页面独占辅助文件的归属原则。

### 验证

- 仓库结构聚焦 unittest：6 个测试通过，当前结构门禁通过。
- 前端 `npm run format:check`、`npm run lint`、`npx vue-tsc --noEmit` 通过；题目编辑和
  导入导出聚焦 Vitest 2 个文件、4 个测试通过。
- `npm run build`：学习端与管理端双生产构建通过；保留既有 VueUse 纯注解位置和
  Mermaid 按需 chunk 超过 600 kB 的非阻塞警告。
- 本轮只移动文件，未重复执行全量覆盖率；沿用 Round 264 的 66 个文件 325 个测试结果。

### 遗留问题

- 其他管理页面只有在出现两个以上独占辅助文件或明确领域闭包时再建立子目录，不为目录
  对称性搬动单文件。

## Round 264：拆分题目导入导出 UI

### 完成内容

- 将题目 Excel / Markdown 文件选择、导入结果和错误详情迁入
  `admin/views/question/QuestionImportExport.vue`；组件继续复用既有
  `useQuestionImportExport`，没有建立第二套文件状态或下载实现。
- 父页面通过公开方法打开导入、下载两类模板和按当前筛选导出；导入成功后组件发出
  `imported` 事件刷新题目列表，筛选参数、Blob 下载和 API 契约保持不变。
- `QuestionManage.vue` 从 921 行降至 825 行，新组件 152 行；当前管理端顶层页面已无
  900 行以上文件。

### 验证

- 聚焦 Vitest：1 个文件、2 个测试通过，覆盖 Excel 导入成功后的结果 / 错误展示与刷新
  事件，以及题型、课程、难度导出筛选参数；ESLint 与 `vue-tsc --noEmit` 通过。
- `npm run coverage`：66 个测试文件、325 个测试通过，语句 41.79%、分支 35.95%、
  函数 34.21%、行 43.38%，门槛通过。
- `npm run build`：学习端与管理端双生产构建通过；保留既有 VueUse 纯注解位置和
  Mermaid 按需 chunk 超过 600 kB 的非阻塞警告。
- 本轮没有后端、数据库或业务规则变化；因本地后端与管理员登录态限制，未把真实文件
  上传记为浏览器业务验收通过。

### 遗留问题

- 题目复审与治理仍由页面编排，但已有独立治理抽屉；后续只有在状态 / API 能完整归属时
  再继续拆分，不以 825 行本身作为缺陷。

## Round 263：拆分题目编辑器并修复课程知识点联动

### 完成内容

- 将 `QuestionManage.vue` 的新增 / 编辑题目表单迁入
  `admin/views/question/QuestionEditorDialog.vue`；组件自行管理校验、选项增删与重排、
  判断题默认选项、知识点树加载和创建 / 更新请求，父页面只负责打开和保存后刷新。
- 修复原页面在新增题目时切换课程不会加载对应知识点树的问题；课程变化现在清空旧课程
  的知识点选择并请求新树，编辑既有题目时仍保留原关联。
- 树选择器使用 Element Plus 支持的 `node-key="id"`，移除原先为绕过错误 `props.value`
  类型而使用的 `as any`；题目与知识点 ID 语义不变。
- 页面从 1206 行降至 921 行，新编辑器 281 行；创建 / 更新 API 与提示文案保持兼容。

### 验证

- 聚焦 Vitest：1 个文件、2 个测试通过，覆盖编辑字段保存和课程 1 → 课程 2 的知识点
  重新加载与旧关联清理；ESLint 与 `vue-tsc --noEmit` 通过。
- `npm run coverage`：65 个测试文件、323 个测试通过，语句 41.05%、分支 35.46%、
  函数 33.35%、行 42.65%，门槛通过。
- `npm run build`：学习端与管理端双生产构建通过；保留既有 VueUse 纯注解位置和
  Mermaid 按需 chunk 超过 600 kB 的非阻塞警告。
- 本轮没有数据库或后端行为变化；因本地后端与管理员登录态限制，未把编辑器真实提交
  记为浏览器业务验收通过。

### 遗留问题

- `QuestionManage.vue` 仍有 921 行；导入 / 导出 UI 已有 composable，可继续迁入自包含
  组件，使页面回归列表、复审和治理编排职责。

## Round 262：拆分投稿管理 AI 工具职责

### 完成内容

- 将 `SubmissionManage.vue` 的 AI 质检、知识点标注和难度评估三个流程迁入
  `admin/views/submission/SubmissionAiTools.vue`；组件通过一个公开命令入口加载对应
  投稿，父页面继续拥有列表、审核、批量通过和正式入库职责。
- 知识点建议应用仍绑定当前投稿 ID 和服务端返回的建议 ID；成功后组件发出 `updated`
  事件，父页面重新加载列表。三个工具的 API、错误提示、展示标签和对话框行为保持不变。
- 页面从 933 行降至 544 行，新组件 375 行；没有新增跨页面状态、路由或 API 包装层。
- 新增组件测试，覆盖质检结果、难度结果，以及知识点建议应用和父页面刷新通知。

### 验证

- 聚焦 Vitest：1 个文件、3 个测试通过；ESLint 与 `vue-tsc --noEmit` 通过。
- `npm run coverage`：64 个测试文件、321 个测试通过，语句 40.23%、分支 34.74%、
  函数 32.46%、行 41.76%，门槛通过。
- `npm run build`：学习端与管理端双生产构建通过；保留既有 VueUse 纯注解位置和
  Mermaid 按需 chunk 超过 600 kB 的非阻塞警告。
- 本轮不改变页面视觉契约；因本地后端与管理员登录态限制，未把 AI 工具真实请求记为
  浏览器业务验收通过。

### 遗留问题

- `QuestionManage.vue` 仍超过 1200 行，是当前最明显的前端职责集中点；投稿页的审核与
  详情对话框可继续拆分，但已不属于超大文件阻塞项。

## Round 261：拆分试卷管理智能组卷职责

### 完成内容

- 将 `ExamManage.vue` 中自包含的智能组卷规则表单、预览、确认创建和局部样式迁入
  `admin/views/exam/SmartExamDialog.vue`；父页面只通过公开 `open` 方法启动流程，并在
  `created` 事件后刷新试卷列表。
- 页面从 931 行降至 714 行，新组件 211 行；智能组卷请求参数、结果展示、错误提示和
  创建成功行为保持不变，没有调整 API、路由或数据库契约。
- 新增组件回归测试，覆盖默认自适应规则生成预览，以及基于预览确认创建并通知父页面。
- 更新前端架构文档，记录管理端考试领域对话框和页面编排的职责边界。

### 验证

- 聚焦 Vitest：1 个文件、2 个测试通过；ESLint 与 `vue-tsc --noEmit` 通过。
- `npm run coverage`：63 个测试文件、318 个测试通过，语句 38.97%、分支 33.39%、
  函数 31.20%、行 40.40%，门槛通过。
- `npm run build`：学习端与管理端双生产构建通过；保留既有 VueUse 纯注解位置和
  Mermaid 按需 chunk 超过 600 kB 的非阻塞警告。
- 本轮不改变视觉和交互契约；管理端开发入口已在 Round 260 浏览器确认，因同一后端与
  管理员登录态限制，未把智能组卷真实数据流程记为浏览器验收通过。

### 遗留问题

- `ExamManage.vue` 的新增/编辑试卷和题目选择器仍共享表单与选择状态，下一轮需先明确
  组件所有权再拆分；`QuestionManage.vue` 与 `SubmissionManage.vue` 仍超过 900 行。

## Round 260：拆分 AI 用量管理页并修复开发入口

### 完成内容

- 将 1146 行 `AiUsageView.vue` 收敛为 298 行页面编排，只保留周期选择、三接口并发加载、
  提醒确认、统计摘要和 ECharts 引用。
- 在 `admin/views/ai-usage/` 下新增运营报告、学习效果主面板、资产类型观察、变式难度
  观察和调用明细组件；最大组件 391 行，其余组件 90–151 行。
- 将 Token、成本、比率、提升值、颜色方向和结论标签映射集中到同域纯函数，避免多个
  展示组件复制判断。
- 浏览器最小验收发现 `npm run dev:admin` 的 HTML 入口被解析为管理端 root 下不存在的
  `/src/admin/main.ts`，导致开发页空白；通过 Vite `/src` 映射和 base 感知入口修复，
  并新增配置契约测试，避免生产构建成功掩盖开发入口回归。

### 验证

- 页面编排、展示映射和管理端 Vite 配置聚焦测试：3 个文件、6 个测试通过。
- 前端 `npm run format:check`、`npm run lint`、`npx vue-tsc --noEmit` 通过。
- `npm run coverage`：62 个测试文件、316 个测试通过，语句 38.23%、分支 32.55%、
  函数 30.12%、行 39.64%，门槛通过。
- `npm run build`：学习端与管理端双生产构建通过；保留既有 VueUse 纯注解位置和
  Mermaid 按需 chunk 超过 600 kB 的非阻塞警告。
- 临时管理端开发服务器不再空白，浏览器正确进入
  `/admin/login?redirect=/ai-usage`，管理员登录页与控制台状态正常。

### 遗留问题

- 本地后端未运行且无可用管理员登录态，目标 AI 用量数据页的桌面 / 移动布局、刷新和
  提醒确认只由组件测试覆盖，未记为真实浏览器业务验收。
- `QuestionManage`、`ExamManage` 和 `SubmissionManage` 仍超过 900 行，继续按独立表单或
  对话框边界拆分。

## Round 259：收口顶层 Service 包职责

### 完成内容

- 将私有试卷原文件记录从顶层 `service` 迁入 `dto.exam`，明确它是 Service 与 HTTP
  下载边界之间传递的值对象，而不是业务服务。
- 将阶段测评知识点与题源快照聚合器迁入 `service.assessment`，将题目访问策略迁入
  `service.question`；只调整包归属和必要可见性，不改变计算、权限或原文件校验逻辑。
- 新增 ArchUnit 规则，要求 `com.learnplatform.service` 顶层类必须是 Spring Service /
  Component；领域辅助类型仍可进入有名称的业务子包。
- 更新后端架构与工程规则，记录顶层 Service、领域辅助类型和 DTO 子包的稳定归属。

### Red / Green 与验证

- Red：规则初版因包含嵌套类产生 39 条超范围违规；限定顶层类后，精确识别 4 个待迁移
  类型。该选择器修正保留在最终规则中，避免把 Service 私有嵌套实现误判为包级类型。
- Green：ArchUnit、私有原文件 Service 与考试 Controller 聚焦测试 19 个通过。
- 后端 `mvn clean verify`：565 个默认测试、Checkstyle 0 违规、SpotBugs 0 问题、
  JaCoCo 门槛通过。
- 本轮无数据库迁移、事务或外部 API 行为变化，未重复执行 Testcontainers 与前端测试。

### 遗留问题

- 其余 Service 和 DTO 仍以顶层技术分层为主；继续按独立业务边界渐进迁移，不做一次性
  全仓改包。

## Round 258：统一独立管理端页面目录

### 完成内容

- 将 11 个管理端页面和题目管理的 2 个同域辅助文件从
  `frontend/src/views/admin/` 整体迁入 `frontend/src/admin/views/`，与独立管理端入口、
  Router 和布局形成单一应用边界。
- 将管理端 Router 的业务页面懒加载改为同应用相对导入，并更新两个直接导入页面的
  Vitest；页面实现、路由路径、API 与交互行为未改变。
- 修正前端架构与工程规则中学习端 / 管理端页面归属描述，并扩展仓库结构门禁，拒绝
  后续在学习端目录重新提交管理页面。历史 changelog 的旧路径保持不变。

### 验证

- 仓库结构门禁 5 个聚焦测试通过。
- 前端 `npm run format:check`、`npm run lint`、`npx vue-tsc --noEmit` 通过。
- `npm run coverage`：59 个测试文件、310 个测试通过，语句 37.39%、分支 31.54%、
  函数 29.27%、行 38.80%，门槛通过。
- `npm run build`：学习端与管理端双生产构建通过；保留既有 VueUse 纯注解位置和
  Mermaid 按需 chunk 超过 600 kB 的非阻塞警告。
- 本轮是无行为变化的目录迁移，未重复执行浏览器流程与后端测试。

### 遗留问题

- 管理端仍有多个 900 行以上页面，需要按真实业务区域继续拆分；本轮只解决应用目录
  归属，不把文件移动冒充页面职责拆分。

## Round 257：拆分学习诊断前端页面

### 完成内容

- 将 1099 行 `LearningDiagnosisView.vue` 收敛为 207 行页面编排，页面继续独占诊断 API、
  AI SSE、路由跳转和练习会话状态。
- 在 `components/statistics/` 下新增概览与习惯、错因分析、课程与推荐三个领域面板，
  以及单题错因和相似题两个弹窗；组件只接收类型化数据并发出用户操作事件。
- 将正确率、掌握状态、推荐原因等纯展示映射集中到同域工具，避免五个组件重复判断。
- 将原 scoped 样式随各展示区域迁移到对应组件，继续使用 Element Plus 和既有 CSS
  tokens，不引入新组件库、模拟业务流或 API 变化。
- 新增页面编排测试，覆盖诊断加载与组件传参、错因详情请求、推荐题进入既有练习会话。

### 验证

- 聚焦 Vitest：1 个文件、3 个测试通过。
- 前端 `npm run format:check`、`npm run lint`、`npx vue-tsc --noEmit` 通过。
- `npm run coverage`：59 个测试文件、310 个测试通过，语句 37.39%、分支 31.54%、
  函数 29.27%、行 38.80%，门槛通过。
- `npm run build`：学习端与管理端双生产构建通过；保留既有 VueUse 纯注解位置和
  Mermaid 按需 chunk 超过 600 kB 的非阻塞警告。
- 临时开发服务器可访问目标路由，但无已登录会话且后端未运行，浏览器只呈现登录态；
  未将诊断数据页桌面 / 移动布局记为已验证。

### 遗留问题

- 其他 900 行以上页面仍包括管理端题目、AI 用量、考试列表、课程总览、投稿与试卷管理。
- 诊断页面的真实数据视觉回归需在可用的登录态与后端环境中补做。

## Round 256：拆分练习查询与判分职责

### 完成内容

- 将原先 478 行、直接依赖 13 个组件的 `PracticeService` 收敛为 58 行兼容门面，保留
  Controller 使用的六个公共方法。
- 将普通练习、错题重练、收藏题查询及答案脱敏迁入题目查询服务；将练习记录分页和
  统计迁入历史服务；将校验、判分、练习事实、错题、复习计划、缓存和课程事件写入
  迁入独立判分服务。
- `@Transactional` 保留在真正执行判分与写入的公开服务方法上，外部 API、访问策略、
  正确答案计算和非关键副作用降级行为保持不变。
- 更新后端架构文档，记录练习门面及三个协作者的责任边界。

### 验证

- 练习 Service、Controller 与 ArchUnit 聚焦测试 21 个通过。
- 后端 `mvn clean verify`：564 个默认测试、Checkstyle 0 违规、SpotBugs 0 问题、
  JaCoCo 门槛通过。
- 尝试执行 `PracticeServiceIntegrationTest`，但本机 Docker 未运行，Testcontainers 在
  ApplicationContext 初始化前因找不到 `/var/run/docker.sock` 失败；没有把该外部环境
  失败记为测试通过。

### 遗留问题

- 本轮尚未取得真实 MySQL 聚焦验证结果；远端集成测试 job 仍应覆盖该测试。
- `PracticeAnswerService` 保留 8 个直接依赖，因为它们共同参与一次判分后的事实写入与
  可降级副作用；继续拆事务内编排的收益暂不足以抵消一致性风险。

## Round 255：拆分学习诊断服务职责

### 完成内容

- 将原先 871 行、直接依赖 12 个组件的 `LearningDiagnosisService` 收敛为 100 行兼容
  门面，保留既有缓存、公共方法和 Controller 调用方式。
- 将用户练习、错题、知识点和题目—知识点关联查询集中到数据加载器；将知识点与课程
  掌握、错因分布、学习习惯及规则建议分别迁入独立分析器。
- 将同步与流式 AI 建议、异常日志和调用审计迁入独立服务；单题错因和相似题推荐继续
  委托既有专用服务，不改变接口、统计口径或数据写入行为。
- 更新后端架构文档，记录学习诊断门面和各协作者的稳定责任边界。

### 验证

- 学习诊断 47 个既有回归测试与 ArchUnit 4 个测试通过。
- 后端 `mvn clean verify`：564 个默认测试、Checkstyle 0 违规、SpotBugs 0 问题、
  JaCoCo 门槛通过。
- 本轮无数据库迁移或跨流程行为变化，未重复执行 Testcontainers 与 Playwright。

### 遗留问题

- 学习诊断协作者暂时仍位于顶层 `service` 包；业务域分包将在依赖门禁补齐后渐进进行，
  不以一次性大规模移动制造无收益冲突。
- 其余高耦合 Service 与超大 Vue 页面仍需继续拆分。

## Round 254：拆分用户端考试与私有试卷控制器

### 完成内容

- 将原先 376 行、依赖 10 个 Service 的 `ExamController` 拆为限时考试、试卷查询、
  试卷学习、私有试卷导入、草稿复核和来源文件六个 HTTP 边界。
- 各 Controller 现为 49–120 行，最多依赖 4 个 Service；限时考试、试卷学习、私有
  导入与原文件访问仍使用原路径、原身份上下文和原响应契约。
- 新增限时考试和试卷查询 MockMvc 测试，覆盖开始/恢复、提交、结果、记录、可访问
  试卷列表与详情；既有私有试卷和试卷学习 10 个契约测试继续通过。

### 验证

- 考试 Controller 与 ArchUnit 聚焦测试 17 个通过。
- 后端 `mvn clean verify`：564 个默认测试、Checkstyle、SpotBugs 与 JaCoCo 门禁通过。
- `git diff --check` 通过；无 Service、数据库或事务行为变化，未重复执行 Testcontainers。

### 遗留问题

- `PrivateExamSourceFile` 仍是放在顶层 `service` 包的值对象，将随考试业务域包整理迁移。
- 高耦合 Service 和超大 Vue 页面仍是后续持续维护重点。

## Round 253：拆分管理端题目控制器职责

### 完成内容

- 将原先同时依赖 7 个 Service、覆盖 CRUD、来源、纠错、复审、版本和文件交换的管理端
  题目控制器拆为查询维护、治理和导入导出三个独立 HTTP 边界。
- 保持 `/api/admin/questions` 下全部既有路径、参数、权限上下文与响应结构不变；现有前端
  和 API 文档无需迁移。
- 将百余行 Markdown 导入模板从 Java Controller 字符串迁入 classpath 资源，模板下载
  只负责 HTTP 响应头与字节传输。
- 补充模板下载契约测试，并让同一组 MockMvc 测试同时装配三个 Controller，防止拆分后
  路由遗漏或冲突。

### 验证

- 管理端题目 Controller 与 ArchUnit 聚焦测试 20 个通过。
- 后端 `mvn clean verify`：562 个默认测试、Checkstyle、SpotBugs 与 JaCoCo 门禁通过。
- `git diff --check` 通过；无数据库、事务或业务行为变化，未重复执行 Testcontainers。

### 遗留问题

- 用户端 `ExamController` 仍聚合试卷、私有导入、原文件、限时考试和学习会话入口，后续
  按稳定路由边界继续拆分。
- 高耦合 Service 与超大 Vue 页面尚未处理完毕。

## Round 252：清理异常测试路径并建立仓库结构门禁

### 完成内容

- 删除误提交为 `CourseControllerTest.java</path` 的重复测试文件；正常的
  `CourseControllerTest.java` 保留且两者原内容逐字节一致，不改变实际测试覆盖。
- 新增 Git 跟踪路径检查，拒绝跨平台非法字符、尾随空格或句点、构建产物目录与系统
  垃圾文件，避免源码因异常扩展名或目录被构建工具静默忽略。
- 将仓库结构检查接入 CI 文档任务，并把稳定路径规则写入工程规范。
- 按用户明确要求保持 CONTINUOUS，后续继续处理高耦合后端类、超大前端页面和业务域
  包边界。

### 验证

- 仓库结构脚本 4 个聚焦测试通过；当前 951 个有效 Git 跟踪路径检查通过。
- 文档结构与链接检查通过；`git diff --check` 通过。

### 遗留问题

- 后端顶层 `service` / `dto` 仍然过度扁平，若干 Controller 与 Service 依赖过多。
- 多个管理端和学习端 Vue 页面仍超过 500 行，需要按真实业务区域渐进拆分。

## Round 251：收紧 Spring Boot 分层与持久化边界

### 完成内容

- 将管理端用户与 AI 调用日志的数据访问、查询组合和事务从 Controller 下沉到独立
  Service，Controller 只保留 HTTP 参数、身份上下文与响应封装。
- 用户列表、统计、配额审计、AI 调用日志、待复审题目和 AI 资产反馈统一通过 DTO / VO
  返回，不再从 HTTP 层暴露 MyBatis 持久化 Entity；既有 JSON 字段保持兼容。
- 将登录 IP 限流服务从 `config` 迁入 `security`，使配置、安全策略和业务服务归属一致。
- 引入 ArchUnit 并建立自动架构门禁：禁止 Controller 依赖 Mapper / Entity、禁止 Controller
  声明事务、禁止底层反向依赖 Controller、禁止 `config` 包承载 `@Service`。
- 更新后端架构文档，明确 DTO 边界、事务归属和自动依赖检查。

### Red / Green 与验证

- Red：架构测试首次运行识别出 84 个 Controller 到持久化层的字节码依赖和 1 个 Controller
  事务边界，稳定覆盖现有越层问题。
- Green：聚焦测试 86 个通过；认证与限流聚焦测试 20 个通过。
- 后端 `mvn clean verify`：561 个默认测试、Checkstyle、SpotBugs 与 JaCoCo 门禁通过。
- 真实 MySQL Testcontainers：13 个集成测试类、77 个用例通过。
- 仓库脚本 23 个测试、文档检查与 `git diff --check` 通过。

### 遗留问题

- 现有按技术层平铺的包结构继续保留；当前没有证据表明一次性迁移为按功能分包能抵消
  大范围文件移动和冲突成本，后续只随独立业务边界渐进调整。
- 本轮未重构仍在演进中的学习诊断长类，也未为了形式统一增加无实际多实现需求的
  `Service` / `ServiceImpl` 接口层。

## Round 250：维护审计、诊断隐私修复与长文件拆分

### 完成内容

- 修复学习诊断推荐的权限缺口：薄弱知识点候选和反复错题在返回题干前统一校验题目
  已启用且对当前用户可见，其他用户私有题和停用题不再进入推荐结果。
- 修复未开始知识点的建议文案：保留 `NOT_STARTED` 事实，不再把内部哨兵值 `-1.0`
  显示成负正确率。
- 从 1,016 行 `LearningDiagnosisService` 抽出独立推荐服务，主类降至约 870 行；推荐
  选择、权限过滤和响应映射形成单一职责边界。
- 从 1,277 行 `AiUsageView` 抽出 ECharts 注册、三张图渲染、resize 与 dispose 生命周期
  composable，页面降至约 1,150 行并只保留页面取数和展示编排。
- 修正系统架构文档中“独立前端尚未完成”的过期描述，与现有双入口、双 Router、双构建
  事实保持一致。

### Red / Green 与验证

- Red：新增回归用例稳定复现其他用户私有题进入每日推荐；新增断言复现未开始知识点显示
  `-1.0%`。
- Green：`LearningDiagnosisServiceTest` 47 个测试通过；前端 ESLint、vue-tsc 和 AI 用量
  API 聚焦 Vitest 6 个测试通过。
- 审计基线：后端 `mvn clean verify -B`、前端 lint / format / coverage / 双生产构建、
  仓库脚本 23 个测试、文档检查、Compose 配置与 `git diff --check` 均通过；生产 npm
  依赖审计为 0 漏洞。

### 遗留问题

- Vite 8 / Rolldown 对第三方 `@vueuse/core` 的纯注解位置给出非阻塞警告；业务构建成功，
  不修改 `node_modules`。
- Mermaid 高级解析器产物仍有一个约 663 kB 的按需 chunk，超过当前 600 kB 提示线；它
  不属于首屏业务 chunk，本轮不通过提高阈值掩盖提示。
- 未执行真实 Docker Playwright：本轮后端行为由服务测试覆盖，前端为图表职责等价搬迁，
  完整 E2E 成本与风险不相称。

## Round 249：Final Fix Pass 收尾修复

### 完成内容

- 修复课程空间学习工具路由 bug：`CourseTool` 改为显式 `routeName` 字段并映射到
  已注册路由名（Practice / Review / WrongQuestions / ExamList / QuestionList），
  五个入口均携带 `courseId` 跳转，不再把 URL path 当路由名使用。
- 补充课程工具入口测试：CourseOverviewView 单测覆盖五个工具的目标路由名与课程
  参数；新增路由名解析测试，直接解析真实路由表，route name 写错时立即失败。
- 移除 App Shell 顶部重复页面标题：桌面端 Header 不再渲染 `<h1>`，页面主标题由
  `LpPageHeader` 或页面内容唯一承担；个人中心、我的收藏、题目投稿标题统一为
  一级标题，每个一级页面只有一个主要 `<h1>`，移动端导航不受影响。
- 收敛课程空间操作层级：Hero 只保留一个主操作（存在推荐目标时「继续学习」，
  否则「开始学习」），课程目录保留为次级按钮，阶段测评、测评历史、课程试卷收纳
  进「更多」下拉，学习工具保持课程工具形态而非五个平级 CTA。
- 用户文案自然化：课程空间、我的课程、Tutor 相关记录等改为自然、简短的学习软件
  用语，保留数据真实性与掌握度克制说明，不向用户解释产品设计动机。
- 课程库默认聚焦 408：历史课程数据与旧业务入口保留，但「更多课程」区域只在搜索
  时出现，不抢占最终 Showcase 展示位。
- README 清理已失真的历史截图描述；未新建或恢复任何截图工作量。

### 验证

- 前端 `npm run format:check`、`npm run lint`（0 错误 0 警告）、`npx vue-tsc
  --noEmit`、`npm run coverage`（58 个测试文件、307 个测试通过）、`npm run build`
  （学习端与管理端双生产构建）通过。
- 真实 Docker Playwright E2E：12 条原有 + 1 条课程空间工具导航共 13/13 通过
  （全新隔离 E2E 数据库），新增测试验证五个工具入口 URL、页面标题与 `courseId`
  携带且全程无 console.error。
- 后端无代码改动，后端 CI 不受影响。

### 遗留问题

- Tutor 课件渲染评估结论：抽 `TutorVisualizationRenderer` 会引入动态组件 props
  联合类型放宽并触碰已验证的 Tutor 单测，属于低收益代码洁癖重构，按 P2 跳过，
  维持现状。
- 历史 9 条 Phase 24 批量提交正文含非项目符号段落，Commit Message 校验只在 push
  新增范围生效，已通过新的合法提交收敛，不重写已 push 历史。

## Round 248：Phase 24 阶段退出（真实 E2E 全量回归）

### 完成内容

- 恢复 Docker 环境并启动隔离 E2E 栈（`learnplatform-e2e` 独立项目，全新数据库），
  执行 Playwright E2E 全量回归：**12/12 通过**（约 1.7 分钟），覆盖登录默认进入
  我的课程、课程库/课程详情/加入课程库、课程空间阶段测评与限定测评、2026 真题
  学习模式与限时考试及可信来源复盘、主观题人工批阅固化总分、私有试卷导入
  （Markdown/PDF/DOCX）、AI 草稿复核启用、考试刷新恢复与服务端判分、错题掌握度
  与重练、投稿审核入库；E2E 同时断言全程无 5xx 接口与 console.error。
- 修复 E2E 过渡竞态：AppLayout 页面切换过渡（out-in）期间旧页面仍在 DOM，课程
  详情 heading 断言会命中旧课程库卡片标题；改为先等详情页独有的「查看题目」按钮
  再判断「加入课程库 / 进入课程空间」分支。
- `docs/project/status.md`：Phase 24 标记已完成，最新验证基线更新 Round 248 真实
  Docker E2E 12/12；项目进入 Finished / Maintenance，下一步指向维护清单。
- `docs/product/roadmap.md`：阶段总览新增 Phase 24 已完成，补充 Phase 24 章节与
  Stop / Transition Condition。

### 验证

- 真实 Docker Playwright E2E：12/12 通过（全新隔离数据库，等价 CI 运行）。
- 前端 typecheck / 297 测试 / lint / format / 双生产构建、后端 `mvn clean verify`、
  check-docs、仓库脚本测试、路由一致性审计均通过（Round 247 基线）。

### 遗留问题

- 演示截图按用户指示跳过；`capture-demo-screenshots.mjs` 保留，需要时在 E2E 栈
  运行期间重新生成。
- Finished / Maintenance 维护清单：后端星号导入展开、learning-plan 无入口端点清理。

## Round 247：最终展示版 UI/UX 重构（Phase 24）

### 完成内容

- 修复相似题/推荐练习跳转断链：WrongQuestionView 与 LearningDiagnosisView 的
  「相似题练习 / 今日推荐练习」原先只传 `?questionIds=`，而答题页只读 sessionStorage；
  现改为按 ID 拉取完整题目写入会话存储（practice_mode=similar/recommended），答题页
  返回/重练按来源路由，恢复真实可用的练习闭环。
- 建立 Design Tokens（`frontend/src/assets/styles/tokens.css`）作为唯一视觉来源：
  颜色（暖白纸面 + 深蓝灰主色）、字体、4px 间距节奏、圆角、边框、阴影、动效、布局与
  z-index；重构 `global.css` 并统一 Element Plus 覆盖；认证页浅色统一。
- 建立 `components/ui/` 基础组件库（LpPageHeader / LpSectionHeading / LpStat /
  LpEmptyState / LpSkeleton / LpDivider / LpSignal / LpProgress / LpKicker）并全局注册。
- 重构 App Shell：浅色极简 Sidebar（我的课程 / 课程库 / 搜索 / 用户区）、新增沉浸式
  FocusLayout（Tutor、限时考试、试卷学习、考试结果）；登录默认进入「我的课程」，
  根路径重定向；删除旧 Dashboard 首页 HomeView。
- 重做「我的课程」（Continue + 学习信号 + 空状态）、「课程库」（408 计算机统考分类 +
  三门规划中占位课程）、「课程详情」（Discover→Understand→Add，改用服务端
  contentReviewStatus 识别已审查内容）、「课程空间」（继续学习 / 目录 / 学习工具 /
  真实学习事实，测评弹窗拆分为三个领域组件）。
- 重做 Tutor 教学页（教材式排版、理解检查反馈动效、跨入口学习证据），练习/复习/错题/
  考试/试卷学习/考试结果页面统一到新视觉语言；1572 行 ExamListView 拆分出
  `components/exam/PrivateExamImportDialog.vue`。
- 信息架构收敛：删除学习报告 / 学习路径 / 知识图谱 / AI 复习建议四个旧页面及其路由与
  前端 API（statistics.ts / ai.ts 无用导出、learningPlan.ts）；学习诊断从一级导航隐藏但
  保留路由；管理端样式与学习端 tokens 对齐。
- E2E 与演示截图脚本同步新 IA；README、演示文档、简历材料、前端架构文档更新。

### 验证

- 前端 `npx vue-tsc --noEmit` 通过；Vitest 57 个文件、297 个测试通过（含新增 format
  工具与重构后视图测试）；`npm run coverage` 覆盖率门禁通过（全源码 36.6% 语句 /
  31.42% 分支 / 29.07% 函数 / 37.91% 行，高于 12/8/10/12 门槛）；ESLint 0 错误 0
  警告；Prettier 全量通过；`npm run build` 学习端与管理端双生产构建通过。
- 路由一致性审计：全仓未发现指向未注册路由名/路径的引用（管理端独立路由与子路由
  相对路径为预期结构）；无明显 broken route。
- 后端 `mvn clean verify -B` 通过（默认测试 + Checkstyle + SpotBugs + JaCoCo 门禁）。
- 待完成：Docker 环境恢复后执行 Testcontainers 与 Playwright E2E 全量回归；演示
  截图按用户要求跳过（旧截图已清理，`capture-demo-screenshots.mjs` 保留可随时重生成）。

### 遗留问题

- 后端 learning-plan 统计端点已无前端入口，保留为后续清理项（不破坏数据表）。
- Docker Desktop 处于暂停状态，E2E 与截图待环境恢复后验证。

## Round 246：代码工程化收口

### 完成内容

- 后端 Checkstyle 从 6 条规则扩展到约 30 条（import 卫生、命名、控制流大括号、switch
  default、单行多语句/多变量分行、修饰符顺序与空白、120 行长），修正全部存量违规并移除
  AiExamGenerationService 中空 if 块包裹的死代码查询；星号导入展开保留为后续独立清理。
- 前端 Prettier 强制范围从少量管理端文件扩展到全量 src / admin / e2e / 根配置，新增
  `format` 写回脚本，93 个文件统一到 Prettier 风格，仅格式变化不改变行为。
- 从 LearningDiagnosisService 抽取约 120 行纯提示词拼接逻辑为独立组件
  LearningDiagnosisPromptBuilder，并新增 3 个单测；服务从约 1137 行收敛到约 1016 行。
- 抽取 ExamListView / ExamManage 重复的 formatTime 与 formatStorage 为
  `src/utils/format.ts`，新增 5 个单测，消除跨页面重复的纯展示逻辑。

### Red / Green 与验证

- 行为不变的重构与纯格式化，未制造 Red；抽取组件后分别新增针对性单测作为回归保护。
- 后端 `mvn verify`：553 个默认测试通过、Checkstyle 0 违规、SpotBugs 0 问题、JaCoCo
  覆盖率门槛通过。
- 前端 lint 0 错误 0 警告、format:check、`npm run build`（学习/管理双构建）通过；
  `npm test` 58 个测试文件、305 个测试通过。
- 本轮未跑 Testcontainers 与 Playwright：无数据库迁移或跨层业务流程变化，沿用既有基线。

### 遗留问题

- 星号导入展开（约 109 处 `import xxx.*`）作为独立清理项，本轮刻意避免一次性大规模
  机械改动；现已由 UnusedImports / RedundantImport 覆盖部分卫生诉求。
- 巨型视图（ExamListView 1581 行、AiUsageView 1279 行、QuestionManage 1206 行）的
  对话框级组件拆分因与父组件状态强耦合，本轮仅完成纯工具抽取，未做深层拆分。
- 未授权 push，最高状态为 LOCALLY_VERIFIED；CI 待 push 后验证。

## Round 245：Phase 23 阶段退出

### 完成内容

- 执行 Phase 23“AI 课程学习平台转型”阶段退出：Exit Criteria 已全部满足、Remaining
  Required 为空，本轮按 Stop / Transition Condition 关闭该阶段，不继续新增 Required。
- `docs/project/status.md`：当前阶段标记 Phase 23 为已完成并指向下一阶段方向，最新验证
  基线新增 Round 245 L3 门禁记录（后端默认测试 549→550），修正 Round 242 基线中“前端
  lint 70 条 any 警告”的过期行，更新下一步与最后整理日期。
- `docs/product/roadmap.md`：阶段总览把 Phase 23 从“Exit 条件已满足，待退出”改为“已完成”。
- `docs/project/changelog/index.md` 与 `README.md` 同步阶段状态。

### Red / Green 与验证

- 纯文档与阶段状态收口，无业务行为变化，未制造 Red。
- L3 门禁（Phase Exit）：后端 Java 21 `mvn clean verify -B` 550 个默认测试通过、Checkstyle
  0 违规、SpotBugs 0 问题、JaCoCo 覆盖率门槛通过；前端 lint 0 错误 0 警告、format:check、
  coverage（57 文件 / 300 测试）与学习/管理双构建通过；仓库脚本 23 个测试、`check-docs.py`
  59 个文档、`docker compose config --quiet`、`git diff --check` 通过。
- 本轮未重跑 Testcontainers 与 Playwright：自 Round 242 全量门禁后无数据库迁移或跨层流程
  变化，按测试策略沿用该基线（13 类 77 用例、12 条 E2E）。

### 遗留问题

- 未授权 push，最高状态为 LOCALLY_VERIFIED；CI 待 push 后验证。
- 下一阶段方向（更多课程与内容、学习画像、可视化升级、桌面端等）待用户决定，NORMAL 模式停止。

## Round 244：Docker 磁盘增长治理

### 完成内容

- 建立 Docker 磁盘增长治理机制：明确占用定位顺序（images → build cache →
  containers → volumes）、构建缓存 4GB 预算、悬空镜像安全回收，以及“安全回收 vs
  需用户授权清理”的两级边界；不引入守护进程或额外服务。
- 新增 `scripts/docker-disk.py`（`report` 诊断 + `reclaim` 安全回收）与对应 unittest，
  只回收构建缓存超预算部分和悬空镜像，不触碰数据卷、容器、带 tag 镜像或其他项目资源。
- 将 Docker build、Compose 重建与完整 Docker E2E 纳入 L2 / L3，不作为默认 L1；普通
  局部修改和普通 commit 不再反复完整重建 Docker 环境。
- E2E 使用独立 Compose 项目名 `learnplatform-e2e`，`down -v --rmi local` 只清理 E2E
  自己的容器、网络、卷与构建镜像，不再误删开发环境持久化数据。

### Red / Green 与验证

- 纯文档、脚本与 Compose 配置治理，无业务行为变化，未制造 Red。
- `python3 scripts/docker-disk.py report`：正确输出当前 Docker 占用与归属分类，并
  识别出其他项目卷（lifepilot、docker_、vscode 等）和容器（lifepilot-mysql、
  gapi-postgres、gapi-rabbitmq）不得自动清理。
- `python3 scripts/docker-disk.py reclaim --dry-run`：验证只执行构建缓存与悬空镜像
  两类安全回收。
- `python3 -m unittest discover -s scripts/tests -p 'test_*.py'`：23 个测试通过。
- `python3 scripts/check-docs.py`、`docker compose config --quiet`、
  `docker compose -f docker-compose.yml -f docker-compose.e2e.yml config --quiet`
  （确认合并后项目名为 `learnplatform-e2e`）、`git diff --check`：通过。
- 刻意未运行完整 `docker build` / `docker compose up --build` / Playwright E2E：
  本轮只改治理规则、脚本与文档，不涉及镜像或业务代码，避免为了验证“减少构建”而
  再次执行昂贵构建。

### 遗留问题

- 未授权 push，最高状态为 LOCALLY_VERIFIED；CI 的 Docker Build 与 Browser E2E job
  待 push 后由 CI 验证，本轮未在本地重复运行。

## Round 243：自主开发执行机制与验证策略收口

### 完成内容

- 修复 ExamService 考试时间测试对机器系统时区与真实墙钟的隐式依赖：全部“当前时间”
  统一来自可注入 `Clock`，生产默认 `Asia/Shanghai`（业务语义不变）；`ExamServiceTest`
  使用 `Clock.fixed` 并新增“恰好到达截止时间即超时”的边界用例，`ExamServiceIntegrationTest`
  的超时用例改为以服务端 Asia/Shanghai 墙钟为基准。
- 修复 Commit Message CI 在授权历史重写 / force push 后旧 BASE_SHA 不可达时的错误
  失败：CI 通过 push 事件显式传入 `FORCED_PUSH`；普通 push 的不可达基线仍明确失败，
  明确 force push 的不可达基线只警告并校验当前 HEAD，不检查整个仓库历史。
- 测试策略明确划分为 L1（开发循环）/ L2（模块与业务闭环）/ L3（项目级门禁），建立
  昂贵验证重复执行规则，明确 Round 不再是工程提交或测试单位。
- 建立 NORMAL / CONTINUOUS 两种执行模式及其持久化（`docs/project/status.md` 的
  “Agent 执行状态”），建立 IMPLEMENTED / LOCALLY_VERIFIED / DELIVERED 三种完成状态，
  并把 CI failure 定义为最高优先级任务。
- 重新梳理 Phase 23：Goal / Exit Criteria / Remaining Required（空）/ Backlog /
  Stop Condition 全部落文档；原“课程总览按知识点学习事实面板”等增强移入 Backlog，
  不再阻止阶段退出。本轮未继续新增任何 Phase 23 产品功能。

### Red / Green 与验证

- Red：`mvn test -Dtest=ExamServiceTest -DargLine="-Duser.timezone=UTC"` 复现 3 失败
  1 错误（与 2026-08 连续 4 次真实 CI failure 一致）；Green 后 UTC 与默认时区均为
  14 个测试通过。
- Commit validator：`scripts/tests` unittest 14 个测试通过（新增普通可达、普通不可达、
  force 不可达、HEAD 非法、force 且 BASE 可达五类场景）；CLI 端到端模拟普通 push、
  普通不可达失败、force 不可达降级通过三种 CI 场景。
- `python3 scripts/check-docs.py` 通过；`mvn clean verify -B` 通过（详见状态基线）。
- 按“本轮自身也遵守 L1/L2/L3”原则：未运行 Testcontainers（无数据库迁移）、未运行
  前端测试与 Playwright（无前端改动）、未运行 Docker 全环境。

### 遗留问题

- 真实 CI 因本轮的 exam/validator 修复恢复 Green 需要 push 后由 CI 验证；本轮未授权
  push，最高状态为 LOCALLY_VERIFIED。
- Phase 23 退出条件已满足，等待用户决定阶段退出与下一阶段方向。

## Round 242：阶段测评历史按知识点筛选闭环

### 完成内容

- 测评历史接口 `/my-courses/{courseId}/stage-assessments` 新增可空 `knowledgePointId`：按逐题快照的
  知识点归属在数据库分页前过滤（`JSON_CONTAINS` 命中 `knowledge_points_json` 中的知识点 ID），
  与本人/课程归属条件组合，分页总数同源。
- 测评历史弹窗新增“按知识点筛选”下拉（只列出课程总览已审查知识点，默认全部），切换后回到第一页
  重新查询；与详情/复盘入口保持一致。
- 只呈现真实历史事实，不推断掌握度、趋势或个性化结论；API 权威文档同步筛选契约。

### Red / Green 与验证

- Red：历史按知识点筛选不存在时，聚焦用例先失败；Green 后服务端快照过滤、分页总数、前端筛选与
  类型检查均通过。
- 真实 MySQL 聚焦验证：限定范围测评完成后，按该知识点筛选历史只返回对应记录，无关联知识点返回空。
- 提交前全量门禁：后端 549 个默认测试、Checkstyle、SpotBugs 和 JaCoCo 门槛通过；前端 57 个文件、
  300 个测试、ESLint（0 错误、0 警告）、格式检查与学习/管理双构建通过；13 个 Testcontainers 类共
  77 个真实 MySQL 用例、12 条真实 Docker Playwright E2E、文档、脚本、Compose 配置和差异检查通过。

## Round 241：到期复习按知识点筛选闭环

### 完成内容

- 复习接口 `/api/review/due` 新增可空 `knowledgePointId`：按该知识点关联的题目在数据库分页前筛选，
  可与课程、目标题目组合生效；无关联题目的知识点明确返回空结果（与错题按知识点筛选对称）。
- 复习页从测评复盘/知识点入口深链进入时按知识点筛选，并展示可清除的“知识点：X”筛选标记；
  课程总览“开始学习”的到期复习目标原本携带的 `knowledgePointId` 现在真正生效。
- 只呈现真实复习事实，不推断掌握度、趋势或个性化结论；API 权威文档同步筛选契约。

### Red / Green 与验证

- Red：按知识点筛选不存在时，聚焦用例先失败；Green 后服务端组合筛选、控制器参数、前端深链与
  筛选标记、类型检查均通过。
- 聚焦单元与控制器测试覆盖知识点筛选只返回关联题、无关联知识点返回空结果、参数透传。
- 提交前全量门禁：后端 548 个默认测试、Checkstyle、SpotBugs 和 JaCoCo 门槛通过；前端 57 个文件、
  299 个测试、ESLint（0 错误、0 警告）、格式检查与学习/管理双构建通过；13 个 Testcontainers 类共
  77 个真实 MySQL 用例、12 条真实 Docker Playwright E2E、文档、脚本、Compose 配置和差异检查通过。

## Round 240：前端存量显式 any 收紧闭环

### 完成内容

- 新增 `src/utils/errors.ts`：统一从 unknown catch 变量提取可读错误信息（兼容业务 Error、
  Axios `response.data.message` 与字符串异常），并提供 `isAbortError` 与 el-tag 语义色板类型
  `SemanticTagType`。
- 全部 70 条存量显式 `any` 警告清零：catch 子句改为 unknown 并通过错误工具提取信息；
  题型/状态 tag 映射改用 `SemanticTagType`；管理端与统计视图的 `res: any`/`(res as any)` 改为
  真实 API 响应类型；echarts 事件与 tooltip 参数用 unknown + 收窄；`request.ts` 成功拦截改为
  `as unknown as AxiosResponse`；测试夹具补齐 `UserInfo` 缺失字段。
- 顺带把 PracticeView 直接调用 `request.get('/courses')` 收敛为既有 `getCoursePage` API，
  ReviewView 的多语句模板表达式改为 `finishReview` 函数（避免 Prettier 与模板解析冲突）。
- `npm run lint` 从“0 错误、70 条警告”变为 **0 错误、0 警告**；行为与接口无变化。

### Red / Green 与验证

- 逐文件替换后 `vue-tsc` 与 ESLint 全程保持 0 错误；Prettier 曾对多语句模板表达式产生不可解析
  输出，改为函数后通过格式检查。
- 前端全量门禁：57 个文件、298 个测试、ESLint（0 错误、0 警告）、格式检查与学习/管理双构建通过；
  12 条真实 Docker Playwright E2E 通过（后端自 Round 238 门禁后无改动，546/77 基线不变）。

## Round 239：生产依赖安全升级闭环（ECharts 6）

### 完成内容

- ECharts 由 5.6.0 升级到 6.1.0，修复 `GHSA-fgmj-fm8m-jvvx`（中危 XSS）；五个使用
  `echarts/core` 树摇 API 的视图（知识图谱、学习报告、首页、AI 用量、管理总览）保持 API 兼容，
  双生产构建与类型检查通过。
- `npm audit fix` 一并修复 js-yaml（`CVE-2026-59870`，eslint 传递依赖）与 mermaid
  （原型污染/CSS 注入公告）到已修复版本；`npm audit` 达到 0 漏洞。
- 真实浏览器回归：高频用户与管理页面、学习报告等可视化页面在 ECharts 6 下正常渲染；
  其余 11 条真实 Docker Playwright 流程无回归。
- 依赖升级不改变任何业务接口或行为；`package.json` 与锁文件为唯一产物变更。

### Red / Green 与验证

- 升级前 `npm audit` 报 ECharts 中危；升级到 6.1.0 并执行审计修复后 `npm audit` 为 0 漏洞。
- 前端全量门禁：57 个文件、298 个测试、ESLint（0 错误、70 条既有警告）、格式检查与学习/管理
  双构建通过；12 条真实 Docker Playwright E2E 通过（在全新数据卷环境执行）。

## Round 238：课程总览最近测评知识点事实摘要闭环

### 完成内容

- 最近完成测评摘要随快照返回各知识点题数与正误数（与完成详情共用同一聚合逻辑，只读创建时固化的
  逐题知识点快照）。
- 课程总览活动面板直接展示“知识点：栈 2/3 · 队列 1/2”形式的事实行，不打开复盘即可看到最近一轮
  各知识点归属；仍可从该摘要进入逐题复盘。
- 标注与统计只呈现题目归属和作答正误事实，不推断掌握度、趋势或个性化结论；API 权威文档同步
  `latestStageAssessment` 的知识点事实摘要契约。

### Red / Green 与验证

- Red：总览摘要的知识点事实不存在时，聚焦用例先失败；Green 后服务端聚合、摘要返回、前端展示、
  类型检查均通过。
- 真实 MySQL 聚焦验证：限定范围测评完成后，课程总览最近摘要按知识点返回本轮题数与正确数。
- 提交前全量门禁：后端 546 个默认测试、Checkstyle、SpotBugs 和 JaCoCo 门槛通过；前端 57 个文件、
  298 个测试、ESLint（0 错误、70 条既有警告）、格式检查与学习/管理双构建通过；13 个 Testcontainers
  类共 77 个真实 MySQL 用例、12 条真实 Docker Playwright E2E、文档、脚本、Compose 配置和差异检查通过。

## Round 237：错题复习按知识点筛选闭环

### 完成内容

- 错题接口新增可空 `knowledgePointId`：按该知识点关联的题目在数据库分页前筛选，可与课程、目标题目
  和掌握状态组合生效；无关联题目的知识点明确返回空结果。
- 测评复盘的知识点汇总对存在错题的知识点提供“复习该知识点错题”入口，深链携带 `knowledgePointId`
  与 `knowledgePointName`；错题页以可清除的知识点筛选标记展示当前范围，清除后恢复全量错题。
- 逐题错题复习深链保持不变；筛选与入口只呈现真实错题事实，不推断掌握度、趋势或个性化结论。
- API 权威文档同步错题筛选契约。

### Red / Green 与验证

- Red：按知识点筛选不存在时，聚焦用例先失败；Green 后服务端组合筛选、控制器参数、前端深链与
  筛选标记、类型检查均通过。
- 真实 MySQL 聚焦验证：知识点筛选只返回关联该知识点的错题，课程与知识点组合筛选同时生效，无关联
  知识点返回空结果。
- 提交前全量门禁：后端 546 个默认测试、Checkstyle、SpotBugs 和 JaCoCo 门槛通过；前端 57 个文件、
  298 个测试、ESLint（0 错误、70 条既有警告）、格式检查与学习/管理双构建通过；13 个 Testcontainers
  类共 77 个真实 MySQL 用例、12 条真实 Docker Playwright E2E（含错题按知识点筛选深链）、文档、
  脚本、Compose 配置和差异检查通过。

## Round 236：阶段测评复盘按知识点事实汇总闭环

### 完成内容

- 完成详情新增 `knowledgePointSummary`：从已固化的逐题知识点快照按知识点汇总本轮题数与正确数，
  只统计事实，不读取当前题目知识点关联，也不推断掌握度、趋势或个性化结论。
- 复盘弹窗在已完成状态展示“按知识点统计”面板：每题知识点归属与“答对 n / m 题”计数，已审查
  知识点提供进入 AI 教学的入口；逐题错题复习深链保持不变。
- 课程总览最近摘要与历史摘要继续只表达最近完成事实，不因新增统计改变既有结论边界。

### Red / Green 与验证

- Red：按知识点汇总不存在时，聚焦用例先失败；Green 后服务端按快照聚合、完成详情返回、前端统计
  面板与入口、类型检查均通过。
- 真实 MySQL 聚焦验证：限定范围测评完成后，详情按知识点返回本轮题数与正确数，且逐题归属与目标
  范围仍随快照固化。
- 提交前全量门禁：后端 546 个默认测试、Checkstyle、SpotBugs 和 JaCoCo 门槛通过；前端 57 个文件、
  297 个测试、ESLint（0 错误、70 条既有警告）、格式检查与学习/管理双构建通过；13 个 Testcontainers
  类共 76 个真实 MySQL 用例、12 条真实 Docker Playwright E2E、文档、脚本、Compose 配置和差异检查通过。

## Round 235：阶段测评复盘的知识点标注闭环

### 完成内容

- 阶段测评创建时为每道快照题固化知识点归属与名称列表（V91 迁移新增 `knowledge_points_json`），
  与原题后续知识点关联变更无关，不重写历史。
- 逐题复盘按知识点展示归属；知识点属于当前课程已审查 Tutor 内容时，前端提供进入 AI 教学的入口
  （与课程总览共用同一批已审查知识点事实），未审查目录节点只作普通标签展示；错题提供进入错题复习
  的深链（复用 `courseId + questionId` 深链契约）。
- 标注与入口只呈现题目归属与作答正误事实，不从单次或多次测评结果推断掌握度、趋势或个性化结论。
- API 与数据库权威文档同步逐题知识点快照契约与 V91 迁移说明。

### Red / Green 与验证

- Red：逐题知识点快照与复盘入口不存在时，聚焦用例先失败；Green 后服务端快照固化、详情解析、
  前端标签与入口、类型检查均通过。
- 真实 MySQL 聚焦验证 V91 迁移、创建时知识点快照、提交后详情仍保留固化归属，且限定范围测评中
  每题只返回关联知识点。
- 提交前全量门禁：后端 545 个默认测试、Checkstyle、SpotBugs 和 JaCoCo 门槛通过；前端 57 个文件、
  297 个测试、ESLint（0 错误、70 条既有警告）、格式检查与学习/管理双构建通过；13 个 Testcontainers
  类共 76 个真实 MySQL 用例、12 条真实 Docker Playwright E2E、文档、脚本、Compose 配置和差异检查通过。

## Round 234：知识点限定阶段测评闭环

### 完成内容

- 阶段测评创建请求新增可空 `knowledgePointId`：不传时保持整门课程选题，传入时服务端独立校验知识点
  属于当前已加入课程且内容审查状态为 `REVIEWED`，只从该知识点关联的当前用户可见已发布客观题中选题；
  题量不足时返回实际题数，没有候选时明确失败，不扩大到其他课程或自动调用模型。
- 目标知识点 ID 与名称随测评会话创建固化（V89 迁移新增 `target_knowledge_point_id` 与
  `target_knowledge_point_name_snapshot`），进行中会话、完成详情、课程最近摘要和分页历史均返回该范围，
  知识点后续改名不重写历史。
- 学习端新增“开始阶段测评”弹窗：默认课程整体，可下拉选择课程总览中已审查 Tutor 知识点（与
  统一“开始学习”共用同一批已审查知识点事实）；测评弹窗、最近摘要和历史列表展示固化范围。
- V90 将 2026 真题中两道线性表客观题（顺序表表头插删移动、单链表结点遍历）追加关联到已审查的
  `cs408-sequential-list-insert-delete` 与 `cs408-singly-linked-list` 原子知识点，使真实环境中的
  已审查知识点可发起限定范围测评；关联幂等且只追加，不改变原顶级知识点关联。
- API 与数据库权威文档同步知识点范围契约和 V89/V90 迁移说明。

### Red / Green 与验证

- Red：知识点越权、未审查、无候选及目标快照不存在时聚焦用例先失败；Green 后服务端课程归属与审查
  校验、候选 SQL 知识点过滤、快照固化、前端范围选择与展示、类型检查均通过。
- 真实 MySQL 聚焦验证 V89 迁移、知识点限定选题只返回关联题、跨课程/未审查知识点拒绝、无候选明确
  失败，以及提交后详情与历史仍保留固化范围；V90 关联在完整迁移链上可执行。
- 提交前全量门禁：后端 544 个默认测试、Checkstyle、SpotBugs 和 JaCoCo 门槛通过；前端 57 个文件、
  296 个测试、ESLint（0 错误、70 条既有警告）、格式检查与学习/管理双构建通过；13 个 Testcontainers
  类共 76 个真实 MySQL 用例、12 条真实 Docker Playwright E2E（含课程整体与知识点限定两轮测评）、
  文档、脚本、Compose 配置和差异检查通过。

## Round 233：阶段测评题源构成快照闭环

### 完成内容

- 阶段测评创建时将题源固化为官方原题、平台人工题、用户私有题或经审查 AI 生成题；官方类别以已发布、
  来源核验的官方试卷引用判断，AI 与私有来源优先保持自身内容域。
- 测评结果、课程总览最近摘要和分页历史均返回四类题源数量；历史分页批量读取来源快照，正式题后续改源
  或试卷关联变化不会重写既有测评构成。
- 学习端在当前测评、最近测评和历史记录展示非零来源数量，同时明确这些数量只解释出处，不计算掌握度、
  难度、质量或推荐效果。

### Red / Green 与验证

- Red：来源类别快照、构成 DTO 与前端展示不存在时，后端和前端聚焦用例按预期失败；Green 后服务、
  课程总览、批量历史聚合、页面和类型检查通过。
- 真实 MySQL 聚焦验证 V88 迁移、AI 生成题来源快照及原正式题改源后的历史构成不漂移。
- 提交前全量门禁：后端 542 个默认测试、Checkstyle、SpotBugs 和 JaCoCo 门槛通过；前端 57 个文件、
  295 个测试、ESLint（0 错误、70 条既有警告）、格式检查与学习/管理双构建通过；13 个 Testcontainers
  类共 75 个真实 MySQL 用例、12 条真实 Docker Playwright E2E、文档、脚本、Compose 配置和差异检查通过。

## Round 232：AI 生成题审查与阶段测评接入闭环

### 完成内容

- 为结构化 AI 变式题增加待审、通过、拒绝状态和审查记录；独立管理端提供母题与生成题对照、状态筛选、
  批准发布和带原因驳回，已完成审查不能重复改写。
- 批准项在同一事务内发布为当前母题课程的正式客观题，从首次版本快照起保留 `AI_GENERATED`、变式引用
  和母题 ID；母题失效或非公开时拒绝发布，待审与驳回项不进入正式题库。
- 阶段测评继续使用既有可解释选题策略且不自动调用模型；合格生成题可作为当前课程候选，题目来源与母题
  链固化到测评快照，并在学习者答题和历史复盘中明确展示。

### Red / Green 与验证

- Red：审查 DTO、迁移、服务、管理接口和前端队列不存在时聚焦测试按预期失败；Green 后单元、控制器、
  API、页面与类型检查通过。
- 真实 MySQL 聚焦验证结构化变式题批准、正式题与首版版本来源、课程知识点继承及阶段测评候选链路。
- 提交前全量门禁：后端 540 个默认测试、Checkstyle、SpotBugs 和 JaCoCo 门槛通过；前端 57 个文件、
  295 个测试、ESLint（0 错误、70 条既有警告）、格式检查与学习/管理双构建通过；13 个 Testcontainers
  类共 75 个真实 MySQL 用例、12 条真实 Docker Playwright E2E、文档、脚本、Compose 配置和差异检查通过。

## Round 231：课程阶段测评历史与总览摘要闭环

### 完成内容

- 新增本人已完成课程阶段测评的倒序分页历史和所有者详情接口；摘要只返回题数、正确数、策略与时间，
  详情读取创建时固化的逐题答案和解析，进行中记录及其他用户记录不可通过历史接口读取。
- 课程总览聚合最近一次已完成测评事实，不计算掌握度或趋势；前端提供最近结果卡片、历史分页、空/错误
  状态和逐题复盘入口，并在窄屏将记录卡片改为纵向操作。

### Red / Green 与验证

- Red：历史 DTO、分页/详情方法、总览摘要字段及前端 API/交互不存在时，后端与前端聚焦测试按预期失败；
  Green 后相关服务、控制器、API、页面和类型检查通过。
- 真实 MySQL 聚焦验证分页历史、本人详情、课程总览最近摘要与既有创建/提交事实；Docker Playwright
  聚焦流程贯通测评提交、历史列表和逐题复盘，并继续完成 2026 真题学习与考试。
- 提交前全量门禁：后端 536 个默认测试、Checkstyle、SpotBugs 和 JaCoCo 门槛通过；前端 55 个文件、
  293 个测试、ESLint（0 错误、70 条既有警告）、格式检查与学习/管理双构建通过；12 个 Testcontainers
  类共 74 个真实 MySQL 用例、12 条真实 Docker Playwright E2E、文档、脚本、Compose 配置和差异检查通过。

## Round 230：课程阶段测评最小闭环

### 完成内容

- 已加入课程的学习者可从课程总览创建或恢复一轮阶段测评；同一用户与课程只保留一个进行中会话，完成
  后显式释放活动键以允许下一轮。
- 服务端仅从当前用户可见的已发布客观题选题，依次考虑未掌握错题、到期复习、近期错误课程事件和确定性
  题序；响应公开实际选题策略，数据不足时不冒充 AI 个性化。
- 创建时固化题干、选项、正确答案和解析，提交前隐藏答案；完整提交由服务端按快照判分，并把逐题结果
  写入错题、复习计划和 `STAGE_ASSESSMENT` 来源课程事件。课程总览弹窗提供答题、结果和逐题解析。

### Red / Green 与验证

- Red：服务、控制器、前端 API 与页面行为尚不存在时，新增聚焦测试按预期失败；Green 后相关后端与
  前端测试通过。
- 真实 MySQL 验证创建/恢复、答案隐藏、服务端判分、课程事实回写和完成后开启新一轮；聚焦 Docker
  Playwright 流程贯通课程加入、阶段测评提交和既有 2026 真题学习、考试及来源复盘。
- 提交前全量门禁：后端 532 个默认测试、Checkstyle、SpotBugs 和 JaCoCo 门槛通过；前端 55 个文件、
  291 个测试、ESLint（0 错误、70 条既有警告）、格式检查与学习/管理双构建通过；12 个 Testcontainers
  类共 74 个真实 MySQL 用例、12 条真实 Docker Playwright E2E、文档、脚本、Compose 配置和差异检查通过。

## Round 229：私有原文件存储清单闭环

### 完成内容

- 新增仅所有者可见的原文件分页清单，按创建时间倒序返回名称、PDF/DOCX 格式、字节数、创建时间以及
  当前关联的待复核草稿或已确认私有试卷；查询不选择解析正文、哈希或 BLOB，也不返回文件 URL。
- 考试中心从累计用量进入清单，提供加载、空状态、窄屏卡片、关联对象下载和删除入口。删除继续调用既有
  草稿/试卷生命周期，已有学习、考试或衍生事实的试卷仍由服务端拒绝，清单不直接删除被引用来源。
- UI 继续复用 Element Plus、既有语义变量和确认弹窗；信息层级优先展示文件名、容量/时间和业务归属，
  危险操作与下载分离，在 390px 窄屏保持可点击和无横向溢出。

### Red / Green 与验证

- Red：清单 DTO、所有者查询、服务分页方法、控制器契约、前端 API 与交互尚不存在时，后端和前端聚焦
  用例按预期失败。Green 后相关服务、控制器、API 和页面测试通过。
- 真实 MySQL 集成验证并发配额及清单所有者隔离、元数据映射和草稿关联；现有 PDF Docker Playwright
  流程扩展为从用量进入清单、窄屏检查并通过既有试卷生命周期删除原文件。
- 提交前全量门禁：后端 528 个默认测试、Checkstyle、SpotBugs 和 JaCoCo 门槛通过；前端 55 个文件、
  289 个测试、ESLint（0 错误、70 条既有警告）、格式检查与学习/管理双构建通过；12 条真实 Docker
  Playwright E2E、文档、脚本、Compose 配置和差异检查通过。

## Round 228：私有试卷原文件累计配额闭环

### 完成内容

- 新增可配置的所有者累计原文件配额，`PRIVATE_EXAM_SOURCE_STORAGE_LIMIT_BYTES` 本地默认 100MB；单文件
  10MB 上限保持不变。PDF/DOCX 确认或创建草稿时必须在同一事务内锁定所有者用户行，再汇总现存来源
  字节并判断是否可写入，超过配额时整笔业务事务回滚。
- 新增所有者用量接口，实时返回已用、上限、剩余字节和文件数且不读取 BLOB；考试中心导入对话框展示
  汇总用量，删除无人引用来源后再次读取即可看到容量释放。
- 存储校验被收敛到独立服务并使用强制既有事务传播，避免未来调用方在锁释放后才插入来源记录。

### Red / Green 与验证

- Red：累计配额服务、Mapper 聚合/行锁与用量 DTO 尚不存在时，聚焦测试按预期失败。Green 后服务、导入、
  草稿、控制器以及前端 API/组件聚焦测试通过。
- 真实 MySQL 并发集成测试以 12 字节配额同时提交两个 8 字节文件，确认只有一个事务成功；删除来源后用量
  回到零。
- 提交前全量门禁：后端 526 个默认测试、Checkstyle、SpotBugs 和 JaCoCo 门槛通过；前端 55 个文件、
  287 个测试、ESLint（0 错误、70 条既有警告）、格式检查与学习/管理双构建通过；12 条真实 Docker
  Playwright E2E、文档、脚本、Compose 配置和差异检查通过。

## Round 227：私有试卷原文件持久化与所有者下载闭环

### 完成内容

- V85 为 `user_exam_source` 增加媒体类型、字节数和 `MEDIUMBLOB` 原文件列；普通 MyBatis 查询默认排除
  二进制列，仅所有者下载路径显式读取，避免列表和来源正文接口无意加载最大 10MB 文件。
- PDF/DOCX 确认或创建待复核草稿时保存原始字节；已确认试卷和草稿分别提供所有者附件下载接口，发送前
  复核私有归属、规范媒体类型、实际长度和 SHA-256，并返回 `private, no-store` 与 `nosniff`，不生成公共 URL。
- 考试中心只在来源确有原文件时展示下载操作；真实 DOCX 浏览器流程校验下载文件名和全部原始字节。
  既有草稿/试卷删除事务删除无人引用来源时同步清理二进制文件。
- POI 5.5.1 带入的 Commons Compress 1.28.0 要求 Commons Lang 3.18.0；显式锁定兼容版本，修复真实
  MySQL Testcontainers 容器复制阶段的 `ArrayFill` 运行时缺类。

### Red / Green 与验证

- Red：原文件持久化重载、所有者文件服务与 Mapper 显式二进制查询尚不存在时，聚焦测试按预期失败。
  Green 后后端服务和控制器测试覆盖 PDF/DOCX 字节转发、跨所有者隐藏、缺失/损坏文件、下载媒体类型与
  私有缓存响应头；前端 API 和组件聚焦测试覆盖 Blob 请求与试卷/草稿下载操作。
- 真实 MySQL 聚焦测试通过 V1–V85 连续迁移和含 BLOB 来源的草稿/试卷删除生命周期；首次运行暴露并修复
  Commons Lang 依赖收敛问题。
- 提交前全量门禁：后端 521 个默认测试、Checkstyle、SpotBugs 和 JaCoCo 门槛通过；前端 55 个文件、
  285 个测试、ESLint、格式检查与学习/管理双构建通过；12 条真实 Docker Playwright E2E、文档、脚本、
  Compose 与差异检查通过。

## Round 226：有限 DOCX 私有试卷导入闭环

### 完成内容

- 考试中心新增最大 10MB 的 DOCX 上传；服务端按文档正文顺序提取普通段落与表格单元格，支持中英文
  结构化字段，空内容、损坏文件、图片、公式、文本框、嵌套表格、页眉页脚和复杂对象均明确拒绝。
- DOCX 预览不写库，确认和草稿创建要求重新上传同一文件并比对原始字节 SHA-256；既有所有者隔离、AI
  逐题复核、启用事务、学习/考试和来源回看链路保持不变，来源记录只保存文件名、格式、哈希和提取文本。
- Apache POI 从 EasyExcel 间接带入且已停止支持的 4.1.2 收敛为直接声明的 5.5.1，并排除冲突旧 schema；
  DOCX 真实测试样本按文档 Skill 生成、渲染和逐页检查，段落与表格无裁切或重叠。

### Red / Green 与验证

- Red：DOCX DTO、提取服务和入口尚不存在时聚焦测试按预期编译失败。Green 后后端测试覆盖段落/表格、
  中英文字段、空内容、复杂对象、损坏/超限、确认和草稿哈希连续性；控制器 multipart 测试通过。
- 前端组件 8 个测试、类型和静态检查通过；单条真实 Docker Playwright 上传—预览—确认—来源追溯—删除
  流程通过。
- 提交前全量门禁：后端 516 个默认测试、Checkstyle、SpotBugs 和 JaCoCo 门槛通过；前端 55 个文件、
  283 个测试、ESLint、格式检查与学习/管理双构建通过；12 条真实 Docker Playwright E2E、文档、脚本、
  Compose 与差异检查通过。

## Round 225：文本型 PDF 私有试卷导入闭环

### 完成内容

- 考试中心新增文本型 PDF 上传；服务端限制 10MB、200 页和 100000 个提取字符，只使用 PDF 现有文本层，
  空文本/扫描件、加密、损坏和超限文件均明确拒绝，不引入 OCR 或复杂版式猜测。
- PDF 预览不写库；确认和草稿创建要求重新上传同一文件，以原始字节 SHA-256 保证预览连续性，并复用既有
  结构化解析、所有者隔离、AI 逐题复核和启用事务。来源记录保存文件名、PDF 格式、原文件哈希和提取文本，
  不保存原始二进制文件。
- 前端沿用同一导入对话框和预览确认流程，multipart 请求显式使用正确媒体类型；真实测试 PDF 同时覆盖
  文本提取、答案预览、确认启用、来源回看和受保护删除。

### Red / Green 与验证

- Red：PDF 服务、multipart 控制器和前端 API 尚不存在时聚焦测试按预期失败。Green 后覆盖文本提取、
  原文件哈希、扫描件/空文本、加密、损坏、超限、确认与草稿转发的后端测试通过。
- 前端组件、类型、静态检查和单条真实 Docker Playwright 上传流程通过；浏览器流程曾发现全局 JSON 请求头
  覆盖 FormData，修正为 multipart 后上传—预览—确认—来源追溯闭环通过。
- 提交前全量门禁：后端 507 个默认测试、Checkstyle、SpotBugs 和 JaCoCo 门槛通过；前端 55 个文件、
  282 个测试、ESLint、格式检查与学习/管理双构建通过；11 条真实 Docker Playwright E2E、文档、脚本、
  Compose 与差异检查通过。

## Round 224：私有试卷删除生命周期闭环

### 完成内容

- 所有者可在考试中心显式删除未确认草稿；确认启用后的草稿只能随其私有试卷统一清理，跨账号删除按
  不存在处理。
- 私有试卷删除会锁定所有者试卷并检查考试、学习、错题、收藏、复习及其他题目衍生引用；仅零引用时
  事务物理清理试卷、拆解题目、选项、知识点关系、关联确认草稿和无人引用的原始资料。
- 考试中心为私有试卷和草稿提供独立确认操作；已有考试事实时展示服务端拒绝原因，删除成功后即时刷新。

### Red / Green 与验证

- Red：生命周期服务骨架下，所有者隔离、确认草稿限制、引用保护和物理清理测试按预期失败。Green 后
  6 个服务测试和控制器聚焦测试通过。
- 真实 MySQL 3 个聚焦测试通过，覆盖草稿与源资料清理、已确认草稿随零引用试卷清理，以及考试记录阻止
  删除；前端组件 6 个测试、类型和静态检查通过。
- 真实 Docker 聚焦浏览器流程通过，覆盖零引用私有试卷与草稿删除、已有考试记录拒删及跨账号不可见。
- 提交前全量门禁：后端 499 个默认测试、Checkstyle、SpotBugs 和 JaCoCo 门槛通过；前端 55 个文件、
  281 个测试、ESLint、格式检查与学习/管理双构建通过；10 条真实 Docker Playwright E2E、文档、脚本与
  Compose 检查通过。

## Round 223：无答案私有试卷 AI 建议与人工复核闭环

### 完成内容

- V84 新增所有者私有的导入草稿与逐题复核表，将原资料答案、AI 建议和用户最终答案分层保存；未确认草稿
  不创建 `exam_paper` 或正式题目。
- 无答案客观题可逐题调用 AI 生成严格 JSON 建议；服务端约束标签必须属于现有选项、单选/判断只能一个、
  多选至少两个，并继续执行用户配额和 AI 调用审计，校验失败不会写入建议。
- 所有者必须逐题提交最终答案和解析，全部复核后草稿才进入 `READY`；确认事务按所有者锁行、只使用最终
  复核结果创建私有试卷，并为网络重试返回同一已启用试卷。
- 考试中心支持保存、关闭后恢复、AI 建议、逐题复核和确认启用；E2E Compose 使用确定性 OpenAI 兼容
  上游替代外部密钥，真实后端配额、HTTP Provider、结构校验、审计与事务均继续运行。

### Red / Green 与验证

- Red：既有预览因缺失答案返回“答案必须匹配现有选项”；草稿服务骨架下所有者权限、AI 结构校验、逐题
  复核和确认测试按预期失败。Green 后 11 个私有导入/草稿服务测试及控制器聚焦测试通过。
- 真实 MySQL V1–V84 连续迁移、前端组件与类型/静态检查、真实 Docker 草稿恢复—AI 建议—人工复核—
  启用—考试判分聚焦浏览器流程通过。
- 提交前全量门禁：后端 492 个默认测试、Checkstyle、SpotBugs 和 JaCoCo 门槛通过；前端 55 个文件、
  280 个测试、ESLint、格式检查与学习/管理双构建通过；9 条真实 Docker Playwright E2E、文档、脚本与
  Compose 检查通过。

## Round 222：用户私有试卷结构化导入闭环

### 完成内容

- V83 为试卷与题目增加所有者、公共/私有可见性，并新增仅所有者读取的原始资料表；存量内容保持公共。
- 用户端新增结构化 Markdown/文本的无写库预览、内容哈希确认和原始资料回看，首期限制为 100000 字符、
  100 题以及答案完整的单选、多选和判断题；确认事务一次创建私有试卷、题目、选项和来源关系。
- 考试中心新增导入、预览确认、私有标识和原文查看；确认后的私有试卷可用于考试与课程试卷学习。
- 试卷/题目读取、考试开始、课程总览、公共题库、练习候选、搜索、相似题、AI 题目/资产、管理列表和
  管理统计统一执行可见性边界，其他账号与管理员默认无法读取用户私有正文。

### 验证

- 私有导入、所有者权限、考试/学习回归、AI 资产、练习和控制器聚焦测试通过；真实 MySQL V1–V83 迁移
  聚焦验证与前端组件、类型、ESLint 检查通过。
- 提交前全量门禁：后端 483 个默认测试、Checkstyle、SpotBugs 和 JaCoCo 门槛通过；前端 55 个文件、
  279 个测试、ESLint、格式检查与学习/管理双构建通过；8 条真实 Docker Playwright E2E、文档、脚本与
  Compose 检查通过。

## Round 221：独立管理端切换正式路径

### 完成内容

- 管理构建 base 与产物从过渡 `/admin-app/`、`dist/admin-app/` 切换为正式 `/admin/`、`dist/admin/`，Nginx
  为管理 Router 提供独立 history fallback。
- 学习端删除全部管理路由和九项重复导航，仅为管理员保留“进入管理系统”外部入口；管理端保留返回学习端。
- `/admin` 使用相对 308 重定向到 `/admin/`，避免 Docker 非 80 映射端口被 Nginx 自动目录跳转丢失。

### 验证

- 聚焦 2 个路由测试文件 13 个用例、类型检查和管理端生产构建通过；真实 Docker 中 `/admin` 响应相对
  `Location: /admin/`，正式路径全页面与登录—主观题批阅 E2E 通过。
- 提交前全量门禁：后端 478 个默认测试、Checkstyle、SpotBugs 和 JaCoCo 门槛通过；前端 55 个文件、
  278 个测试、ESLint、格式检查与学习/管理双构建通过；7 条真实 Docker Playwright E2E、文档、脚本与
  Compose 检查通过。

## Round 220：完成独立管理端页面迁移覆盖

### 完成内容

- 独立管理端新增用户、投稿和 AI 调用分析路由与导航，至此全部现有管理页面均可由独立入口访问。
- 投稿入库后的正式题目跳转改用稳定路由名，新旧管理 Router 均复用同一页面和业务逻辑。

### Red / Green 与验证

- Red：三条目标路径在独立 Router 中回退总览；补齐后 6 个权限与路由测试、类型检查通过。
- 真实 Docker/Nginx 聚焦 E2E 逐页加载用户、投稿和 AI 运营真实接口，无 5xx。
- 提交前全量门禁：后端 478 个默认测试及质量插件通过；前端 55 个文件、281 个测试、静态检查与双构建
  通过；7 条真实 Docker Playwright E2E、文档、脚本与 Compose 检查通过。

## Round 219：迁移课程知识点与题目管理

### 完成内容

- 独立管理端新增课程、知识点和题目管理路由与导航，复用既有页面、API 和治理抽屉；课程到知识点及返回
  课程的跳转改用稳定路由名，使新旧两个管理入口都能消费同一页面。
- 真实页面回归从独立课程列表进入指定课程知识点树，再由导航打开题目管理，不复制内容 CRUD 逻辑。

### Red / Green 与验证

- Red：新增三条独立路由契约后，旧配置把 `/courses` 回退到总览；补齐路由后 5 个权限与路由测试通过，
  TypeScript 检查和独立管理端生产构建通过。
- 提交前全量门禁：后端 478 个默认测试和质量插件通过；前端 55 个文件、280 个测试、静态检查和双构建
  通过；文档、脚本与 Compose 检查通过。7 条浏览器流程中，既有错题测试因随机抽到正确答案为 B 的题目
  首轮失败；将样本限定为 Java 基础单选题后聚焦复验通过，其余 6 条（含本轮独立管理页面）均通过。

## Round 218：迁移管理总览与试卷管理

### 完成内容

- 独立管理端新增平台总览和试卷管理路由与导航，直接复用已有成熟页面、API 契约和设计变量；默认入口改为
  平台总览，主观题批阅继续保持独立入口。
- 旧 `/admin` 总览和 `/admin/exams` 在迁移期继续可用；本轮未复制统计、组卷、发布或来源核验逻辑。

### Red / Green 与验证

- Red：路由契约要求独立管理端根路径和 `/exams` 分别进入总览、试卷管理，旧配置仍重定向主观题批阅；
  增加路由后 4 个守卫与路由测试通过，类型检查和独立管理端生产构建通过。
- 真实 Docker/Nginx 聚焦 E2E 通过管理员令牌进入独立总览，点击导航加载试卷管理真实接口，页面无 5xx。
- 提交前全量门禁：后端 478 个默认测试与全部质量插件通过；前端 55 个文件、279 个测试、ESLint、
  Prettier 和双生产构建通过；7 条真实 Docker Playwright E2E、文档、脚本和 Compose 检查通过。

## Round 217：建立独立管理端首个可运行切片

### 完成内容

- 新增独立管理端 HTML 入口、Vite 构建、Router、权限守卫、登录页、响应式布局和导航，在过渡路径
  `/admin-app/` 迁入主观题批阅；原 `/admin/**` 继续承载尚未迁移的管理页面。
- 学习端与管理端仍在同一前端依赖包中，但分别产出 `dist/` 与 `dist/admin-app/`，由同一 Docker/Nginx
  镜像按路径提供，不新增后端、数据库、微前端或重复依赖体系。
- 共享请求层移除对学习端 Router 的静态依赖，改由两个应用分别注册登录失效跳转；管理端路由和登录均
  校验管理员角色，学习者令牌不能进入管理页面，后端管理接口继续作为真实安全边界。

### Red / Green 与验证

- Red：无令牌访问独立批阅路由时仍直接进入页面；增加守卫后转入管理登录。第二个 Red 确认普通学习者
  令牌留在管理登录页但未被清除；修正后显式清理令牌。聚焦 2 个文件、4 个前端测试通过。
- 独立管理端生产构建通过；真实 Docker/Nginx 下从 `/admin-app/login` 登录并完成 2026 两题批阅的
  Playwright 闭环通过，未拦截业务 API。
- 提交前全量门禁：后端 478 个默认测试、Checkstyle、SpotBugs 和 JaCoCo 门槛通过；前端 55 个文件、
  278 个测试、ESLint、Prettier 以及学习端/管理端双生产构建通过；7 条真实 Docker Playwright E2E、
  文档、仓库脚本和 Compose 配置全部通过。

## Round 216：接入 2026 数据结构综合题人工批阅闭环

### 完成内容

- V81 建立主观题评分点与人工批阅事实：考试提交后主观题不再按关键词自动判分，而是进入待批阅状态；
  管理员逐评分点给分后由服务端重算成绩，全部主观题完成时才固化最终总分。
- V82 迁入已核验 2026 来源中的第 41、42 题、分步参考答案和 7 个评分点，另建 13 题、45 分的数据结构
  分卷；既有 11 题、22 分客观题卷保持不变，避免改写历史考试引用。
- 用户端区分暂定分、待批阅和已批阅结果，待批阅阶段不泄露主观题参考答案；试卷学习模式改为作答后
  自评，不再把关键词命中当作可信得分。管理端新增桌面表格、移动卡片和评分抽屉。
- 发布门禁要求每道主观题存在评分点且评分点总分严格等于试卷题目分值。

### 调整与验证

- Red 覆盖主观题考试不得自动判分、学习模式自评、发布评分点门禁和人工批阅事务；聚焦后端单元测试、
  Checkstyle、前端 5 个测试文件 27 个用例、ESLint 与类型检查均通过。
- 真实 MySQL 测试确认 V1–V82 连续迁移，并贯通 13 题试卷提交、两题逐项批阅和 23/45 最终成绩固化。
- 隔离 Docker Playwright 新增真实登录、45 分试卷提交、管理员两题批阅与学习者 27/45 结果复核闭环。
- 提交前全量门禁：后端 478 个默认测试通过，Checkstyle 0 违规、SpotBugs 0 问题、JaCoCo 门槛通过；
  前端 54 个文件、275 个测试通过，ESLint、Prettier 检查和生产构建通过；文档、脚本与 Compose 检查通过。
  7 条浏览器流程首次运行时，既有错题测试因新主观题进入通用练习池而随机抽到简答题；测试显式筛选
  单选题后聚焦复验通过，其余 6 条（含新增批阅闭环）在同轮均通过。

## Round 215：真实浏览器验收 2026 真题学习与考试

### 完成内容

- 新增隔离 Docker Playwright 闭环：学习者通过真实登录加入 408 数据结构课程，在考试中心核对 2026 官方
  试卷名称、来源、11 题和 22 分结构，完成全部逐题学习并确认首答前 AI 辅导门禁。
- 同一测试再完成 11 题限时考试，全部选择 A 后由服务端判得 4/22 分；结果页展示来源已核验、全国统考
  名称、来源 URL、11 条逐题结果、数据结构分区和原始题号。
- E2E 使用独立 Compose 项目和独立卷，未覆盖已有停止容器或普通开发数据库；验收后已清理隔离环境。

### 调整与验证

- 初次执行发现测试将 UI 标签“官方原题”写成“官方真题”；后续又修正未等待课程详情导致跳过加入、
  学习路由误写为 `/exams/learning/{id}` 以及考试名称遗漏“综合”三处测试契约问题。后端在未加入课程时
  始终正确拒绝学习会话，没有为通过浏览器测试弱化权限。
- 聚焦真实 Docker Playwright：新增 2026 真题闭环 1 条通过，运行 8.8 秒；使用真实 MySQL、Flyway
  V1–V80、JWT、前后端接口和服务端判分，未拦截或模拟业务 API。
- 提交前全量门禁：后端 472 个默认测试通过，Checkstyle 0 违规、SpotBugs 0 问题、JaCoCo 门槛通过；
  前端 53 个文件、272 个测试通过，ESLint、Prettier 检查和生产构建通过；6 条真实 Docker
  Playwright E2E 全部通过；文档、仓库脚本、Docker Compose 配置与工作区空白检查通过。

## Round 214：让 Tutor 消费跨入口课程学习证据

### 完成内容

- Tutor 会话启动时按目标知识点及其同课程祖先目录关联题目，聚合真题学习作答与错答、成功的试卷 AI
  辅导、当前未掌握错题、到期复习和历史复习作答；没有相关题目时返回空上下文。
- V80 为 `tutor_session` 增加 `learning_context_json`，把本次会话实际消费的计数和最近时间固化为快照。
  快照不复制原始答案、正确答案或 AI 输出，也不替代原始课程事件、错题和复习计划。
- Tutor 页面展示“跨入口学习证据”卡片，并明确这些记录不代表掌握度；全零上下文不制造进度展示。

### Red / Green

- Red：新增聚合服务测试后，空实现返回的真题作答数为 0，未满足预期 3 次，形成有效失败。
- Green：聚合与 Tutor 会话测试 6 个通过，页面测试 3 个通过。真实 MySQL 首轮又发现迁入根知识点以
  `parent_id=0` 表示根节点，聚合器会误丢弃已收集范围；修正根终点后，第二轮继续发现 MySQL `JSON`
  会规范化空格，字符串匹配无法可靠识别错答，最终改用 `JSON_EXTRACT` 按布尔值查询并转绿。

### 验证

- 真实 MySQL Testcontainers：迁移测试 5 个通过，确认 V1–V80 连续迁移和 Tutor 快照字段；跨入口集成
  测试 1 个通过，以真实 2026 真题完成“错答—试卷 AI 辅导—错题状态—相关 Tutor—上下文快照”闭环。
- 提交前全量门禁：后端 472 个默认测试通过，Checkstyle 0 违规、SpotBugs 0 问题、JaCoCo 门槛通过；
  前端 53 个文件、272 个测试通过，ESLint、Prettier 检查和生产构建通过；文档、仓库脚本、Docker
  Compose 配置与工作区空白检查通过。

## Round 213：绑定试卷学习 AI 辅导上下文

### 完成内容

- 新增试卷学习专用流式 AI 辅导接口。解析和变式调用会重新校验当前用户、课程、试卷、会话与题目关系，
  并绑定该题最近一次答案、尝试序号和判分结果；未首次作答的题目不能请求辅导。
- V79 新增 `exam_learning_ai_interaction`，保存用户、课程、试卷、会话、题目、答案与调用状态，不复制
  完整提示词或 AI 输出。成功调用以交互 ID 幂等写入统一课程学习事件，失败调用保留简短失败状态。
- 试卷学习页使用专用上下文接口，并移除会绕开会话边界的通用学习资产入口；其他练习与错题页面继续
  使用通用题目助手。

### Red / Green

- Red：原通用助手只携带题目 ID，既不要求首次作答，也不记录试卷、会话、课程和答案上下文；V78 数据库
  中也不存在可追溯的试卷学习 AI 交互表。
- Green：服务、控制器、前端组件和页面测试覆盖首次作答门禁、本人会话边界、专用流式路由、最近一次
  答案上下文、成功课程事件与失败状态；真实 MySQL 测试从 2026 真题错答贯通 AI 调用、交互记录和课程事件。

### 验证

- 后端聚焦单元测试：12 个通过；前端相关 API、组件和试卷学习页测试：19 个通过。
- 真实 MySQL Testcontainers：5 个聚焦测试通过，确认 V1–V79 连续迁移，并以真实 2026 试卷完成
  “开始学习—错答—上下文 AI 辅导—交互记录—课程学习事件”闭环。
- 提交前全量门禁：后端 470 个默认测试通过，Checkstyle 0 违规、SpotBugs 0 问题、JaCoCo 门槛通过；
  前端 53 个文件、270 个测试通过，ESLint、Prettier 检查和生产构建通过；文档、仓库脚本、Docker
  Compose 配置与工作区空白检查通过。

## Round 212：迁入 2026 年 408 数据结构客观题试卷

### 完成内容

- 用户提供的离线页面经标题、正文和 canonical 来源核对后确认属于 2025 年，不能改标为 2026 年；随后
  沿页面年份链接核对真实 2026 页面并以其作为迁移来源。
- V78 迁入 2026 年 408 数据结构选择题第 1–11 题、44 个选项、正确答案、解析和课程知识点映射，形成
  22 分的已发布官方试卷；试卷保留全国统考名称、年份、来源 URL、数据结构分区和原始题号。
- 当前课程只有 408 数据结构，因此不把组成原理、操作系统和计算机网络题错误归入课程。第 41、42 题
  虽已识别，但现有简答题任一关键词命中规则无法可信判定多问综合题，本轮不将其接入自动判分试卷。

### Red / Green

- Red：V1–V77 的真实 MySQL 迁移链中不存在目标 2026 试卷，新增迁移验收测试按预期收到空结果。
- Green：V78 执行后，聚焦测试确认试卷来源、发布状态、11 题、22 分、44 个选项、11 个唯一正确选项、
  原始题号、知识点映射以及答案键 `A,D,C,B,D,D,B,D,B,A,C`。

### 验证

- 真实 MySQL Testcontainers：迁移与考试服务 14 个聚焦测试通过，确认 V1–V78 连续迁移、目标试卷
  数据约束及既有考试状态机回归。
- 后端 `mvn clean verify -B`：466 个默认测试通过，Checkstyle 0 违规、SpotBugs 0 问题、JaCoCo 门槛通过。
- `python3 scripts/check-docs.py` 与仓库脚本单元测试通过；本轮文件 `git diff --check` 通过。全工作区检查
  仍会报告用户已有 `.gitignore` 换行产生的尾随空白，本轮未覆盖该文件。

## Round 211：打通课程试卷限时考试与来源复盘

### 完成内容

- V77 为限时考试增加可释放的活动记录键；迁移时固化已超时记录、收敛同一用户和试卷的重复活动记录，
  并以数据库唯一约束保护并发首次开始。开始接口现在真正复用未过期记录，完成或确认超时后允许新一轮。
- 新增本人限时考试会话接口，返回带偏移的服务端时钟和截止时间且不返回答案；客户端刷新后恢复同一
  记录、试卷和剩余时限，不再依赖 `sessionStorage`。未提交的本地答案不宣称可恢复，到时明确进入超时
  状态，不使用截止后必然被服务端拒绝的请求冒充自动交卷。
- 只有已完成记录可以读取考试结果。结果通过服务端映射不可变试卷配置，展示来源核验信息、原试卷分区
  和完整原始题号，并提供课程总览和按课程、题目定位的错题复习入口。
- 考试记录页区分进行中、已完成和已超时，移动端使用独立记录卡；作答和结果页面沿用现有语义变量，
  补齐键盘按钮语义、焦点状态、触控尺寸与移动端布局。
- 真实 Docker E2E 覆盖服务端开始、倒计时下降、刷新恢复同一作答 URL、三类题作答、提交和权威结果。
  本轮没有导入或冒充 408 官方真题，也没有完成试卷学习 AI 上下文绑定。

### Red / Green

- Red：旧实现重复开始会创建新记录，会话路由不存在，进行中记录可读取结果；前端依赖本地缓存，刷新
  会重置倒计时，结果也可被本地数据替代。
- Green：服务、控制器和页面聚焦测试覆盖活动记录复用、并发唯一键竞争、超时释放、本人会话、结果隔离、
  服务端绝对截止时间、三状态路由、来源与原题号展示。
- 真实 MySQL Green 前发现将活动键设为 `null` 时 MyBatis 默认更新策略不会持久化，随后以显式空值更新
  策略修正；审查又发现缺失键 `FOR UPDATE` 的 gap lock 可能造成并发首次开始死锁，最终改为先非锁定定位、
  再按主键锁定并重检，唯一键冲突后使用当前读恢复胜出记录。

### 验证

- 后端使用 Microsoft OpenJDK 21 执行 `mvn clean verify -B`：466 个默认测试通过；Checkstyle、SpotBugs
  和 JaCoCo 门禁通过。
- 真实 MySQL Testcontainers 聚焦验证：13 个测试通过，确认 V1–V77 连续迁移、活动键唯一约束、恢复、
  提交与释放状态机。
- 前端全量 Vitest：52 个文件、267 个测试通过；相关 ESLint、生产构建和 5 条真实 Docker Playwright
  E2E 通过。
- 文档检查、仓库脚本单元测试、Docker Compose 配置检查和非用户 `.gitignore` 范围的空白检查通过。

## Round 210：统一后端 Java 21 基线

### 完成内容

- 将 Maven 编译目标从 Java 17 升级到 Java 21，并移除仅为本机 JDK 26 测试兼容而固定的 ByteBuddy
  覆盖，恢复使用 Spring Boot 管理的测试依赖版本。
- 将 GitHub Actions 的常规后端验证与 MySQL Testcontainers 集成验证统一到 Temurin 21。
- Docker 原有 Java 21 构建和运行镜像保持不变；README、后端架构和本地开发指南同步声明 Java 21 基线。
- 历史审计和旧开发日志中的 JDK 17 记录保持原样，继续反映当时的真实环境。

### Red / Green

- Red 例外：本轮是纯构建兼容配置调整，不改变业务逻辑，无法构造有意义的行为失败用例；以 Java 21
  环境下的完整 Maven 质量门禁作为替代验证。

### 验证

- 使用 Microsoft OpenJDK 21.0.9 执行后端 `mvn clean verify -B`：457 个默认测试通过；编译器使用
  `release 21`，Checkstyle 0 违规、SpotBugs 0 问题，JaCoCo 行与分支覆盖率门槛通过。
- `python3 scripts/check-docs.py`、仓库脚本单元测试与本轮文件的 `git diff --check`：通过。

## Round 209：打通课程试卷逐题学习模式

### 完成内容

- V76 新增独立于限时考试的试卷学习会话与逐题尝试表；同一用户和试卷最多存在一个进行中会话，完成后
  可开始新一轮，历史尝试继续保留用于复盘。
- 已加入课程的用户可从课程总览或试卷列表进入学习模式，逐题多次作答并立即获得服务端判分与解析；
  每道题至少尝试一次后才能完成本轮，完成态页面转为只读逐题复盘。
- 学习会话保留可信来源、原始分区和题号结构；未作答题目不返回正确选项、答案或解析，其他用户不能访问
  会话，客户端不能指定正确性或分数。
- 每次逐题判分继续写入统一课程学习事件、错题和间隔复习计划，但不创建或复用 `exam_record`，限时考试
  的时限、整卷提交和提前隐藏答案语义保持不变。
- 课程内容与课件扩张在本模块后保持暂停；下一阶段转入限时考试结果的来源/原号复盘和连续课程入口，
  不制造 408 官方题目数据。

### Red / Green

- Red：V76 之前的真实 MySQL 迁移链缺少两张试卷学习表；服务测试中的课程库边界、会话创建、越权访问、
  题目归属、逐题判分、完成门禁和状态转换因学习服务尚不存在而按预期失败。
- Green：服务端与控制器聚焦测试确认上述边界；前端 API、课程入口、试卷列表和学习页面测试确认创建、
  恢复、逐题提交、完成与原号展示链路。

### 验证

- 后端 `mvn clean verify -B`：457 个默认测试通过；Checkstyle、SpotBugs 和 JaCoCo 门禁通过。
- 前端 `npm run coverage`：51 个测试文件、254 个测试通过，全源码语句覆盖率 24.38%；Lint 0 错误、
  70 条存量警告，生产构建通过。
- 真实 MySQL Testcontainers 聚焦测试确认 V1–V76 连续迁移和两张学习表的关键字段；文档检查与脚本
  单元测试通过。

## Round 208：建立官方试卷来源与原始题号契约

### 完成内容

- V75 为试卷增加普通练习与官方原题性质，以及考试名称、年份、可核验来源和人工核验
  状态；为试卷题目增加分区、大题、小题、子题与完整展示题号，不写入任何试卷或题目数据。
- 官方试卷通过创建、更新或独立发布进入已发布状态前，服务端统一校验来源、年份、人工核验和每道题的
  展示题号；普通练习保持兼容，智能组卷不会自动获得官方标记。
- 管理端可以录入和复查上述字段；用户考试中心只把来源已核验的试卷标为“官方原题”，并在考试过程
  展示原试卷分区和题号。
- 用户私有试卷因尚无所有者隔离和可见性规则而保持不可写，避免私有资料被公共考试列表读取。
- 再次确认 LearnPlatform 与 AiStu 当前均没有可直接导入的 408 结构化历年真题，本轮没有用自拟题或
  AI 生成题填充官方入口。课程内容和课件扩张继续暂停，开发转入正式题目学习业务闭环。

### Red / Green

- Red：V75 之前的真实 MySQL 迁移链缺少十个来源与题号字段；官方试卷在来源未核验或题号缺失时仍可
  发布，新增迁移和服务测试均按预期失败。
- Green：迁移测试确认 V1–V75 连续执行，服务测试确认两类缺失被拒绝并允许完整来源的官方试卷发布；
  前端测试确认可信来源标识和原始展示题号。

### 验证

- 后端 `mvn clean verify -B`：448 个默认测试通过，Checkstyle、SpotBugs 与 JaCoCo 门禁通过；真实 MySQL
  Testcontainers 聚焦测试确认 V1–V75 连续迁移和十个新增字段。
- 前端 `npm run coverage`：50 个测试文件、251 个测试通过，全源码语句覆盖率 23.62%；Lint 0 错误、
  70 条存量警告，生产构建通过。
- 文档检查覆盖 58 个 Markdown 文件，脚本单元测试 9 个通过，非用户 `.gitignore` 范围的空白检查通过。

## Round 207：打通课程复习与错题深链

### 完成内容

- `GET /api/review/due` 增加可选 `questionId`，并修复原有 `courseId` 只声明未生效的问题；课程和题目
  条件现在会在到期排序与数量限制前共同生效，客户端从课程总览进入后自动打开服务端选择的到期题。
- `GET /api/wrong-questions` 增加可选 `questionId`，并把课程筛选从分页后的内存过滤前移到数据库分页条件；
  目标课程的记录和总数保持一致，课程总览可精确进入推荐错题。
- 无课程目标的全局复习与错题入口保持兼容；所有筛选继续绑定认证用户，不扩大读取范围。

### Red / Green

- Red：复习服务的课程筛选用例收到两个课程的题目；真实 MySQL 错题用例按课程筛选后总数仍为 2 而非 1；
  两个旧页面也不会消费课程总览传入的课程和目标题目。
- Green：服务、控制器、前端 API 和页面测试确认课程/题目条件贯穿请求；真实 MySQL 用例确认错题分页总数
  与目标题目都按课程正确收窄。

### 验证

- 后端 `mvn clean verify -B`：443 个默认测试通过，Checkstyle、SpotBugs 与 JaCoCo 门禁通过；真实 MySQL
  聚焦用例确认课程筛选和精确题目筛选后的分页数据。
- 前端 `npm run coverage`：49 个测试文件、250 个测试通过；Lint 0 错误、70 条存量警告，生产构建通过。
- 文档检查覆盖 58 个 Markdown 文件，脚本单元测试 9 个通过。


## Round 206：建立统一“开始学习”目标选择

### 完成内容

- 新增 `POST /api/my-courses/{courseId}/start-learning`，客户端不再需要为课程级“开始学习”预选
  `knowledgePointId` 或 `questionId`；服务端在请求时复用课程总览的统一目标排序并返回首个目标。
- 课程总览增加独立主动作，并按 `TUTOR`、`DUE_REVIEW`、`WRONG_QUESTION`、`COURSE_SEQUENCE`
  将服务端目标路由至 Tutor、复习、错题或题库入口；已有候选列表仍可用于显式选择。
- 目标选择只读取已保存课程事实，不生成学习事件或掌握证据；课程库归属校验继续由服务端执行。

### Red / Green

- Red：新增接口契约测试时，旧代码没有 `start-learning` 路由并返回 500；新增课程总览组件测试时，
  页面不存在由服务端选择目标的主按钮。
- Green：控制器与服务测试确认认证用户可在不提供知识点时取得统一首目标；前端 API 与组件测试确认
  请求不携带客户端目标，并按 Tutor、到期复习和错题类型进入对应入口。

### 验证

- 后端 `mvn clean verify -B`：440 个默认测试通过，Checkstyle、SpotBugs 与 JaCoCo 门禁通过。
- 前端 `npm run coverage`：47 个测试文件、248 个测试通过；Lint 0 错误、71 条存量警告，生产构建通过。
- 文档检查覆盖 58 个 Markdown 文件，脚本单元测试 9 个通过。


## Round 205：完成队列基础 Web Tutor 模块并暂停内容扩张

### 完成内容

- 迁入 FIFO 语义、循环队列数组表示、循环队列判空判满与长度、链队列四项独立审查内容。
- 循环队列两项复用既有 `ARRAY_QUEUE_REPRESENTATION` v1 受限课件，不建立重复渲染器；内容补足
  `rear` 约定、牺牲槽位公式和删除最后链结点后的尾指针重置。
- 按当前开发优先级，完成本模块后暂停后续 408 内容与课件扩张，转入统一“开始学习”业务能力。

### Red / Green

- Red：真实 MySQL 迁移测试先要求 48 项内容，旧链只返回 44 项。
- Green：V71–V74 加入后，真实 MySQL 测试确认 V1–V74、48 项内容、课件类型、正确选项和路径可用。


## Round 204：完成栈应用 Web Tutor 模块

### 完成内容

- 迁入括号匹配、表达式转换与求值、递归与调用栈三项独立审查内容。
- 新增 `FACTORIAL_CALL_STACK` v1 受限课件，只回放 2–6 的固定阶乘活动记录；不执行函数体、表达式或脚本。
- V68–V70 保留扫描结束判空、二元操作数弹出顺序、基例和逐层返回边界，理解检查仍由服务端判分。

### Red / Green

- Red：真实 MySQL 迁移测试先要求 44 项内容，旧链只返回 41 项；调用栈组件不存在时新增 Vitest 无法解析导入。
- Green：真实 MySQL 测试确认 V1–V70、44 项内容、正确选项和路径；组件测试确认调用栈先增长到基例再逐层返回。

### 验证

- 前端 `npm run coverage`：47 个测试文件、244 个测试通过；Lint 0 错误、71 条存量警告，生产构建通过。
- 后端默认测试、静态检查、覆盖率门禁和真实 MySQL 迁移验证通过；文档检查与脚本测试通过。


## Round 203：完成栈基础 Web Tutor 模块

### 完成内容

- 迁入栈的 LIFO 语义、顺序栈、链栈和合法出栈序列四项独立审查内容。
- V64–V67 明确 `top` 约定不可混用、链栈表头操作和辅助栈模拟边界；本模块不引入可执行课件或客户端判分。

### Red / Green

- Red：真实 MySQL 迁移测试先要求 41 项内容，旧链只返回 37 项。
- Green：V64–V67 加入后，真实 MySQL 测试确认 V1–V67、41 项内容、正确选项和学习路径可用。


## Round 202：完成线性表进阶 Web Tutor 模块

### 完成内容

- 迁入双链表、循环链表、单链表逆置、有序链表归并和顺序表/链表取舍五项独立审查内容。
- 新增 `LINKED_LIST_REVERSAL` v1 受限课件，只回放固定元素上的保存后继、反向链接和指针推进；不接收脚本、动态组件或用户运行态。
- V59–V63 补齐双向成对不变量、循环终止、稳定归并和复杂度前提；理解检查和可导航路径仍由服务端按照课程归属与审查状态处理。

### Red / Green

- Red：逆置课件组件尚不存在时，新增 Vitest 用例按预期无法解析导入。
- Green：组件测试确认保存后继与已逆置前缀的展示；真实 MySQL 测试确认 V1–V63、37 项内容、课件类型、正确选项和学习路径可用。

### 验证

- 聚焦前端组件与 Tutor 页面测试通过；完整前后端验证结果见本轮状态基线。


## Round 201：完成线性表核心 Web Tutor 模块

### 完成内容

- 迁入数组式线性表布局权衡，以及 List ADT、顺序表连续存储、顺序表插删、单链表表示和结点插删。
- 新增 `SEQUENTIAL_LIST_STORAGE` v1 受限课件，固定回放首地址、元素宽度和直接寻址；不解释或执行内容内表达式。
- V54–V58 将线性表核心内容连成受限学习路径；理解检查和可导航目标继续只由服务端依据课程归属和审查状态处理。

### Red / Green

- Red：连续存储课件组件尚不存在时，新增 Vitest 测试按预期无法解析导入；数组布局内容加入前，真实迁移测试按预期只有 26 项内容。
- Green：组件测试确认地址公式和审查输入不变；真实 MySQL 测试确认 V1–V58、32 项内容和理解检查可用。

### 验证

- 前端 `npm run coverage`：45 个测试文件、242 个测试通过；构建通过。
- 后端 `mvn clean verify -B`：438 个测试、Checkstyle、SpotBugs 和 JaCoCo 门禁通过。


## Round 200：完成 RootishArrayStack Web Tutor 学习模块

### 完成内容

- 依据 AiStu ODS 原始资料迁入递增块布局、下标映射、按位更新、块增缩、浪费空间界和性能六项内容；
  保持块容量递增、跨块逻辑下标、块级增缩与 `O(sqrt(n))` 空间界的原始范围。
- 新增 `ROOTISH_ARRAY_STACK_LAYOUT` v1 受限课件。配置要求第 b 个块长度恰为 b+1，固定 Vue 渲染器只
  回放块布局、逻辑顺序和容量和，不执行内容内公式或脚本。
- V47–V52 将六项内容连为课程内学习路径；理解检查和可导航目标继续由服务端按审查状态与课程归属处理。

### Red / Green

- Red：布局组件尚不存在时，新增 Vitest 测试按预期无法解析导入。
- Green：组件测试确认递增块、逻辑顺序和审查输入不变；真实 MySQL 迁移测试确认完整 V1–V52 迁移链与路径。

### 验证

- 前端定向测试：2 个文件、2 个测试通过；`npm run build` 通过。
- 后端 `CourseLibraryMigrationIntegrationTest`：1 个真实 MySQL Testcontainers 测试通过。


## Round 199：完成 DualArrayDeque Web Tutor 学习模块

### 完成内容

- 依据 AiStu 可追溯 ODS 原始资料独立审查并迁入 DualArrayDeque 的五项内容：双栈表示、按位更新、
  再平衡、再平衡摊还成本和操作复杂度；内容保持 `front` 逆序、`back` 正序与三倍失衡的原始边界。
- 新增 `DUAL_ARRAY_DEQUE_REPRESENTATION` 与 `DUAL_ARRAY_DEQUE_BALANCE` v1 受限课件。固定 Vue 渲染器
  分别回放下标映射和一次失衡重建，不接收脚本、动态组件或用户运行态，也不写入学习证据。
- V42–V46 将五项已审查内容连为课程内学习路径；正确选项仍只在服务端保存，路径目标继续经过课程归属
  与 `REVIEWED` 状态校验。

### Red / Green

- Red：再平衡课件组件尚不存在时，新增 Vitest 测试按预期无法解析导入。
- Green：固定重建器保持逻辑序列与已审查输入不变；真实 MySQL 迁移测试确认 V1–V46、二十项 Tutor
  内容、课件类型、理解检查和学习路径均可用。

### 验证

- 前端定向测试：3 个文件、3 个测试通过；`npm run build` 通过。
- 后端 `CourseLibraryMigrationIntegrationTest`：1 个真实 MySQL Testcontainers 测试通过。


## Round 198：完成 ArrayDeque Web Tutor 学习模块

### 完成内容

- 依据 AiStu 的可追溯 ODS 原始资料和本轮独立审查，迁入
  `ods-arraydeque-representation`：ArrayDeque 以逻辑起点 `j`、元素数量 `n` 和循环数组表示 List，
  `get(i)`/`set(i, x)` 通过 `a[(j+i) mod capacity]` 直接访问。
- 新增 `ARRAY_DEQUE_REPRESENTATION` v1 受限互动课件。固定 Vue 渲染器只回放回绕、逻辑序列与指定
  `get(i)` 的物理槽位，不执行脚本、不保存用户运行态，也不将播放过程记为学习证据。
- 迁入 `ods-arraydeque-nearest-end-shifting`：插入点靠近逻辑前端时，`j` 向左回绕并仅搬移前缀；
  `ARRAY_DEQUE_FRONT_SHIFT_INSERT` v1 课件以固定状态回放该分支，不将其泛化为尾端分支、删除或通用脚本。
- 迁入 `ods-arraydeque-performance`：`get/set` 为 `O(1)`，忽略 resize 的更新为
  `O(1 + min(i, n-i))`，从空结构开始的 m 次更新中全部 resize 总成本为 `O(m)`；V40/V41 将三项内容
  收敛为课程内学习路径。正确选项继续只保存在服务端，首次判分复用既有幂等课程学习事件。

### Red / Green

- Red：新增 ArrayDeque 访问和前端搬移课件组件测试时模块尚不存在，Vitest 按预期无法解析导入；迁移
  测试先要求第十五个 Tutor 内容，V41 加入前实际只有十四个。
- Green：V39/V40 写入两类受限课件，V41 写入复杂度内容并完成路径；固定渲染器测试覆盖跨界 `get(3)`、
  前缀搬移、`j` 回绕和输入不变性，真实 MySQL 迁移测试确认完整迁移链可用。

### 验证

- 后端 `CourseLibraryMigrationIntegrationTest`：1 个真实 MySQL Testcontainers 测试通过，覆盖 V1 至 V41
  迁移链、十五个已审查 Tutor 内容、受限课件类型、正确选项、学习路径与用户课程唯一约束。
- 前端聚焦测试：3 个文件、3 个测试通过；`npm run build` 通过。


## Round 197：完成 ArrayQueue Web Tutor 学习模块

### 完成内容

- 迁入并独立审查 `ods-arrayqueue-performance`：忽略 `resize` 时入队与出队均为 `O(1)`；从空队列
  开始的 `m` 次更新中，全部 `resize` 总成本为 `O(m)`，故更新为 `O(1)` 摊还时间。内容明确一次
  实际 `resize` 仍可能复制 `n` 个元素，不能把摊还结论误写为每次最坏 `O(1)`。
- 以 Flyway V38 为 ArrayQueue 的表示、入队、出队、线性化复制和操作复杂度五个已审查内容建立课程内
  学习路径。服务端仅解析当前课程内且仍为 `REVIEWED` 的引导目标，避免客户端凭 `contentKey` 跳转到
  跨课程或未审查内容。
- Tutor 理解检查结果新增已解析的引导知识点 ID；学习者可直接复习前置内容或学习下一内容，同一路由
  切换知识点时会重载会话，形成从课程目录、教学、服务端判分到后续学习的 Web 闭环。
- 收紧 Git 交付规则：后续以完整模块、阶段或业务闭环为默认提交边界，不再按单条 Tutor 内容、迁移、
  组件、测试通过或对话轮次自动提交；未达到边界的关联改动保留在工作区继续开发。

### Red / Green

- Red：课程详情测试先要求第十二个 Tutor 入口；服务测试先要求返回可用的引导知识点 ID，旧实现返回
  `null`；Tutor 页面测试先要求“学习下一内容”操作，旧页面仅显示文本提示，三处均按预期失败。
- Green：Flyway V37 写入已审查内容及 `QUEUE_AMORTIZED` 正确选项，V38 补全五个内容的学习路径；
  后端安全解析引导目标，前端提供可执行跳转并在路由参数变化时重载会话。
- 真实 MySQL 迁移测试首次执行时发现测试 SQL 的 `IN` 占位符少于传入内容键数量；修正测试后，V1 至
  V38 的完整迁移链和十二个 Tutor 内容断言通过。

### 验证

- 后端 `mvn clean verify -B`：438 个默认测试通过，Checkstyle、SpotBugs 与 JaCoCo 门槛通过；
  `CourseLibraryMigrationIntegrationTest`：1 个真实 MySQL Testcontainers 测试通过。
- 前端 `npm run coverage`：39 个测试文件、236 个测试通过；`npm run lint` 无错误（71 条存量显式
  `any` 警告），`npm run format:check` 与 `npm run build` 通过。
- `python3 scripts/check-docs.py`、仓库 Python 测试（9 个）与 `git diff --check` 通过。

## Round 196：收敛后续提交粒度

### 完成内容

- 将提交粒度明确为可独立验收的业务、管理或工程能力，避免按单个文件、函数或原子知识名词
  机械拆分提交。
- 规定共享用户入口、审核范围和验证闭环的紧密内容切片应合并；具有独立产品语义、审核结论、
  迁移风险或回滚价值的切片继续单独提交。
- 不改写既有 Git 历史；本规则仅约束后续工作。

### 验证

- `python3 scripts/check-docs.py` 验证文档链接、结构与职责约束。
- `git diff --check` 验证文档无空白错误。

## Round 195：迁入 ArrayQueue 线性化复制 Tutor 切片

### 完成内容

- 依据 AiStu 的可追溯 ODS 原始记录与本轮独立审查，迁入 `ods-arrayqueue-resize`：跨界的 FIFO
  队列调整容量时，从旧队首开始以 `b[k] = a[(j+k) mod oldCapacity]` 复制到新数组，再令 `j = 0`。
- 新增 `ARRAY_QUEUE_RESIZE` v1 受限互动课件。固定 Vue 渲染器只回放旧数组回绕、逻辑顺序复制和
  队首归零，不执行脚本、不保存用户运行态，也不把播放过程写为学习证据。
- 内容和理解检查明确排除扩缩容触发条件、特定容量策略的合理性与摊还复杂度；正确选项继续不返回
  客户端，首次判分复用已有幂等课程学习事件。

### Red / Green

- Red：新增线性化复制 Schema、固定课件、课程目录入口和迁移断言后，前端无法解析缺失组件、Schema
  函数不存在，目录只显示十个入口。
- Green：Flyway V36 写入已审查内容、`ARRAY_QUEUE_RESIZE` v1 配置和 `COPY_LOGICAL_ORDER` 正确选项；
  固定渲染器测试覆盖跨界 FIFO 顺序、新数组连续布局、`j = 0` 与输入不变性。

### 验证

- 后端 `mvn clean verify -B`：438 个默认测试通过，Checkstyle、SpotBugs 与 JaCoCo 门槛通过；
  `CourseLibraryMigrationIntegrationTest`：1 个真实 MySQL Testcontainers 测试通过。
- 前端 `npm run coverage`：38 个测试文件、235 个测试通过，覆盖率门槛通过；Schema、线性化复制课件和
  课程详情聚焦测试：3 个文件、22 个测试通过；`npm run lint` 无错误（71 条存量显式 `any` 警告）、
  `npm run format:check` 与 `npm run build` 通过。
- `python3 scripts/check-docs.py`、仓库 Python 测试与 `git diff --check` 通过。

## Round 194：迁入 ArrayQueue 出队 Tutor 切片

### 完成内容

- 依据 AiStu 的可追溯 ODS 原始记录与内部抽查，独立审查并迁入
  `ods-arrayqueue-dequeue`：非空队列出队时保存 `a[j]`，再令 `j = (j+1) mod capacity`、`n` 减一；
  其余 FIFO 元素无需搬移。
- 新增 `ARRAY_QUEUE_DEQUEUE` v1 受限互动课件。固定 Vue 渲染器只回放队首读取、循环前移和计数
  更新，不执行脚本、不保存用户运行态，也不把播放过程写为学习证据。
- 内容和理解检查明确排除空队列、缩容与复杂度结论；正确选项继续不返回客户端，首次判分复用已有
  幂等课程学习事件。

### Red / Green

- Red：新增出队 Schema、课件组件和课程详情入口的聚焦前端测试在相应实现不存在时失败；迁移集成
  测试先要求第十个已审查内容，旧迁移链仅有九个。
- Green：Flyway V35 写入已审查内容、`ARRAY_QUEUE_DEQUEUE` v1 配置和 `ADVANCE_HEAD` 正确选项；
  固定渲染器测试覆盖队首读取、回绕前移、计数减少、FIFO 顺序及输入不变性。

### 验证

- 后端 `mvn clean verify -B`：438 个默认测试通过，Checkstyle、SpotBugs 与 JaCoCo 门槛通过；
  `CourseLibraryMigrationIntegrationTest`：1 个真实 MySQL Testcontainers 测试通过。
- 前端 `npm run coverage`：37 个测试文件、233 个测试通过，覆盖率门槛通过；Schema、出队课件和
  课程详情聚焦测试：3 个文件、21 个测试通过；`npm run lint` 无错误（71 条存量显式 `any` 警告）、
  `npm run format:check` 与 `npm run build` 通过。
- `python3 scripts/check-docs.py`、仓库 Python 测试（9 个）与 `git diff --check` 通过。

## Round 193：迁入 ArrayQueue 入队 Tutor 切片

### 完成内容

- 依据 AiStu 的可追溯 ODS 原始记录与内部抽查，独立审查并迁入
  `ods-arrayqueue-enqueue`：在容量足够时，将新元素写入 `a[(j+n) mod capacity]`，随后令 `n`
  加一；已有 FIFO 元素不需要搬移。
- 新增 `ARRAY_QUEUE_ENQUEUE` v1 受限互动课件。固定 Vue 渲染器只回放循环队尾的定位、写入和
  计数更新，不执行脚本、不保存用户运行态，也不把播放过程写为学习证据。
- 内容和理解检查明确排除满队 `resize`、出队和复杂度结论；正确选项继续不返回客户端，首次判分
  复用已有幂等课程学习事件。

### Red / Green

- Red：新增 Schema、课件组件和课程详情入口的聚焦前端测试在相应实现不存在时分别失败；迁移集成
  测试先要求第九个已审查内容、课件类型和服务端正确答案。
- Green：Flyway V34 写入已审查内容、`ARRAY_QUEUE_ENQUEUE` v1 配置和 `TAIL_SLOT` 正确选项；
  固定渲染器测试覆盖回绕队尾、写入后才增加 `n`、FIFO 顺序及输入不变性。

### 验证

- 后端 `mvn clean verify -B`：438 个默认测试通过，Checkstyle、SpotBugs 与 JaCoCo 门槛通过；
  `CourseLibraryMigrationIntegrationTest`：1 个真实 MySQL Testcontainers 测试通过。
- 前端 `npm run coverage`：36 个测试文件、231 个测试通过，覆盖率门槛通过；Schema、入队课件和
  课程详情聚焦测试：3 个文件、20 个测试通过；`npm run lint` 无错误（71 条存量显式 `any` 警告）、
  `npm run build` 通过。
- `python3 scripts/check-docs.py`、仓库 Python 测试（9 个）与 `git diff --check` 通过。

## Round 192：迁入 ArrayQueue 循环数组表示 Tutor 切片

### 完成内容

- 依据 AiStu 的可追溯 ODS 原始记录与内部抽查，独立审查并迁入
  `ods-arrayqueue-representation`：ArrayQueue 用队首物理下标 `j` 和元素数量 `n` 表示 FIFO
  队列，第 `k` 个逻辑元素位于 `a[(j+k) mod capacity]`。
- 新增 `ARRAY_QUEUE_REPRESENTATION` v1 受限互动课件。固定 Vue 渲染器只按容量、队首和逻辑
  元素序列回放回绕映射与 FIFO 顺序，不执行脚本、不保存用户运行态，也不把播放过程写为学习证据。
- 内容和服务端理解检查明确不将物理下标回绕误写成数组旋转，且不延伸到入队、出队、扩缩容或复杂度。
  正确选项继续不返回客户端，首次判分复用已有幂等课程学习事件。

### Red / Green

- Red：课件组件测试在固定渲染器模块不存在时无法解析，按预期失败。
- Green：V33 写入第八个已审查内容、受限配置和 `INDEX_ONE` 正确选项；组件测试覆盖从 `j = 6`
  跨数组末端的回绕、FIFO 顺序和输入不变性，课程详情测试覆盖已加入课程库后的真实深链接。

### 验证

- 后端 `mvn clean verify -B`：438 个默认测试通过，Checkstyle、SpotBugs 与 JaCoCo 门槛通过；
  `CourseLibraryMigrationIntegrationTest`：1 个真实 MySQL Testcontainers 测试通过。
- 前端 `npm run coverage`：35 个测试文件、229 个测试通过，覆盖率门槛通过；循环数组课件与课程详情
  聚焦测试：2 个文件、5 个测试通过；`npm run lint` 无错误（71 条存量显式 `any` 警告）、
  `npm run format:check` 与 `npm run build` 通过。
- `python3 scripts/check-docs.py`、仓库 Python 测试（9 个）与 `git diff --check` 通过。

## Round 191：迁入 FastArrayStack 批量复制优化 Tutor 切片

### 完成内容

- 依据 AiStu 的可追溯 ODS 原始记录与内部抽查，独立审查并迁入
  `ods-fastarraystack-block-copy`：批量复制或移动函数可替代连续区间的显式循环并降低常数开销，
  但处理元素数量不变，因此不改变插入、删除与 `resize` 的渐近时间复杂度。
- 内容明确不把原始资料中的特定运行时加速示例当作平台性能保证；实际收益取决于语言、元素类型、
  库实现和硬件。
- 课程详情向已加入课程库的用户开放第七个 Tutor 深链接；理解检查继续在服务端首次判分并写入课程
  学习事件，不引入客户端判分、桌面端迁移或新课件运行时。

### Red / Green

- Red：迁移聚焦测试先要求七个已审查 Tutor 知识及批量复制检查答案，旧数据库仅有六个内容；
  课程详情测试先要求第七个入口，旧页面只有六个，均按预期失败。
- Green：Flyway V32 写入已审查内容和 `CONSTANT_FACTOR_ONLY` 正确选项；课程详情向已加入课程库的
  用户提供对应 Tutor 深链接。

### 验证

- 后端 `mvn clean verify -B`：438 个默认测试通过，Checkstyle、SpotBugs 和 JaCoCo 门槛通过。
- 后端 `CourseLibraryMigrationIntegrationTest`：1 个真实 MySQL Testcontainers 测试通过。
- 前端课程详情聚焦测试：4 个测试通过；`npm run lint` 无错误（71 条存量显式 `any` 警告）、
  `npm run format:check` 与 `npm run build` 通过。
- `python3 scripts/check-docs.py` 与 `git diff --check` 通过。

## Round 190：迁入 ArrayStack 操作复杂度 Tutor 切片

### 完成内容

- 依据 AiStu 的可追溯定理与原始记录，独立审查并迁入 `ods-arraystack-performance`：`get/set` 为最坏 `O(1)`；忽略 resize 时按位 `add/remove` 为 `O(1+n-i)`；尾端 push/pop 为 `O(1)` 摊还时间。
- 内容明确区分尾端操作没有位置搬移与单次仍可能触发 `O(n)` resize，避免将摊还复杂度错误表述为每次最坏 `O(1)`。
- 课程详情将第六个已迁入内容开放为真实 Tutor 入口；理解检查继续由服务端首次判分并写入课程学习事件，不新增客户端判分或可执行课件。

### Red / Green

- Red：迁移聚焦测试先要求六个已审查 Tutor 知识及操作复杂度检查答案，旧数据库仅有五个内容，按预期失败。
- Green：Flyway 迁移写入已审查内容和 `TAIL_AMORTIZED` 正确选项；课程详情测试确认已加入课程库时提供第六个 Tutor 深链接。

### 验证

- 后端 `CourseLibraryMigrationIntegrationTest`：1 个真实 MySQL Testcontainers 测试通过。
- 前端课程详情聚焦测试：4 个测试通过；`npm run lint` 无错误（71 条存量显式 `any` 警告）、`npm run format:check` 与 `npm run build` 通过。

## Round 189：迁入 ArrayStack 容量调整摊还成本 Tutor 切片

### 完成内容

- 依据 AiStu 的可追溯定义，独立审查并迁入 `ods-arraystack-amortized-resize`：从空结构开始的 `m` 次 `add/remove` 中，全部 `resize` 总成本为 `O(m)`。
- 内容明确保留一次实际 `resize` 仍可能复制 `n` 个有效元素、耗时 `O(n)` 的最坏情况，避免把摊还分析错误表述为每次扩缩容都是常数时间。
- 课程详情将第五个已迁入内容开放为真实 Tutor 入口；新内容继续复用服务端首次判分和课程学习事件，不新增可执行课件或客户端判分。

### Red / Green

- Red：迁移聚焦测试先要求五个已审查 Tutor 知识与摊还检查答案，旧数据库仅有四个内容；课程详情测试先要求第五个入口，旧页面仅显示四个，均按预期失败。
- Green：Flyway 迁移写入已审查内容和 `TOTAL_LINEAR` 正确选项；已加入课程库时课程详情提供对应的 Tutor 深链接。

### 验证

- 后端 `CourseLibraryMigrationIntegrationTest`：1 个真实 MySQL Testcontainers 测试通过。
- 前端课程详情聚焦测试：4 个测试通过。

## Round 188：迁入 ArrayStack 容量调整 Tutor 切片

### 完成内容

- 依据 AiStu 的可追溯概念、算法与源记录，独立审查并迁入 `ods-arraystack-resize`：`resize` 分配长度 `max(1, 2n)` 的新数组，按原顺序复制 `n` 个有效元素后切换后备数组；单次复制成本为 `O(n)`。
- 增加 `ARRAY_STACK_RESIZE` v1 受限互动课件。固定 Vue 渲染器只根据旧容量和有效元素回放分配、逐槽复制和切换，不执行脚本、不保存用户运行态，也不将浏览过程写成学习证据。
- 容量调整的理解检查继续复用服务端首次判分与课程学习事件；正确选项不返回客户端。摊还成本保留为未迁入的后续提示，不将单次 `O(n)` 错写为摊还结论。

### Red / Green

- Red：容量调整知识加入课程详情测试后，旧页面只开放 3 个 Tutor 节点；新增课件测试在渲染器模块不存在时无法解析，均按预期失败。
- Green：课程详情开放第四个经迁入内容的真实入口，固定渲染器测试覆盖新数组分配、按序复制、切换和输入不变性。

### 验证

- 前端容量调整课件与课程详情聚焦测试：2 个文件、5 个测试通过。
- 前端 `npm run lint`：0 错误、71 条存量显式 `any` 警告；`npm run format:check` 与 `npm run build` 通过。

## Round 187：迁入元素数量与数组容量 Tutor 切片

### 完成内容

- 依据 AiStu 可追溯原始记录，独立审查并迁入 `ods-array-size-capacity`：`n` 只计逻辑元素，`length(a)` 是后备数组槽位总数，且保持 `0 ≤ n ≤ length(a)`；空闲槽位不属于逻辑序列。
- 为该内容新增服务端理解检查；正确选项不返回客户端，首次判分继续只写入幂等课程学习事件。
- 课程详情现为三个已迁入的 Tutor 内容提供真实入口；插入和删除答错后的前置提示不再错误标记为“尚未开放”。

### Red / Green

- Red：`CourseLibraryMigrationIntegrationTest` 先断言三个已审查 Tutor 知识，迁移尚未加入时实际返回两个，按预期失败。
- Green：真实 MySQL Flyway 迁移测试确认三个已审查原子知识均可迁入；课程详情组件测试确认已加入课程库时容量前置知识提供真实 Tutor 跳转。

### 遗留问题

- `ods-array-backed-storage`、ArrayStack 容量调整与操作复杂度仍未迁入；课程页面不会将它们显示为可访问教学。

## Round 186：迁入 ArrayStack 按位删除 Tutor 切片

### 完成内容

- 从 AiStu 的可追溯 ODS 源记录中独立审查并迁入 `ods-arraystack-removal`：删除后保存目标值、将后缀从左向右左移、更新有效元素数量，并保留稳定内容键、来源、版本和审查状态。
- 新内容复用既有课程库、Tutor 会话、服务端首次判分和课程学习事件；没有伪造 408 历年真题、扩展桌面端或把尚未迁入的前置知识开放为页面。
- 理解检查的正误解释改由已审查 `check_json` 提供，避免将 ArrayStack 插入的“从右向左”解释错误返回给删除等其他内容。

### Red / Green

- Red：为删除内容增加反馈断言后，旧服务返回了插入专用的“从右向左”解释，聚焦测试按预期失败。
- Green：服务按当前 Tutor 内容读取正误解释后，Tutor 服务测试和真实 MySQL Flyway 迁移测试通过。

### 验证

- 后端 `TutorSessionServiceTest`：4 个测试通过。
- 后端 `CourseLibraryMigrationIntegrationTest`：1 个真实 MySQL Testcontainers 测试通过，覆盖两条已审查原子知识和删除理解检查的正确选项。

## Round 185：课程总览展示已迁入 Tutor 内容状态

### 完成内容

- 课程总览接口新增 `tutorProgress`，仅列出当前课程中已审查且已迁入的 Tutor 内容。
- 进度严格从当前用户、当前课程的 Tutor 会话与首次服务端判分导出：无会话为未开始，有会话但未正确为已尝试，任一首次正确检查为已完成。
- 课程总览前端显示可访问内容状态，并分别提供开始、继续和再次学习入口；未迁入目录节点不会被标成未完成。

### Red / Green

- Red：后端状态推导用例在 DTO 尚无 `tutorProgress` 时按预期编译失败；前端页面用例在状态面板尚不存在时按预期失败。
- Green：后端服务测试覆盖完成、已尝试和未开始的排序与语义；前端页面测试覆盖状态文案和继续学习深链接。

### 验证

- 后端 `mvn clean verify -B`：437 个测试通过，Checkstyle、SpotBugs 和 JaCoCo 门槛通过。
- 前端 `npm run coverage`：33 个测试文件、225 个测试通过，覆盖率门槛通过；`npm run lint` 为 0 错误、71 条存量显式 `any` 警告，`npm run build` 通过。
- `python3 scripts/check-docs.py`、仓库 Python 测试（9 个）与 `git diff --check` 通过。

## Round 184：收敛课程详情的课程库学习入口

### 完成内容

- 课程详情并行读取当前用户课程库状态：未加入时保留唯一主操作“加入课程库”，已加入时改为“进入课程总览”。
- 对已审查的 ArrayStack Tutor 节点，未加入用户只看到“加入课程库后可学习”的前置条件；已加入后才显示真实 Tutor 跳转。
- 加入成功后直接进入对应课程总览，避免用户回到课程库列表后再寻找下一步。

### Red / Green

- Red：新增课程详情页面测试后，旧页面仍直接显示 Tutor 入口、加入后跳转课程库列表，3 个测试按预期失败。
- Green：按课程库状态渲染入口并改为课程总览深链接后，新增的页面测试全部通过。

### 验证

- 前端课程详情页面测试、完整测试与覆盖率：32 个测试文件、224 个测试通过，覆盖率门槛通过。
- `npm run lint` 无错误（71 条存量显式 `any` 警告）、生产构建、Prettier 检查、文档检查与 `git diff --check` 通过。

## Round 183：课程总览接入未完成 Tutor 教学

### 完成内容

- 课程总览优先推荐当前课程中已审查、尚未完成理解检查的 Tutor 内容，随后才是到期复习、错题和默认目录。
- Tutor 目标使用课程内知识点标识跳转到已有 Tutor 页面；正确完成理解检查后不再被当作未完成教学推荐。
- 未新增或伪造教学内容、题目来源、课件执行能力或客户端判分逻辑。

### Red / Green

- Red：扩展 `CourseOverviewServiceTest` 后，服务尚未注入 Tutor 内容和会话查询依赖，聚焦测试按预期编译失败。
- Green：实现课程范围、`REVIEWED` 审查状态和首次正确检查的筛选后，聚焦服务测试通过。

### 验证

- 后端 `CourseOverviewServiceTest`、前端类型检查与生产构建、文档检查。

## Round 182：ArrayStack 受限互动课件 Web 展示

### 完成内容

- 在已审查 Tutor 内容中增加 `ARRAY_STACK_INSERTION` v1 的序列化参数：容量、初始元素、插入位置和插入值；不存储脚本、动态组件名或用户运行态。
- 新增 Vue 固定渲染器，按“准备、从右向左搬移、写入”推导每一步数组状态，提供前后步按钮、左右方向键、焦点可见状态、移动端横向容纳和减弱动态效果。
- Tutor 页面仅在课件配置通过容量、元素、位置和值的边界校验后渲染；互动过程不写入课程学习事件，也不影响理解检查的服务端首次判分。
- 增加 MySQL 迁移断言，确认完整 Flyway 链中 ArrayStack 内容含受限课件类型。

### Red / Green

- Red：`TutorArrayStackInsertion` 组件测试在固定渲染器尚不存在时，因模块无法解析而失败。
- Green：组件测试覆盖从右向左的状态推进、输入配置不被修改、首步不可后退与方向键推进。

### 验证

- 前端组件测试、Lint、生产构建；后端 Tutor 服务聚焦测试与课程库迁移 Testcontainers 测试。

## Round 181：ArrayStack 首个 Web Tutor 闭环

### 完成内容

- 新增版本化已审查 Tutor 内容、用户课程 Tutor 会话和首次理解检查结果；目标不在用户课程库、非本课程或未审查时拒绝开始。
- 迁入 ArrayStack 按位插入的确定性讲解步骤与检查题；服务端不向客户端返回正确选项，并以条件更新锁定首次判分。
- 首次检查判分追加 `AI_TUTOR` 课程学习事件；讲解或打开页面不被误记为掌握证据。
- 在课程目录的已审查 ArrayStack 知识点提供“开始 AI 教学”入口，并新增 API/数据库文档。

### Red / Green

- Red 例外：本轮在功能代码落地前没有保留可运行的失败测试，不能宣称严格 TDD。
- Green：`TutorSessionServiceTest` 覆盖课程库边界与正确选项不泄露；前端课程 API 契约测试覆盖 Tutor 会话和检查请求。

## Round 180：选定首个已审查教学切片

### 完成内容

- 从 AiStu 迁入已审查的 `ods-arraystack-insertion` 原子知识，作为 408“栈、队列和数组”目录下的首个 Web 教学切片；记录稳定标识、来源、版本和迁入时审查状态。
- 修复课程总览默认目录目标仍按 `parent_id IS NULL` 查询的错误，使其符合现有知识树以 `parent_id = 0` 表示顶层目录的约定。
- 明确首个 Web 闭环的跨层契约：课程归属、已审查内容、课件 Schema、服务端判分、可追加事件和真题来源保持独立边界。
- AiStu 当前没有可迁入的 408 结构化历年真题，因此没有用自拟检查题冒充官方原题。

### Red / Green

- Red：新增课程总览根目录约定断言，在旧的 `IS NULL` 查询下按预期失败。
- Green：改为 `parent_id = 0` 后通过；迁移集成测试覆盖原子知识的来源、版本和审查状态。

### 遗留问题

- V23 迁入的是课程目录和可追溯元数据，不包括 React 课件、RAG chunks、Tutor 会话或理解检查的 Web 运行时。
- 首个真题学习入口仍等待具备明确来源与结构的 408 原题。

### 验证

- 后端 `mvn clean verify -B`：432 个默认测试通过，Checkstyle 0 违规、SpotBugs 0 问题、JaCoCo 门槛通过。
- Testcontainers `CourseLibraryMigrationIntegrationTest`：1 个测试通过。
- 前端知识点与课程 API 契约测试：19 个测试通过；`npm run build` 通过。
- `python3 scripts/check-docs.py` 与 `git diff --check`：通过。

## Round 179：课程总览与下一步学习目标

### 完成内容

- 新增仅对当前用户已加入课程开放的 `GET /api/my-courses/{courseId}/overview`，从现有课程学习事件、错题与到期复习计划聚合真实学习事实。
- 总览明确区分已作答、答对、待复习和待处理错题；不根据浏览、自评或统计结果生成掌握度。
- 以到期复习、未掌握错题、课程默认目录的稳定顺序返回候选目标；前端“开始”复用已有课程题库筛选入口，不假装已存在 Tutor 教学流。
- 课程库入口改为深链接课程总览；新增加载、失败、空记录、刷新和移动端单列状态。

### Red / Green

- Red 例外：实现过程中未能在生产代码写入前保留可运行的失败测试，不能将本轮描述为严格 TDD。
- Green：`CourseOverviewServiceTest` 覆盖课程库边界、真实聚合和到期复习优先；`CourseLibraryControllerTest` 覆盖认证用户传递。
- 前端课程 API 契约测试覆盖总览查询路径。

### 验证

- 后端聚焦 Maven 测试：5 个测试通过。
- 前端 API 测试：12 个测试通过；`npm run lint` 无错误（71 条存量 `any` 警告）；`npm run format:check`、`npm run build` 通过。

### 遗留问题

- 总览尚未接入原子知识、Tutor 会话、互动课件或阶段测评；默认目录目标只提供既有学习入口。
- 本轮未进行已登录真实浏览器验收，原因是当前本地浏览器没有可复用的认证会话且不绕过验证码。

## Round 178：接入个人课程库前端

### 完成内容

- 新增“我的课程库”用户入口，使用真实 `GET /api/my-courses` 查询当前登录用户的已加入课程，并提供加载、空状态、刷新和课程内容入口。
- 课程详情新增幂等“加入课程库”操作，调用真实 `POST /api/my-courses/{courseId}` 后跳转课程库。
- 在侧边导航加入“我的课程库”深链接；明确加入课程只建立学习入口，不声称已开始学习、已掌握或已有课程进度。
- 将工作区已有的 `frontend/tsconfig.json` 路径配置修正一并纳入本轮提交；该改动由用户预先提供，未被本轮覆盖。

### Red / Green

- Red：新增个人课程库 API 契约测试后，因导出函数尚不存在而有 2 条测试按预期失败。
- Green：补齐请求封装后，`src/__tests__/api/course.test.ts` 的 11 个测试通过。
- `npm run lint`：无错误，仍有 71 条存量显式 `any` 警告。
- `npm run format:check`、`npm run build` 与 `git diff --check`：通过。

### 浏览器检查

- 直接访问 `/my-courses` 在未登录状态正确跳转到登录页；当前本地浏览器会话没有有效登录状态。
- 未尝试绕过登录验证码，因此已登录后的真实接口、桌面/移动布局仍需在可用登录会话中补充检查。

### 遗留问题

- 课程总览、进度聚合和“开始学习”候选目标选择仍未实现；当前课程库页面只展示已加入关系和已有课程内容入口。

## Round 177：建立课程学习事件底座

### 完成内容

- 新增版本化、可追加的 `course_learning_event`，以用户、课程和稳定幂等键约束来源事实重放。
- 事件仅在用户已将所属课程加入个人课程库后写入，避免将普通题库练习误作课程进度。
- 将练习、复习、考试逐题作答和结构化 AI 变式题首次判分映射为同一课程事件流；原始答案不复制进事件载荷。
- 补充课程事件的数据模型与数据流文档；课程总览聚合和“开始学习”接口仍未实现。

### 验证

- `CourseLearningEventServiceTest` 覆盖课程库边界、事件版本、幂等键和载荷语义。
- `CourseLibraryMigrationIntegrationTest` 扩展验证 V22 事件唯一约束。

### 遗留问题

- AI 资产的聚合查看次数尚未投影为课程事件；首版只将能由稳定来源记录唯一标识的 AI 变式题首次判分作为 AI 入口事实。
- 工作区已有 `frontend/tsconfig.json` 修改，本轮未读取其 diff、未修改也不纳入提交。

## Round 176：建立个人课程库与 AiStu 408 课程入口

### 完成内容

- 新增个人课程库后端接口，支持当前用户幂等添加已开放课程并查询自己的课程列表。
- 新增 `user_course` 表，以数据库唯一约束保护同一用户和课程关系不重复。
- 为课程和知识点增加可空的稳定内容键与来源字段，保留存量平台内容兼容性。
- 从 AiStu taxonomy 首批迁入 408 数据结构课程和 8 个顶层章节，不把加入课程解释为开始学习或掌握。
- 记录 AiStu 知识、Tutor 协议、课件与本地学习状态的复用边界，明确 Electron、React 和 Zustand 不直接成为 Web 服务端事实。

### Red / Green

- Red：`CourseLibraryServiceTest` 的新增、幂等、停用课程拒绝和用户列表映射按预期失败；Controller 身份传递测试已通过。
- Green：`CourseLibraryServiceTest` 与 `CourseLibraryControllerTest` 共 7 个聚焦测试通过。
- MySQL Testcontainers：`CourseLibraryMigrationIntegrationTest` 通过，确认 408 顶层目录导入及用户课程唯一约束。
- `mvn clean verify -B`：426 个默认测试通过，Checkstyle 0 违规、SpotBugs 0 问题，JaCoCo 门槛通过。
- `python3 scripts/check-docs.py`、`git diff --check`：通过。

### 遗留问题

- 本轮未迁入 122 个原子知识点、676 个检索 chunks 或互动课件。
- 课程学习事件、课程总览、“开始学习”目标选择和前端个人课程库仍待后续实现。
- 工作区已有 `frontend/tsconfig.json` 修改，本轮未读取其 diff、未修改也不纳入提交。

## Round 175：完善 Git 提交规范与自动校验

### 完成内容

- 采用 Conventional Commits 兼容格式，并结合单人主分支开发补充原子提交、多个聚焦提交、复杂提交正文、footer、revert 和检查点规则。
- 新增 `build` 与 `revert` 类型，明确 `style`、`chore` 和跨层业务提交的使用边界。
- 建立单一 kebab-case scope、中文主题、72 字符硬上限和禁止结尾标点规则。
- 要求破坏性变更同时使用 `!` 与 `BREAKING CHANGE:`，并说明迁移影响。
- 增强提交消息检查脚本，增加构建类型、scope、长度、标点和破坏性变更校验。
- 新增无第三方依赖的检查脚本单元测试，并将其接入 CI 文档任务。
- 将 Agent 工作流从“每轮一次提交”调整为默认一个、必要时多个可独立验证和回滚的聚焦提交。

### 验证

- `python3 -m unittest discover -s scripts/tests -p 'test_*.py'`：9 个测试通过。
- `python3 scripts/check-commit-messages.py <base-sha> <head-sha>`：最近 1 个非合并提交通过。
- `python3 scripts/check-docs.py`：通过。
- `git diff --check`：通过。

## Round 174：重置 AI 课程学习平台方向

### 完成内容

- 将产品定位从以刷题、错题和考试为中心，调整为以课程长期学习过程为中心的 Web 优先 AI 学习平台。
- 确认课程商店、个人课程库和课程总览的产品组织，以及 AI 教学与试卷学习两个并列入口。
- 明确官方原题、用户私有资料和经审查 Agent 通过的 AI 生成题的来源与使用边界。
- 将 Phase 22 调整为阶段性完成，新增 Phase 23“AI 课程学习平台转型”及单课程、单试卷最小验收顺序。
- 接受统一课程学习状态决策，要求两个入口共享题目作答、错题、误解、课件、复习和测评事件。
- 接受用户学习端与管理系统独立前端应用的目标，继续共用 Vue 技术栈、Spring Boot 后端、账号和业务数据。
- 明确 Web 为当前主开发端，AiStu 桌面端与可视化整体升级暂不并行推进。

### 验证

- `python3 scripts/check-docs.py`：通过。

## Round 173：移除人工上下文交接机制

### 完成内容

- 删除不再使用的 Agent 交接文档和 `context-handoff` 项目 Skill。
- 从长期协作规则、文档导航和 Agent 工具说明中移除交接流程。
- 收敛文档检查脚本中的项目 Skill 清单，继续以 `docs/project/status.md` 作为当前项目状态的唯一权威来源。

### 验证

- `python3 scripts/check-docs.py`：通过。

## Round 172：成熟认证体系

### 完成内容

- 用 Cloudflare Turnstile 前后端完整校验替换图片验证码。
- 登录支持用户名或验证邮箱，并继续保留 JWT、角色、状态和失败限流规则。
- 新注册用户必须完成邮箱验证码和一次性注册票据闭环，历史账号允许暂时没有邮箱。
- 新增忘记密码、重置链接验证和一次性密码重置流程；重置后旧 JWT 失效。
- 新增 Mailpit 本地邮件接收服务和 SMTP、Turnstile 环境变量契约。
- 登录、注册、忘记密码和重置密码页面统一为响应式认证工作台视觉。
- 数据库升级到 V20，增加邮箱身份、认证版本、邮箱验证和密码重置表。

### 安全边界

- Turnstile 前端成功状态不受信任，后端必须向 Cloudflare `siteverify` 二次校验。
- 邮箱验证码、注册票据和密码重置令牌不保存明文。
- 注册票据与重置令牌均由数据库条件更新保证一次性消费。
- 忘记密码使用中性响应，不披露邮箱是否存在。

### 验证

- 后端 `mvn clean verify -B`：419 个测试、Checkstyle、SpotBugs 和 JaCoCo 门禁通过。
- MySQL Testcontainers `AuthSchemaIntegrationTest`：V20 邮箱唯一约束与认证安全表验证通过。
- 前端 `npm run coverage`：30 个测试文件、215 个测试通过；认证四页均有关键行为测试。
- 前端全源码覆盖率：语句 13.98%、分支 10.54%、函数 11.95%、行 12.98%。
- 前端 `npm run build`：通过。
- 前端 `npm run lint`：0 错误，保留 71 个存量显式 `any` 警告。
- `python3 scripts/check-docs.py`、默认 Compose 与 E2E Compose 配置检查：通过。
- 临时浏览器检查覆盖 1440px 登录页和 375px 注册、找回密码、重置密码入口，无横向溢出或控制台错误。
## Round 183：扩展 ArrayStack Tutor 学习路径提示

## 完成内容

- 以 AiStu 已声明的知识关系为依据，在已审查的 ArrayStack 按位插入切片中增加“元素数量与数组容量”前置提示和“ArrayStack 的操作复杂度”后续提示。
- 理解检查仍由服务端首次判分：答错返回前置提示，答对返回后续目标；重复提交返回同一方向的既有结果。
- 前端在教学页中展示前置范围，并在检查结果后展示对应的下一步建议、可访问状态和明确限制。
- 不迁入、不开通上述两个知识点的独立页面或 Tutor 内容，因为它们尚未完成独立迁入与审查；路径提示不构成掌握证据。

## Red / Green

- Red：新增 Tutor 判分后路径建议测试，预期失败（`guidanceType` 为 `null`）。
- Green：补齐服务端受限路径读取与响应后，`TutorSessionServiceTest` 的 3 个测试通过。
- 前端聚焦 Vitest：课程 API 与 ArrayStack 课件共 15 个测试通过。

## 遗留问题

- 408 历年真题仍缺少可验证的年份、题号与来源资料，官方试卷入口继续保持不可用。
- 前置与后续知识尚未以独立可访问的 Web Tutor 内容迁入；下一步需先完成单独审查与内容切片确认。
