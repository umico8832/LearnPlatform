# 2026-09 开发日志

## Round 291：拆分私有试卷草稿数据与 AI 答案职责

### 完成内容

- `PrivateExamDraftService` 从 435 行、8 个直接依赖收敛为 127 行、3 个依赖，只保留草稿生命周期门面。
- `PrivateExamDraftDataService` 管理来源、草稿、题目、JSON 转换和展示模型；
  `PrivateExamDraftAnswerService` 管理配额校验、提示词、结构化响应和调用审计。
- 原始文件配额、所有者隔离、答案结构校验、逐题人工复核、幂等确认和事务入口不变。

### 验证

- 私有草稿、确认导入和 ArchUnit 共 17 个聚焦测试通过。

### 遗留问题

- 本轮沿用既有单次 AI 补全调用方式，不增加批量生成或重试队列。

## Round 290：拆分试卷展示与发布校验职责

### 完成内容

- `ExamPaperService` 从 477 行、6 个直接依赖收敛为 226 行、4 个依赖，只编排管理端写事务。
- `ExamPaperViewService` 承担公开 / 私有访问范围、分页和题目详情展示；`ExamPaperValidationService`
  统一试卷类型、发布条件、官方来源与题号、主观题评分点校验。
- 管理端与学习端 API、正确答案隐藏、官方试卷来源约束、草稿锁定和事务语义不变。

### 验证

- 试卷服务、管理端与学习端 Controller、私有导入、智能组卷创建和 ArchUnit 共 32 个测试通过。

### 遗留问题

- 私有试卷草稿复核仍在独立后续批次中治理。

## Round 289：拆分题目可视化渲染职责

### 完成内容

- `QuestionVisualInteractive.vue` 从 831 行收敛为 70 行，只保留加载、解析失败回退和空状态。
- 内容解析、可视化类型分派与 ECharts、Mermaid、递归树分别进入 `question-visual` 下的纯函数和领域组件；
  单个新组件均低于 500 行。
- 原属性、JSON / Markdown 容错、图表选项、Mermaid 安全级别和响应式展示不变。

### 验证

- 题目可视化与学习资产 3 个测试文件、7 个测试通过；相关 ESLint 与 `vue-tsc --noEmit` 通过。

### 遗留问题

- 本轮未执行真实浏览器视觉回归；最终前端结构收口时统一执行生产构建与关键流程验收。

## Round 288：提取自适应练习策略

### 完成内容

- `AdaptivePracticeService` 从 452 行收敛为 294 行，只保留练习事实读取、候选题查询和展示模型组装。
- 无状态的难度权重、题量分配和推荐摘要进入 `AdaptivePracticePolicy`，与 Mapper 和 Spring 生命周期解耦。
- 新用户权重、历史正确率调节、近期题目排除、推荐摘要字段和随机选题行为不变。

### 验证

- 新增策略边界与摘要测试，连同练习 Controller 纳入后端 57 个聚焦回归测试并通过。

### 遗留问题

- 随机候选不足时的跨难度补位继续保留既有实现，本轮未改变推荐算法。

## Round 287：拆分题目查询与写入职责

### 完成内容

- `QuestionService` 从 427 行、7 个直接依赖收敛为 170 行、3 个依赖，只保留公开查询、访问校验和兼容门面。
- `QuestionViewService` 统一组装课程、选项、知识点和重复题展示模型；`QuestionMutationService` 承担题目、
  选项、知识点关系写入，以及版本快照和已发布试卷保护。
- 管理端与用户端返回字段、私有题访问规则、缓存失效、事务入口和错误码不变。

### 验证

- 题目服务及相关管理端、用户端 Controller、AI 变式审核与 ArchUnit 纳入后端 57 个聚焦回归测试并通过。

### 遗留问题

- 本轮只调整题目核心维护边界；批量导入导出服务仍在后续结构治理范围内。

## Round 286：拆分智能组卷选择与展示职责

### 完成内容

- `AiExamGenerationService` 从 590 行、7 个直接依赖收敛为 150 行、4 个依赖，只保留预览与创建编排。
- 候选题与学习事实加载、难度 / 知识点均衡选择、预览分布与说明、草稿试卷创建分别进入
  `AiExamCandidateLoader`、`AiExamQuestionSelectionService`、`AiExamPreviewPresentationService` 与
  `AiExamPaperCreationService`。
- 题目公开与启用范围、既有随机扰动、选题权重、预览字段和 `ExamPaperService` 事务创建契约不变。

### 验证

- 智能组卷门面及四个协作者新增 10 个测试通过，相关管理端 Controller 与 ArchUnit 通过。
- 后端聚焦回归共 57 个测试通过。

### 遗留问题

- 选题继续保留既有 `Math.random()` 微扰，测试验证业务约束和分布字段，不固定随机排序。

## Round 285：拆分课程总览展示闭包

### 完成内容

- `CourseOverviewView.vue` 从 937 行收敛为 260 行，只保留路由、真实 API、学习入口和阶段测评状态机。
- 新增 `CourseOverviewContent.vue`，集中承载课程空间的加载 / 失败 / 空数据、目录、学习工具、学习事实、
  继续学习入口和响应式展示；沿用既有 Design Tokens、组件层级和移动端断点。
- 补充总览失败重试与最近阶段测评直接复盘回归测试，原路由、事件、加载状态和接口参数不变。

### 验证

- `CourseOverviewView.test.ts` 16 个测试通过。
- 前端 ESLint 0 错误、学习端与管理端生产构建通过。

### 遗留问题

- 本轮未执行真实浏览器截图回归；构建继续存在第三方 VueUse pure annotation 与既有大 chunk 警告。

## Round 284：拆分通用 AI 门面职责

### 完成内容

- `AiService` 从 502 行、8 个直接依赖、19 个公开方法的综合实现收敛为 102 行、3 个依赖的兼容门面。
- 题目解析 / 变式题 / 试卷学习辅导、复习建议、知识点总结和带配额审计的 Provider 调用分别进入
  `AiQuestionAssistanceService`、`AiReviewSuggestionService`、`AiKnowledgeSummaryService` 与
  `AiInvocationService`；既有 Controller 和试卷学习调用契约保持不变。
- 配额检查、调用耗时、成功 / 失败审计、Prompt 指纹输入、题目可见性和流式回调语义保持原样。

### 验证

- AI、复习与试卷学习相关 13 个聚焦测试通过。
- `mvn clean verify -B`：后端全量测试、ArchUnit、生产 / 测试双 Checkstyle、SpotBugs 和 JaCoCo
  门禁全部通过。

### 遗留问题

- 错题复习建议继续逐条读取关联题目，这是既有查询语义；本轮只拆分职责，不在缺少性能样本时改写
  查询或提示词内容。

## Round 283：拆分考试与私有试卷核心编排职责

### 完成内容

- `ExamService` 从 473 行、11 个直接依赖收敛为 66 行、3 个依赖，保留兼容门面；考试会话、提交锁定、
  逐题判分与学习事实写入、结果展示分别进入独立 Service，原事务、超时和缓存失效语义不变。
- `CourseStageAssessmentService` 从 365 行、10 个直接依赖收敛为 207 行、7 个依赖；测评快照、学习事实
  回写和 VO 组装分别进入独立协作者，题目选择、服务端判分、错题 / 复习 / 课程事件与答案脱敏保持不变。
- `PrivateExamImportService` 从 454 行、9 个直接依赖收敛为 151 行、6 个依赖；结构化文本解析与校验、
  已确认私有试卷持久化分别独立，来源哈希、原文件配额、所有者隔离和事务确认契约不变。

### 验证

- 考试、阶段测评、私有试卷导入 / 草稿 / PDF / DOCX 与相关 Controller 共 57 个聚焦测试通过。
- `mvn clean verify -B`：后端全量测试、ArchUnit、生产 / 测试双 Checkstyle、SpotBugs 和 JaCoCo
  门禁全部通过。

### 遗留问题

- 本轮只调整职责和依赖方向，不改写既有 SQL、试卷业务状态或数据库结构；真实 MySQL 集成测试留给
  后续项目级 L3 / 远端 CI 统一执行。

## Round 282：拆分题目投稿正式入库职责

### 完成内容

- 新增 138 行的 `QuestionSubmissionImportService`，独立承担已审核投稿创建正式题目、选项与知识点关联、
  来源追踪、初审记录和投稿状态更新事务；管理端入库端点直接委托该服务。
- 新增 127 行的 `QuestionSubmissionOptionService`，在投稿提交与正式入库间统一选择题、判断题、填空题和
  简答题的 JSON 解析、答案规范化与选项标签规则；新增 89 行的 `QuestionSubmissionViewService`，统一
  投稿者、审核人和课程名称富化及分页转换。
- `QuestionSubmissionService` 从 475 行收敛为 190 行，直接依赖从 9 个降为 4 个，只保留投稿、查询、
  审核、状态统计和知识点标注应用。入库与视图专项测试迁至新边界，并补充控制器委托测试。

### 验证

- `QuestionSubmissionServiceTest`、`QuestionSubmissionImportServiceTest`、
  `QuestionSubmissionViewServiceTest` 与 `AdminQuestionSubmissionControllerTest` 共 7 个测试通过。
- `mvn clean verify -B`：后端全量 586 个测试通过；ArchUnit 5 个规则、生产 / 测试双
  Checkstyle、SpotBugs 和 JaCoCo 门禁全部通过。

### 遗留问题

- 投稿列表的展示富化继续按记录读取用户、审核人和课程，这是拆分前的既有查询行为；本轮不同时引入
  批量查询或缓存，避免改变缺失关联数据时的兼容语义。

## Round 281：拆分课程总览目标与测评摘要职责

### 完成内容

- 新增 144 行的 `CourseOverviewTargetService`，集中从知识点、已审查 Tutor 内容和理解检查事实计算
  Tutor 进度，并按 Tutor、到期复习、未掌握错题、课程顺序生成统一下一目标。
- 新增 50 行的 `CourseOverviewAssessmentService`，只读取最近一次已完成阶段测评及逐题快照，组装
  题源构成和知识点事实摘要，不推断掌握度。
- `CourseOverviewService` 从 257 行收敛为 122 行，直接依赖从 12 个降为 8 个，只保留课程库校验、
  课程学习事件、可见题目、错题、到期复习等核心事实读取与最终编排；原专项测试迁到对应新边界。

### 验证

- `CourseOverviewServiceTest`、`CourseOverviewTargetServiceTest`、
  `CourseOverviewAssessmentServiceTest` 与 `CourseLibraryControllerTest` 共 13 个测试通过。
- `mvn clean verify -B`：后端全量 583 个测试通过；ArchUnit 5 个规则、生产 / 测试双
  Checkstyle、SpotBugs 和 JaCoCo 门禁全部通过。

### 遗留问题

- 总览继续按请求读取并在内存聚合课程事实；本轮不新增进度副本，也不改写既有查询数量、时间边界或
  目标优先级，性能优化仍需以真实数据证据为前提。

## Round 280：拆分试卷学习上下文职责

### 完成内容

- 新增 94 行的 `ExamPaperLearningContextService`，集中承担试卷存在性与可见性、发布状态、课程绑定、
  个人课程库关系、题目开放状态与课程归属校验，并按既有顺序读取试卷题目和选项。
- `ExamPaperLearningService` 从 392 行收敛为 320 行，直接依赖从 12 个降为 8 个；主服务继续负责
  会话创建 / 恢复、逐题判分、完成状态及错题、复习、课程事件和缓存写事务。
- 将课程库访问校验迁为上下文服务直接测试，并补齐私有试卷隔离、跨课程题目拒绝与完整读取路径；
  URL、DTO、数据库、事务、查询顺序、答案脱敏和判分契约不变。

### 验证

- `ExamPaperLearningContextServiceTest`、`ExamPaperLearningServiceTest`、
  `ExamLearningAiServiceTest` 与 `ExamLearningControllerTest` 共 24 个测试通过。
- `mvn clean verify -B`：后端全量 583 个测试通过；ArchUnit 5 个规则、生产 / 测试双
  Checkstyle、SpotBugs 和 JaCoCo 门禁全部通过。

### 遗留问题

- 会话展示继续在试卷题目整体校验后逐题重新读取题目，这是拆分前的既有查询顺序；本轮不同时改写
  查询次数和结果组装，后续只有在性能证据与等价测试充分时再处理。

## Round 279：拆分 AI 用量总览与运营报告职责

### 完成内容

- 新增 296 行的 `AiUsageReportService`，集中承担等长周期统计对比、异常提醒推导与持久化、
  新提醒通知、未确认提醒查询和管理员确认事务。
- `AiUsageService` 从 497 行收敛为 232 行，直接依赖从 4 个降为 2 个，只保留全局、功能、模型、
  每日、用户和失败调用等管理端总览聚合。
- `AdminAiUsageController` 按端点直接委托总览、运营报告和学习效果服务；报告与提醒测试迁到新边界，
  并补充控制器委托测试。URL、DTO、数据库、阈值、通知和事务契约不变。

### 验证

- `AiUsageServiceTest`、`AiUsageReportServiceTest` 与 `AdminAiUsageControllerTest` 共 19 个测试通过。
- `mvn clean verify -B`：后端全量 580 个测试通过；ArchUnit 5 个规则、生产 / 测试双
  Checkstyle、SpotBugs 和 JaCoCo 门禁全部通过。

### 遗留问题

- 总览和报告继续在读取周期日志后进行内存聚合；本轮只拆分业务职责，没有在缺少性能证据时同时改写
  SQL 聚合、时间边界或指标公式。

## Round 278：拆分题目学习资产上下文职责

### 完成内容

- 新增 122 行的 `QuestionAssetContextService`，集中读取题目、按顺序加载选项、知识点与课程，并按
  既有顺序组装 AI 上下文；题型中文标签、正确答案标记、可选段落省略和缺题异常语义保持不变。
- `QuestionLearningAssetService` 从 393 行收敛为 307 行，直接依赖从 11 个降为 8 个，移除选项、
  题目—知识点、知识点和课程四个上下文专用 Mapper，只消费完成的上下文字符串。
- 原服务的上下文测试迁为新服务直接测试，并补齐全部题型标签、完整文本与换行顺序及缺题异常；
  原生成服务继续覆盖同步 / 流式 Provider、缓存、审计、变式题和反馈路径。

### 验证

- `QuestionAssetContextServiceTest`、`QuestionAssetPromptFactoryTest`、
  `QuestionLearningAssetServiceTest`、`AiControllerTest` 与 `AiAssetViewControllerTest` 共
  43 个测试通过。
- `mvn clean verify -B`：后端全量 579 个测试通过；ArchUnit 5 个规则、生产 / 测试双
  Checkstyle、SpotBugs 和 JaCoCo 门禁全部通过。

### 遗留问题

- 访问校验与上下文加载继续各自读取一次题目，这是拆分前的既有查询顺序；本轮不把职责迁移和缓存或
  查询性能改写混合，后续只有在有性能证据和结果等价测试时再优化。
