# 2026-09 开发日志

## Round 318：修复考试服务的 Spring 构造函数注入

### 完成内容

- 为考试会话、交卷和记录展示服务的生产构造函数显式标记依赖注入，恢复 Spring 容器装配；
  保留测试用固定时钟构造函数和现有考试时间语义。
- 增加 Spring 上下文装配回归测试，覆盖三个服务及考试门面的依赖连接，避免直接实例化的单元测试漏检启动问题。

### 验证

- 新测试在修复前复现 CI 的 `No default constructor found`；修复后装配测试和 14 个考试单测通过。
- JDK 21 下后端 `mvn clean verify` 通过：608 个测试，Checkstyle、SpotBugs 和 JaCoCo 门禁通过。
- 本机 Docker daemon 不可用，未运行本地 MySQL 集成测试和 E2E；远端验收以修复提交对应的 CI 结果为准。

## Round 317：收敛文档事实来源与更新路径

### 完成内容

- 数据库入口与迁移策略直接引用 SQL 目录，移除手写最新版本和逐版本清单；学习域按内容标识、
  课程事实、Tutor 快照与测评事务说明存储，不再复制教学迁移批次。
- 前后端架构保留模块入口、依赖方向、状态与事务归属，删去辅助类清单；AI 架构按已核对的调用治理边界更新。
- 阶段测评与试卷学习分别由 API 维护调用行为、数据库维护存储不变量、数据流描述跨模块过程，互相引用。
  roadmap 的有效候选统一指向 future，演示截图统一引用测试文档的 E2E 启停步骤。
- 工作流增加针对本次事实的三项收尾检查：确认归属、替换或删除失效描述、消除第二份完整定义。
  没有事实变化时不强制文档 diff，不以收尾检查启动全库整治。

### 验收

| 任务场景 | 已核对的读取与更新路径 |
|---|---|
| 新增数据库迁移 | 数据库入口 → 迁移策略与实际 SQL；只更新受影响存储语义，不再修改最新版本副本 |
| 调整前后端职责 | 系统架构 → 对应模块边界与源码；只有架构事实改变时更新文档，不逐个登记辅助类 |
| 修改阶段测评 | API 的请求与可见行为、数据库的快照与事务分别维护；数据流引用两者，保留跨模块过程 |
| 完成候选能力 | future 移出已完成项；roadmap 引用候选来源，阶段成果与当前状态按实际变化更新 |
| 生成演示截图 | demo 的截图命令 → testing 的隔离环境启动与清理，不维护第二套 E2E 生命周期 |

- 55 份 Markdown 的现有门禁和 diff 空白检查通过。
- 一次性清单核对确认接口集合与学习域表集合未变；14 种受限课件 Schema 仍在 API 文档，模块入口均可定位到源码。
- 已核对删减内容中的答案隔离、内容审核、会话幂等、不可变快照及原子回写约束，没有把历史批次重新复制一份。
- 仅做文档、代码引用和静态路径验收；未运行独立 Agent 行为实验。

### 交付边界

- 四步已完成，无剩余必需工作；保留原 CONTINUOUS 执行模式，不启动额外功能。
- 未修改应用代码、Skills、检查器或测试，未新增测试；未重跑应用或仓库工具测试，未运行 Docker 或远端 CI。
- 独立的 local-development.md 修改保留在工作区，不纳入本次提交；本次只创建聚焦本地提交，不推送。

## Round 316：合并重复规范并按 Codex 机制维护 Skills

### 完成内容

- 归并删除 Agent 工具说明、审查清单、工程规则、AI 战略和演示检查清单五份文档。
  规则分别归入文档中心、工作流、前后端架构、测试策略、PRD 与演示指南。
- 前端任务直接进入项目 Skill，辅助 UX 资料按需读取；删除重复的技能触发表和通用加载说明，
  保留上游来源与升级边界，不移动或修改上游 Skills。
- Skills 门禁仅检查已安装内容，不再要求固定九个技能或可选 UI 元数据；新增自有 Skill 的 Markdown
  自动纳入检查，上游内容继续排除项目专属格式规则。
- 清理 future 已实现候选项与过期依赖结论，PRD 区分自动审查目标与当前管理员发布权限，
  README 明确课程总体事实和最近测评的知识点摘要。保留原业务实现与授权边界。
- AiStu 的批次迁移说明移至本记录的历史快照，架构文档只维护来源、协议、适配和审核边界。

### 验证

- 新增两个工具测试：接受可选安装与无 UI 元数据的 Skill，拒绝无效发现元数据。
  旧检查器在合法安装场景产生预期失败；修改后全部仓库工具 43 个测试通过。
- 两个项目 Skill 的 quick_validate 通过；文档门禁、仓库布局和 diff 空白检查通过。
- 静态核对前端设计、临时浏览器验收、后端归属、文档维护、只读审查与提交六类任务的读取路径；
  没有运行独立 Agent 的行为实验。

### 边界与遗留

- 未改应用代码、部署配置或上游 Skills，未执行应用测试、Docker、push 或远端 CI。
- status 保留原 Execution Mode，只替换当前目标与相应验证类别，不追加开发轮次摘要。
- 工作区同期出现的 local-development.md 修改不属于本任务，保留且不纳入提交。

### AiStu 资源迁移历史快照

以下保留原资源复用文档中的迁移批次与教学范围证据；属于历史记录，不表示当前待办或完整课程清单。
稳定约束见[资源复用边界](../../architecture/aistu-resource-reuse.md)。

#### 实现边界

V21 首先建立稳定内容键和 `user_course` 个人课程关系，并迁入 408 数据结构课程及
8 个顶层章节。该迁移只建立真实课程目录入口，不表示 122 个原子知识点、676 个
chunks 或 10 个课件已经迁入 Web。

后续顺序是：

1. 定义服务端课程学习事件、幂等键、来源和版本；
2. 将现有练习、错题、复习、考试和 AI 观察事件映射到统一语义；
3. 选择完成审查的最小知识与课件切片，并迁移框架无关模型；
4. 实现课程总览和“开始学习”的候选目标选择；
5. Web 闭环稳定后，再决定桌面端是否复用同一服务协议。

React 组件、Electron IPC、窗口生命周期和本地持久化不属于共享协议。若课件算法模型
可独立于 React，应先提取可序列化 Schema、状态转换和测试向量，再用 Vue 实现 Web
展示层，避免维护两套不一致的教学逻辑。

#### 初始 Web 教学切片

首批选定 `ods-arraystack-insertion`（ArrayStack 按位插入）。它在 AiStu 知识快照中
对应容量检查、从右向左搬移、写入新元素和数量更新；AiStu 的
`ods.arraystack-insertion.v1` 课件在 2026-07-25 已完成独立教学审查。V23 只迁入该
原子知识的目录记录、稳定标识、版本和迁入时审查状态，尚未把 React 组件、RAG chunks
或 Electron 运行时带入 Web。

首个闭环的跨层契约如下，后续接口与失败用例必须遵守：

| 层 | 输入或事实 | 约束与失败语义 |
|---|---|---|
| 课程目标 | `courseId`、`knowledgePointId=ods-arraystack-insertion` | 用户必须先加入课程；目标不属于课程、内容未审查或不存在时拒绝开始教学。 |
| 教学内容 | 版本化原子知识与经注册的课件标识 | Tutor 只能引用已迁入、已审查的内容；课件参数必须经 Schema 校验，不能执行模型生成的前端代码。 |
| 理解检查 | 题目来源、题目 ID、服务端判分记录 | 自拟检查题不得标为官方原题；客观题答案不从客户端接收为最终事实。 |
| 学习状态 | 可追加、可幂等的课程学习事件 | 讲解展示或打开课件不等于掌握；只有结构化操作和判分等实际事实可以影响后续候选目标。 |
| 真题学习 | 可追溯的试卷、年份、题号、来源与原题结构 | 当前 AiStu 资源中没有可迁入的 408 结构化历年真题，因而不能以自拟题替代；缺少来源时此入口保持不可用。 |

V75 已在 LearnPlatform 建立通用试卷来源、核验状态和分层题号字段，并以服务端门禁阻止未核验
官方试卷发布。该结构只解决未来原题的可信录入与展示契约，不改变 AiStu 当前没有可迁入 408
结构化历年真题的事实，也没有在迁移中写入替代数据。

第二批选定同一来源中可独立验证的 `ods-arraystack-removal`（ArrayStack 按位删除）。它
保留来源、版本和审查状态，并以服务端理解检查确认“删除后后缀从左向右搬移”的顺序；该
内容没有将 AiStu 的 React 运行时或未迁入前置知识带入 Web。

第三批选定 `ods-array-size-capacity`（元素数量与数组容量）。独立审查范围仅包括有效元素
数量 `n`、后备数组容量 `length(a)`、逻辑区间 `a[0]…a[n-1]` 与不变量
`0 ≤ n ≤ length(a)`；不迁入其尚未审查的上游“数组后备存储”或下游扩缩容内容。它成为
按位插入与按位删除可访问的前置教学，不再只作为受限提示。

第四批选定 `ods-arraystack-resize`（ArrayStack 的容量调整）。独立审查范围仅包括
`max(1, 2n)` 的新数组容量、按原顺序复制 `n` 个有效元素、后备数组切换，以及单次
`O(n)` 复制成本；固定 Web 课件只回放旧数组、新数组和逐槽复制，不能执行内容脚本或把
回放过程记录为掌握；在第五批迁入前，摊还成本仅作为受限后续提示。

第五批选定 `ods-arraystack-amortized-resize`（ArrayStack 调整容量的摊还成本）。独立审查
范围限定为：从空结构开始的任意 `m` 次 `add` 与 `remove` 中，全部 `resize` 的总成本为
`O(m)`；它不改变一次实际 `resize` 的 `O(n)` 最坏成本，也不自动扩大为全部 ArrayStack
操作复杂度结论。

第六批选定 `ods-arraystack-performance`（ArrayStack 的操作复杂度）。独立审查范围限定为：
`get/set` 的最坏时间为 `O(1)`；忽略 `resize` 时 `add(i,x)` 与 `remove(i)` 为
`O(1+n-i)`；以尾端 `add(n,x)` 和 `remove(n-1)` 实现的 push/pop 为 `O(1)` 摊还时间。
单次尾端操作仍可能触发 `O(n)` 的 resize，不将摊还结论错误提升为最坏常数时间。

第七批选定 `ods-fastarraystack-block-copy`（FastArrayStack 的批量复制优化）。独立审查范围
限定为：以运行环境的批量复制或移动函数替代连续区间的显式循环，可能降低常数开销；需要
处理的元素数量不变，因此不改变插入、删除和 `resize` 的渐近时间复杂度。原始资料的具体
加速倍数不迁入为平台承诺，实际收益依赖语言、元素类型、库实现和硬件。

第八批选定 `ods-arrayqueue-representation`（ArrayQueue 的循环数组表示）。独立审查范围限定为：
队首物理下标 `j`、元素数量 `n`、`0 ≤ k < n` 与 `a[(j+k) mod capacity]` 的映射；固定 Web
课件仅回放有限数组中的回绕与 FIFO 逻辑顺序，不将物理下标回绕解释为数组旋转，也不延伸至入队、
出队、扩缩容或复杂度结论。

第九批选定 `ods-arrayqueue-enqueue`（ArrayQueue 的入队）。独立审查范围限定为：容量足够时，
将新元素写入 `a[(j+n) mod capacity]`，随后令 `n` 加一；已有队列元素不发生搬移。固定 Web
课件仅回放队尾槽位定位、写入和计数更新，不延伸至满队 `resize`、出队或复杂度结论。

第十批选定 `ods-arrayqueue-dequeue`（ArrayQueue 的出队）。独立审查范围限定为：非空队列保存
`a[j]` 后，将 `j` 更新为 `(j+1) mod capacity`、`n` 减一；其余元素不发生搬移。固定 Web 课件仅
回放队首读取、队首下标前移与计数更新，不延伸至空队列、缩容或复杂度结论。

第十一批选定 `ods-arrayqueue-resize`（ArrayQueue 调整容量时的线性化复制）。独立审查范围限定为：
对跨越旧数组末端的非空 FIFO 队列，以 `b[k] = a[(j+k) mod oldCapacity]` 将逻辑顺序复制到新数组
`b[0..n-1]`，随后令 `a = b`、`j = 0`。固定 Web 课件只回放旧数组回绕、按逻辑顺序复制与队首归零，
不陈述扩缩容触发条件、特定容量策略的合理性或摊还复杂度结论。

第十二批选定 `ods-arrayqueue-performance`（ArrayQueue 的操作复杂度）。独立审查范围限定为：忽略
`resize` 时 `add(x)` 与 `remove()` 均为 `O(1)`；从空队列开始的任意 `m` 次更新中，全部 `resize`
总成本为 `O(m)`，因此更新为 `O(1)` 摊还时间。该结论不将一次实际复制 `n` 个元素的 `resize` 错写成
最坏 `O(1)`，也不扩展到其他队列实现。

ArrayQueue 模块完成时，Web 端只复用 AiStu 已审查关系中的稳定 `contentKey`，用 V38 将循环数组表示、
入队、出队、线性化复制和操作复杂度连成受限学习路径。服务端解析路径后再次校验目标课程归属和
`REVIEWED` 状态，再向客户端返回可导航知识点 ID；数据库中的标题或目标标识不能直接成为权限事实。

第十三批选定 `ods-arraydeque-representation`（ArrayDeque 的循环数组表示与访问）。独立审查范围限定为：
逻辑起点 `j`、元素数量 `n` 与 `a[(j+i) mod capacity]` 的逻辑下标映射，`get(i)`/`set(i, x)` 可直接
访问对应槽位。固定 Web 课件只回放回绕与指定 `get(i)` 访问，不延伸至插入、删除时向较近端搬移、
resize 或复杂度结论。

第十四批选定 `ods-arraydeque-nearest-end-shifting`（ArrayDeque 向较近端搬移）。独立审查范围限定为：
插入位置靠近逻辑前端时，先将 `j` 左移回绕，仅搬移前缀再写入；它不将该分支概括为固定后缀搬移，
也不包含尾端分支、删除、resize 或复杂度。第十五批选定 `ods-arraydeque-performance`，限定为
`get/set` 的 `O(1)`、忽略 resize 的 `O(1 + min(i, n-i))` 更新成本，以及从空结构开始的 m 次更新中
resize 总成本 `O(m)`；不将摊还界写成单次最坏界。

ArrayDeque 模块完成时，Web 端用 V40/V41 将表示、近端搬移和复杂度连成受限学习路径。与其他 Tutor
路径一致，服务端只在目标属于当前课程且状态为 `REVIEWED` 时返回可导航知识点 ID。

第十六至二十批选定 `ods-dualarraydeque-representation`、`ods-dualarraydeque-end-operations`、
`ods-dualarraydeque-balance`、`ods-dualarraydeque-amortized-balance` 与 `ods-dualarraydeque-performance`。
独立审查范围分别限定为双栈的前缀逆序/后缀正序表示、按位更新的内部栈路由、严格三倍失衡后的按逻辑
顺序重建、两次重建之间的摊还间隔，以及与 ArrayDeque 相同的渐近时间界。只有表示和再平衡使用固定
受限 Web 课件：前者回放下标映射，后者回放一次已满足触发条件的重建；两者均不接受脚本、动态组件或
用户运行态，也不将一次 `O(n)` 重建误称为最坏 `O(1)`。

DualArrayDeque 模块通过 V42–V46 形成顺序学习路径。路径数据只保存稳定 `contentKey`、标题和说明，
服务端在返回可导航 ID 前继续验证课程归属和 `REVIEWED` 状态。

第二十一至二十六批选定 `ods-rootisharraystack-block-layout`、`ods-rootisharraystack-index-mapping`、
`ods-rootisharraystack-update`、`ods-rootisharraystack-grow-shrink`、`ods-rootisharraystack-space` 与
`ods-rootisharraystack-performance`。范围依次限定为递增容量块、下标定位公式、跨块按位搬移、块级增缩、
平方根额外空间界和复杂度汇总。只有递增块布局采用固定受限课件；课件数据验证每个块的容量不变量，
不接受公式执行、脚本或用户运行态。V47–V52 将六项内容连接为课程内学习路径。

第二十七批选定 `ods-array-layout-tradeoffs`，只比较已审查数组式实现的更新位置、空间浪费与内部机制，
不把渐近界偷换为实际速度结论。第二十八至三十二批选定线性表定义、顺序表存储、顺序表插删、单链表表示
和链表结点插删。仅顺序表存储使用固定受限课件，回放首地址、元素宽度和直接寻址；其余内容不执行数据
结构操作或内容内脚本。V54–V58 通过稳定 `contentKey` 连成从 List ADT 到指针安全改接的学习路径。

第三十三至三十七批选定双链表、循环链表、单链表逆置、有序链表归并和顺序/链式表示的取舍。逆置使用
固定 `LINKED_LIST_REVERSAL` v1 课件，仅回放已审查元素序列上的 `prev`、`cur` 与保存的后继；不会执行
用户输入、指针表达式或内容内脚本。V59–V63 保留原资料对成对指针不变量、回到起点的遍历终止、稳定归并
和操作前提的边界。

第三十八至四十一批选定栈的 LIFO 语义、顺序栈、链栈和合法出栈序列。保持源资料对 `top` 约定、表头
改接和辅助栈模拟的边界；该批只复用服务端教学与检查协议，不引入可执行课件。

第四十二至四十四批选定括号匹配、表达式转换与求值、递归调用栈。递归内容复用源资料的阶乘可视化意图，
但收敛为固定 `FACTORIAL_CALL_STACK` v1 参数与 Vue 渲染器；它只回放小规模活动记录，不执行函数体、
表达式或内容脚本。

第四十五至四十八批选定 FIFO 语义、循环队列表示与状态、链队列。循环队列复用已经注册的
`ARRAY_QUEUE_REPRESENTATION` v1 渲染器，不建立第二套等价课件；文本和理解检查补足 `rear` 约定、
牺牲槽位公式与空链队列尾指针重置边界。

## Round 315：精简 Agent 入口与专项规则归属

### 完成内容

- 根 AGENTS.md 从 178 行收敛为 49 行，保留项目定位、授权与安全底线，按任务触发条件和动作时机路由读取。
  默认只读入口和当前状态，产品规划与历史按需查阅。
- 文档权威来源、状态快照更新和长期规则准入集中到 docs/index；Skills 所有权、前端路由与入口维护集中到
  agent-tooling。复用现有目录，不新增分层 AGENTS 或 Skills。
- 工作流集中定义执行模式、持续维护终点和完成状态，区分授权的本地任务完成与远端交付；提交授权保持不变。
- 测试适用行为与例外归并到 testing，结构与事实审查归并到 review-checklist；删除工作流中的重复定义并修正引用。

### 验证

- 纯文档整理，不制造 Red，不新增测试，也不改动现有检查器。
- 文档检查覆盖 60 个 Markdown 文件及仓库 Skills，链接、导航、结构、职责与 diff 空白检查通过。
- 静态走查状态更新、判分修复、前端调整、只读审查和提交五类任务，核对动作前的必读入口、领域规范与
  验证位置；没有启动新的 Agent 任务或宣称运行效果已验证。
- 执行模式仍为原 CONTINUOUS 值，敏感信息、Git 远端授权、数据保护和按风险验证的底线保留。

### 遗留问题

- 本次不涉及应用代码或运行环境；未执行应用测试、Docker 或远端 CI。
- PRD、future 与 README 的产品能力一致性问题仍单独待处理。

## Round 314：恢复状态快照与文档职责门禁

### 完成内容

- 状态文档从 888 行压缩为 62 行，保留当前维护边界、能力摘要、按类别的最新验证与当前缺口；移除重复轮次记录、已退出阶段的
  过程及过期环境断言，保留原 CONTINUOUS 模式与授权范围。
- 原状态中的 71 个编号轮次均能在既有月份日志中定位；历史日志保持原样，关键验证通过锚点链接追溯。
- 文档检查限制状态快照为 150 行、10000 字符；拒绝正文中的中英文编号轮次，仅允许通过历史文档链接
  引用。历史日志与审计档案不受快照预算限制。
- AGENTS 明确状态按类别替换、保留验证适用范围；开发工作流引用同一维护规则。

### 验证

- Red：新增 7 个测试，旧检查器产生 9 处预期断言失败，复现轮次追加、错误引用及无预算限制。
- Green：7 个测试通过，新检查器对原状态文件报告行数、字符数和轮次记录三项违规。
- 全部仓库工具 41 个测试通过；60 个 Markdown 文件与 Skills 文档检查、仓库布局检查和 diff 空白检查通过。

### 遗留问题

- 未运行应用测试、Docker 或远端 CI；本次修改仅涉及仓库文档及检查工具。
- PRD 审查机制、future 候选状态与 README 能力措辞等其他审查发现未纳入本次状态文档修复。

## Round 313：收敛私有试卷导入逻辑密度

### 完成内容

- `PrivateExamImportDialog.vue` 脚本从 307 行降为 263 行，总文件从 599 行降为 555 行；
  `privateExamImportRequests` 隔离文本、PDF、DOCX 的预览、确认与 AI 补全草稿请求分派。
- 代码结构门禁新增 Vue 单个脚本块不超过 300 行，继续保留 SFC 699 行硬上限；总行数与逻辑密度分别
  约束，避免大段样式掩盖脚本膨胀。
- 文本 / 文件导入、内容哈希确认、原文件重复提交和草稿复核协议不变。

### 验证

- 私有试卷列表与导入 11 个测试通过；相关 ESLint、TypeScript 和新增结构门禁 5 个测试通过。
- 当前 Vue 脚本块最高 263 行，代码结构门禁通过。

### 遗留问题

- 导入组件仍是 10 个状态的完整状态机，但状态共同维护导入表单、预览与复核阶段，不继续拆成跨组件事务。

## Round 312：拆分相似题练习闭包

### 完成内容

- `WrongQuestionView.vue` 从 611 行降为 508 行、响应式状态从 11 处降为 8 处、API 模块从 5 组降为
  3 组，只保留错题列表、筛选、掌握度与错题重练协调。
- `SimilarQuestionsDialog` 自包含相似题检索、结果展示、完整题目补全、会话存储和练习页导航。
- 相似度展示、最多 8 题、完整题目补全与既有 `similar` 会话协议不变。

### 验证

- 新增相似题组件测试，覆盖推荐加载、完整题目补全、会话写入和导航；连同错题路由筛选共 3 个测试通过。
- 相关 ESLint 与 TypeScript 检查通过。

### 遗留问题

- 错题页总行数仍为 508，但脚本仅 202 行且状态 / API 已收敛；剩余主要是错题卡片模板与响应式样式，
  不为跨过 500 软线机械拆分。

## Round 311：结构重构全量本地回归

### 完成内容

- 修复 `ReviewScheduleQueryService` 一处超过 120 字符的格式门禁问题，不改变查询行为。
- 对本轮后端职责拆分与前端业务闭包拆分执行完整本地回归，确认聚焦测试之外的模块未出现回归。

### 验证

- 后端 `mvn clean verify -B` 共 607 个测试通过，Checkstyle、SpotBugs 与 JaCoCo 通过。
- 前端 79 个测试文件、351 个测试、覆盖率门槛、ESLint、Prettier 和学习 / 管理双生产构建通过。
- 前端覆盖率为语句 47.17%、分支 40.74%、函数 38.19%、行 49.29%。

### 遗留问题

- 本机 Docker daemon 仍未验证为可用，本轮未重跑 Testcontainers 与 Playwright E2E，不将其记为通过。

## Round 310：拆分投稿审核与详情闭包

### 完成内容

- `SubmissionManage.vue` 从 544 行降为 368 行、响应式状态从 18 处降为 12 处，只保留列表、筛选、
  选择、批量审核 / 入库和 AI 工具协调。
- `SubmissionReviewDialog` 自包含审核校验、AI 意见生成与提交，`SubmissionDetailDialog` 自包含详情展示
  和正式题库导航；题型、状态、时间与选项解析集中到纯展示模块。
- 单条审核、详情导航、批量操作、列表与统计刷新语义不变；非数组选项 JSON 现在安全回退为空列表。

### 验证

- 新增 2 个审核组件测试，覆盖拒绝理由约束、AI 意见应用、审核提交和刷新事件。
- 相关 ESLint 与 TypeScript 检查通过。

### 遗留问题

- 列表与批处理共享选择、加载和刷新状态，属于同一管理表格事务边界，暂不抽成转发型包装层。

## Round 309：拆分学习资产可见性追踪

### 完成内容

- `QuestionLearningAsset.vue` 从 521 行进一步降为 493 行、响应式状态从 12 处降为 10 处，继续保留
  资产内容和变式训练业务编排。
- `useAssetViewTracking` 独立管理 IntersectionObserver 生命周期、视口状态、同题同类型去重、失败重试
  与变式训练查看结果回传。
- 可见后才记录、重复进入视口不重复记录、失败后可重试的语义不变。

### 验证

- 资产可见性、变式训练和反馈共 4 个测试通过；相关 ESLint 与 TypeScript 检查通过。

### 遗留问题

- 资产生成和变式训练都修改当前资产状态，继续由同一主组件协调，暂不拆散该一致性边界。

## Round 308：拆分学习资产反馈闭包

### 完成内容

- `QuestionLearningAsset.vue` 从 687 行降为 521 行、响应式状态从 15 处降为 12 处，保留资产读取、
  流式生成、可见性记录和变式训练协调。
- `QuestionAssetFeedback` 自包含单类资产的既有反馈读取、有用 / 无用评价、重新反馈和补充说明提交；
  已无调用方的反馈初始状态工厂同步移除。
- 反馈失败继续不阻断学习内容，反馈 API、按钮状态和补充说明语义不变。

### 验证

- 新增 2 个反馈组件测试，覆盖既有反馈读取、负向反馈和补充说明提交；连同资产可见性与训练测试共
  4 个测试通过。
- 相关 ESLint 与 TypeScript 检查通过。

### 遗留问题

- 主组件仍协调资产缓存、流式生成、可见性记录和变式训练；其中可见性记录可在后续作为独立横切职责拆分。

## Round 307：拆分逐卡复习会话状态机

### 完成内容

- `ReviewView.vue` 从 681 行降为 456 行、页面响应式状态从 18 处降为 9 处，只保留统计、计划维护、
  到期卡片装载和 AI 建议协调。
- `ReviewSessionPanel` 自包含逐卡答题、提交、正确性判定、跳题、结束和完成状态，并在产生复习事实后
  通知父页面刷新统计；状态标签映射随会话展示迁入 `components/review/`。
- 到期筛选、自动开始、提交判定、会话提示和计划维护 API 语义不变。

### 验证

- 新增会话组件测试覆盖开始、答案提交、正确反馈和统计刷新事件；连同路由目标复习测试共 3 个测试通过。
- 相关 ESLint 与 TypeScript 检查通过。

### 遗留问题

- 计划列表与 AI 建议仍由页面协调，但两者状态独立、依赖较少，当前继续拆分收益有限。

## Round 306：拆分试卷编辑与选题业务闭包

### 完成内容

- `ExamManage.vue` 从 692 行降为 204 行，只保留试卷列表、统计、发布、删除和对话框协调。
- `ExamPaperEditorDialog` 自包含新增 / 编辑表单、详情读取、题目选择和保存生命周期；
  `examPaperEditorModel` 集中表单初始化、详情映射、请求映射与官方来源发布约束。
- 列表、智能组卷、手工组卷 API 与刷新语义不变；题目排序值为 `0` 时不再被详情数组下标覆盖。

### 验证

- 新增 2 个纯模型测试，覆盖详情 / 请求双向映射和官方试卷发布约束。
- 相关 ESLint、TypeScript 与学习 / 管理双生产构建通过。

### 遗留问题

- 编辑器仍是 393 行的完整业务对话框，但脚本状态围绕同一试卷草稿，当前没有继续拆成无业务价值包装层。

## Round 305：拆分题目复审业务闭包

### 完成内容

- `QuestionManage.vue` 从 697 行进一步降为 462 行，只保留题目列表与重复题、纠错、版本治理协调。
- `QuestionReviewDialog` 自包含复审记录读取、AI 建议、建议应用、表单校验和复审提交，并通过
  `reviewed` 事件请求列表刷新。
- 复审 API、动作、提示、建议应用、历史记录和刷新时机不变；原内联样式转为组件级样式。

### 验证

- 新增组件测试覆盖打开复审、生成并应用 AI 建议和提交修订；连同管理列表共 3 个测试通过。
- 相关 ESLint、TypeScript 与学习 / 管理双生产构建通过。

### 遗留问题

- 重复题、纠错和版本治理继续共享一组抽屉与页面状态，当前构成同一题库治理入口，暂不拆成孤立包装层。

## Round 304：拆分复习卡片展示与状态策略

### 完成内容

- `ReviewScheduleQueryService` 从 366 行、18 个方法降为 274 行、13 个方法，保留到期筛选、统计、
  历史与 AI 上下文查询。
- `ReviewScheduleCardViewService` 独立承担题目 / 课程批量补全、逾期状态和题干展示组装；
  `ReviewSchedulePolicy` 统一新卡片、困难和已掌握状态阈值，避免查询统计反向依赖展示服务。
- 公共查询入口、SQL 筛选、DTO、状态标签与阈值语义不变。

### 验证

- 新增 3 个展示与策略测试；连同查询、Controller、SM-2 和 ArchUnit 共 27 个测试通过。

### 遗留问题

- AI 复习上下文和统计仍共享查询服务，但使用同一复习事实且当前依赖数为 4，暂不机械拆分。

## Round 303：拆分用户列表与账户维护职责

### 完成内容

- `UserManage.vue` 从 687 行降为 291 行，只保留用户表格、统计卡片和行命令协调。
- `useAdminUserList` 独立管理分页筛选、统计、表格选择、状态切换、批量状态和删除命令；
  `UserMaintenanceDialogs` 自包含创建用户、角色、密码和 AI 日配额表单。
- 管理端 API、确认交互、表单校验、审计记录和列表 / 统计刷新语义不变。

### 验证

- 新增 2 个列表 composable 测试；相关 ESLint、TypeScript 与学习 / 管理双生产构建通过。

### 遗留问题

- 对话框表单仍主要由类型检查和生产构建覆盖；后续行为变更时再按风险补组件交互测试。

## Round 302：建立代码结构回归门禁并完成本地收口验证

### 完成内容

- 新增 `check-code-structure.py`，限制 Spring Service 不超过 400 行和 8 个直接依赖、Vue SFC
  不超过 699 行，并接入 CI 文档任务。
- 新增 4 个脚本测试覆盖阈值、超限拒绝、静态常量和非 Service 排除规则。
- 修复完整门禁发现的 Markdown 解析器一行多语句，以及 4 个前端文件的存量格式回归。

### 验证

- 后端 `mvn clean verify -B`：604 个测试、Checkstyle、SpotBugs 和 JaCoCo 通过。
- 前端 73 个测试文件、341 个测试与覆盖率门槛通过；ESLint、Prettier、TypeScript 和学习 / 管理
  双生产构建通过。覆盖率为语句 44.56%、分支 37.70%、函数 35.93%、行 46.42%。
- 仓库脚本 33 个测试、路径门禁、代码结构门禁和文档门禁通过。

### 遗留问题

- 本机 Docker daemon 未运行，无法连接本地 socket；本轮未重跑 Testcontainers 与 Playwright E2E，
  不将这两项记为通过。

## Round 301：拆分复习答题事实写入职责

### 完成内容

- `SpacedRepetitionService` 从 380 行、9 个直接依赖降为 293 行、5 个直接依赖，只保留复习计划、
  错题计划同步、SM-2 调度与事务编排。
- `ReviewAnswerRecordingService` 独立承担题目判分、练习记录、课程学习事件和错题事实更新。
- 复习接口、事务入口、答案归一化、学习事件类型、错题更新和调度算法不变。

### 验证

- 新增 3 个答题事实测试；复习调度、错题同步、查询与 ArchUnit 共 28 个测试通过。

### 遗留问题

- 当前直接依赖最高值已降为 8；下一轮用自动门禁阻止体量和依赖数量回退。

## Round 300：拆分题目目录与管理列表状态

### 完成内容

- `QuestionManage.vue` 从 825 行降为 697 行，分页筛选、表格选择、删除和学习资产缓存命令进入
  `useQuestionAdminList`；复审、重复题、纠错和版本治理仍由页面编排。
- `QuestionListView.vue` 从 819 行降为 644 行，路由筛选、分页、收藏、评论展开和纠错状态进入
  `useQuestionCatalog`。
- API 参数、确认交互、错误提示、分页与筛选语义不变。

### 验证

- 新增 2 个 composable 测试文件、4 个测试；相关 ESLint 与 `vue-tsc --noEmit` 通过。
- 当前全部 Vue SFC 少于 700 行，后端 Service 最高 388 行。

### 遗留问题

- 本轮聚焦状态边界；前端全量测试、覆盖率和双生产构建在结构治理收口门禁统一执行。

## Round 299：提取题目学习资产状态模型

### 完成内容

- `QuestionLearningAsset.vue` 从 773 行降为 687 行，保留资产 API、流式生成、反馈和可见性编排。
- 七类资产页签元数据、内容 / 模型 / 反馈初始结构，以及变式训练状态应用与重置进入 `assetState`。
- 资产类型顺序、提示文案、切题重置、变式训练恢复和反馈状态不变。

### 验证

- `QuestionLearningAsset` 2 个测试通过，相关 ESLint 与 `vue-tsc --noEmit` 通过。

### 遗留问题

- IntersectionObserver 的真实浏览器查看上报在最终关键流程验收中继续核对。

## Round 298：拆分练习答案与全局搜索交互状态

### 完成内容

- `PracticeSessionView.vue` 从 758 行降为 687 行，答案、多选、可提交判断、退出路由和题型展示独立。
- `GlobalSearchDialog.vue` 从 754 行降为 699 行，移动视口、全局快捷键、结果模型和文本高亮独立。
- 增加共享 `useMobileViewport`，集中搜索与练习的 768px 监听和卸载清理。
- 练习提交参数、结果弹窗关闭时机、搜索防抖、键盘导航和路由行为不变。

### 验证

- `PracticeSessionView` 测试通过，相关 ESLint 与 `vue-tsc --noEmit` 通过。

### 遗留问题

- 全局搜索尚无专属组件测试，最终学习端生产构建与浏览器流程继续覆盖快捷键集成。

## Round 297：拆分试卷管理展示映射与复习建议流

### 完成内容

- `ExamManage.vue` 从 714 行降为 697 行，题型和试卷来源标签进入纯展示模块。
- `ReviewView.vue` 从 734 行降为 681 行，SSE 数据帧消费、查询参数归一化和状态标签映射独立。
- 试卷类型文案、复习卡片状态、AI 建议增量展示与错误提示不变。

### 验证

- `ReviewView` 2 个测试通过，相关 ESLint 与 `vue-tsc --noEmit` 通过。

### 遗留问题

- 管理端试卷页暂无专属组件测试，最终管理端生产构建继续覆盖其类型与模板集成。

## Round 296：提取布局响应式与考试倒计时状态

### 完成内容

- `AppLayout.vue` 从 710 行降为 695 行，视口监听和移动侧栏状态进入 `useResponsiveSidebar`。
- `ExamTakeView.vue` 从 717 行降为 690 行，服务端权威时间偏移、倒计时更新和定时器清理进入
  `useExamCountdown`。
- 侧栏断点、桌面自动关闭、考试响应延迟扣除、超时跳转和提交状态保护不变。

### 验证

- `ExamTakeView` 9 个测试通过，相关 ESLint 与 `vue-tsc --noEmit` 通过。

### 遗留问题

- `AppLayout` 尚无专属组件测试，最终前端完整构建与流程回归继续覆盖布局集成。

## Round 295：拆分 AI 学习效果跨题统计职责

### 完成内容

- `AiLearningEffectService` 从 535 行收敛为 388 行，保留时间窗口事实读取、同题、变式训练和资产类型聚合。
- `AiLearningCrossQuestionService` 独立读取知识点关系并计算 30 天跨题迁移对照；
  `AiLearningEffectConclusionService` 独立生成总体样本结论。
- 最小样本、最小用户数、同题 / 跨题分组、正确率差值和观察性非因果表述不变。

### 验证

- AI 学习效果、管理端 AI 用量 Controller 与 ArchUnit 共 14 个测试通过。

### 遗留问题

- 统计仍为观察性数据，不将相关性解释为 AI 内容造成的因果提升。

## Round 294：拆分投稿质检结果构造职责

### 完成内容

- `SubmissionAiQualityService` 从 411 行收敛为 223 行，只保留投稿读取、Prompt、AI 调用治理和审核意见。
- `SubmissionQualityResultService` 统一解析结构化 AI 结果、处理异常输出并执行无 AI 基础规则检查。
- 缓存键、配额、调用审计、提示词、评分降级和审核意见格式不变。

### 验证

- 投稿 AI 质检与 ArchUnit 共 15 个测试通过，覆盖标准 JSON、异常输出和 Provider 失败降级。

### 遗留问题

- AI 质检继续只作为管理员审核参考，不自动修改投稿状态。

## Round 293：拆分 Excel 题目行转换职责

### 完成内容

- `QuestionImportExportService` 从 469 行收敛为 312 行，只保留 Excel 流、逐行入库和失败清理。
- `QuestionExcelRowService` 统一题型映射、选项与答案解析、导出单行组装及选项 / 知识点读取。
- Excel 模板、列格式、中文与英文题型、判断题默认选项和逐行错误信息不变。

### 验证

- 新增 3 个题型与选项边界测试；连同管理端题目 Controller、ArchUnit 共 24 个测试通过。

### 遗留问题

- EasyExcel 文件级往返在最终 L3 阶段继续由完整后端门禁覆盖。

## Round 292：拆分 Markdown 题目解析与入库职责

### 完成内容

- `MarkdownQuestionParser` 从 616 行收敛为 306 行，只保留导入校验、事务写入、关联维护和失败清理。
- `MarkdownQuestionContentParser` 独立承担 Markdown 字段语法、题型推断、内联 / 列表选项和答案归一化，
  不依赖 Mapper。
- 支持格式、容错规则、题型映射、判断题默认选项、课程 / 知识点匹配和逐题失败语义不变。

### 验证

- Markdown 解析、私有试卷导入、管理端题目 Controller 与 ArchUnit 共 37 个测试通过。

### 遗留问题

- Excel 导入导出仍在下一后端结构批次中治理。

## Round 291：拆分私有试卷草稿数据与 AI 答案职责

### 完成内容

- `PrivateExamDraftService` 从 435 行、8 个直接依赖收敛为 127 行、3 个依赖，只保留草稿生命周期门面。
- `PrivateExamDraftDataService` 管理来源、草稿、题目、JSON 转换和展示模型；
  `PrivateExamDraftAnswerService` 管理配额校验、提示词、结构化响应和调用审计。
- 原始文件配额、所有者隔离、答案结构校验、逐题人工复核、幂等确认和事务入口不变。

### 验证

- 私有草稿、确认导入和 ArchUnit 共 17 个聚焦测试通过。

### 遗留问题

- 本轮沿用既有单次 AI 补全调用方式，不增加批量生成或重试队列。

## Round 290：拆分试卷展示与发布校验职责

### 完成内容

- `ExamPaperService` 从 477 行、6 个直接依赖收敛为 226 行、4 个依赖，只编排管理端写事务。
- `ExamPaperViewService` 承担公开 / 私有访问范围、分页和题目详情展示；`ExamPaperValidationService`
  统一试卷类型、发布条件、官方来源与题号、主观题评分点校验。
- 管理端与学习端 API、正确答案隐藏、官方试卷来源约束、草稿锁定和事务语义不变。

### 验证

- 试卷服务、管理端与学习端 Controller、私有导入、智能组卷创建和 ArchUnit 共 32 个测试通过。

### 遗留问题

- 私有试卷草稿复核仍在独立后续批次中治理。

## Round 289：拆分题目可视化渲染职责

### 完成内容

- `QuestionVisualInteractive.vue` 从 831 行收敛为 70 行，只保留加载、解析失败回退和空状态。
- 内容解析、可视化类型分派与 ECharts、Mermaid、递归树分别进入 `question-visual` 下的纯函数和领域组件；
  单个新组件均低于 500 行。
- 原属性、JSON / Markdown 容错、图表选项、Mermaid 安全级别和响应式展示不变。

### 验证

- 题目可视化与学习资产 3 个测试文件、7 个测试通过；相关 ESLint 与 `vue-tsc --noEmit` 通过。

### 遗留问题

- 本轮未执行真实浏览器视觉回归；最终前端结构收口时统一执行生产构建与关键流程验收。

## Round 288：提取自适应练习策略

### 完成内容

- `AdaptivePracticeService` 从 452 行收敛为 294 行，只保留练习事实读取、候选题查询和展示模型组装。
- 无状态的难度权重、题量分配和推荐摘要进入 `AdaptivePracticePolicy`，与 Mapper 和 Spring 生命周期解耦。
- 新用户权重、历史正确率调节、近期题目排除、推荐摘要字段和随机选题行为不变。

### 验证

- 新增策略边界与摘要测试，连同练习 Controller 纳入后端 57 个聚焦回归测试并通过。

### 遗留问题

- 随机候选不足时的跨难度补位继续保留既有实现，本轮未改变推荐算法。

## Round 287：拆分题目查询与写入职责

### 完成内容

- `QuestionService` 从 427 行、7 个直接依赖收敛为 170 行、3 个依赖，只保留公开查询、访问校验和兼容门面。
- `QuestionViewService` 统一组装课程、选项、知识点和重复题展示模型；`QuestionMutationService` 承担题目、
  选项、知识点关系写入，以及版本快照和已发布试卷保护。
- 管理端与用户端返回字段、私有题访问规则、缓存失效、事务入口和错误码不变。

### 验证

- 题目服务及相关管理端、用户端 Controller、AI 变式审核与 ArchUnit 纳入后端 57 个聚焦回归测试并通过。

### 遗留问题

- 本轮只调整题目核心维护边界；批量导入导出服务仍在后续结构治理范围内。

## Round 286：拆分智能组卷选择与展示职责

### 完成内容

- `AiExamGenerationService` 从 590 行、7 个直接依赖收敛为 150 行、4 个依赖，只保留预览与创建编排。
- 候选题与学习事实加载、难度 / 知识点均衡选择、预览分布与说明、草稿试卷创建分别进入
  `AiExamCandidateLoader`、`AiExamQuestionSelectionService`、`AiExamPreviewPresentationService` 与
  `AiExamPaperCreationService`。
- 题目公开与启用范围、既有随机扰动、选题权重、预览字段和 `ExamPaperService` 事务创建契约不变。

### 验证

- 智能组卷门面及四个协作者新增 10 个测试通过，相关管理端 Controller 与 ArchUnit 通过。
- 后端聚焦回归共 57 个测试通过。

### 遗留问题

- 选题继续保留既有 `Math.random()` 微扰，测试验证业务约束和分布字段，不固定随机排序。

## Round 285：拆分课程总览展示闭包

### 完成内容

- `CourseOverviewView.vue` 从 937 行收敛为 260 行，只保留路由、真实 API、学习入口和阶段测评状态机。
- 新增 `CourseOverviewContent.vue`，集中承载课程空间的加载 / 失败 / 空数据、目录、学习工具、学习事实、
  继续学习入口和响应式展示；沿用既有 Design Tokens、组件层级和移动端断点。
- 补充总览失败重试与最近阶段测评直接复盘回归测试，原路由、事件、加载状态和接口参数不变。

### 验证

- `CourseOverviewView.test.ts` 16 个测试通过。
- 前端 ESLint 0 错误、学习端与管理端生产构建通过。

### 遗留问题

- 本轮未执行真实浏览器截图回归；构建继续存在第三方 VueUse pure annotation 与既有大 chunk 警告。

## Round 284：拆分通用 AI 门面职责

### 完成内容

- `AiService` 从 502 行、8 个直接依赖、19 个公开方法的综合实现收敛为 102 行、3 个依赖的兼容门面。
- 题目解析 / 变式题 / 试卷学习辅导、复习建议、知识点总结和带配额审计的 Provider 调用分别进入
  `AiQuestionAssistanceService`、`AiReviewSuggestionService`、`AiKnowledgeSummaryService` 与
  `AiInvocationService`；既有 Controller 和试卷学习调用契约保持不变。
- 配额检查、调用耗时、成功 / 失败审计、Prompt 指纹输入、题目可见性和流式回调语义保持原样。

### 验证

- AI、复习与试卷学习相关 13 个聚焦测试通过。
- `mvn clean verify -B`：后端全量测试、ArchUnit、生产 / 测试双 Checkstyle、SpotBugs 和 JaCoCo
  门禁全部通过。

### 遗留问题

- 错题复习建议继续逐条读取关联题目，这是既有查询语义；本轮只拆分职责，不在缺少性能样本时改写
  查询或提示词内容。

## Round 283：拆分考试与私有试卷核心编排职责

### 完成内容

- `ExamService` 从 473 行、11 个直接依赖收敛为 66 行、3 个依赖，保留兼容门面；考试会话、提交锁定、
  逐题判分与学习事实写入、结果展示分别进入独立 Service，原事务、超时和缓存失效语义不变。
- `CourseStageAssessmentService` 从 365 行、10 个直接依赖收敛为 207 行、7 个依赖；测评快照、学习事实
  回写和 VO 组装分别进入独立协作者，题目选择、服务端判分、错题 / 复习 / 课程事件与答案脱敏保持不变。
- `PrivateExamImportService` 从 454 行、9 个直接依赖收敛为 151 行、6 个依赖；结构化文本解析与校验、
  已确认私有试卷持久化分别独立，来源哈希、原文件配额、所有者隔离和事务确认契约不变。

### 验证

- 考试、阶段测评、私有试卷导入 / 草稿 / PDF / DOCX 与相关 Controller 共 57 个聚焦测试通过。
- `mvn clean verify -B`：后端全量测试、ArchUnit、生产 / 测试双 Checkstyle、SpotBugs 和 JaCoCo
  门禁全部通过。

### 遗留问题

- 本轮只调整职责和依赖方向，不改写既有 SQL、试卷业务状态或数据库结构；真实 MySQL 集成测试留给
  后续项目级 L3 / 远端 CI 统一执行。

## Round 282：拆分题目投稿正式入库职责

### 完成内容

- 新增 138 行的 `QuestionSubmissionImportService`，独立承担已审核投稿创建正式题目、选项与知识点关联、
  来源追踪、初审记录和投稿状态更新事务；管理端入库端点直接委托该服务。
- 新增 127 行的 `QuestionSubmissionOptionService`，在投稿提交与正式入库间统一选择题、判断题、填空题和
  简答题的 JSON 解析、答案规范化与选项标签规则；新增 89 行的 `QuestionSubmissionViewService`，统一
  投稿者、审核人和课程名称富化及分页转换。
- `QuestionSubmissionService` 从 475 行收敛为 190 行，直接依赖从 9 个降为 4 个，只保留投稿、查询、
  审核、状态统计和知识点标注应用。入库与视图专项测试迁至新边界，并补充控制器委托测试。

### 验证

- `QuestionSubmissionServiceTest`、`QuestionSubmissionImportServiceTest`、
  `QuestionSubmissionViewServiceTest` 与 `AdminQuestionSubmissionControllerTest` 共 7 个测试通过。
- `mvn clean verify -B`：后端全量 586 个测试通过；ArchUnit 5 个规则、生产 / 测试双
  Checkstyle、SpotBugs 和 JaCoCo 门禁全部通过。

### 遗留问题

- 投稿列表的展示富化继续按记录读取用户、审核人和课程，这是拆分前的既有查询行为；本轮不同时引入
  批量查询或缓存，避免改变缺失关联数据时的兼容语义。

## Round 281：拆分课程总览目标与测评摘要职责

### 完成内容

- 新增 144 行的 `CourseOverviewTargetService`，集中从知识点、已审查 Tutor 内容和理解检查事实计算
  Tutor 进度，并按 Tutor、到期复习、未掌握错题、课程顺序生成统一下一目标。
- 新增 50 行的 `CourseOverviewAssessmentService`，只读取最近一次已完成阶段测评及逐题快照，组装
  题源构成和知识点事实摘要，不推断掌握度。
- `CourseOverviewService` 从 257 行收敛为 122 行，直接依赖从 12 个降为 8 个，只保留课程库校验、
  课程学习事件、可见题目、错题、到期复习等核心事实读取与最终编排；原专项测试迁到对应新边界。

### 验证

- `CourseOverviewServiceTest`、`CourseOverviewTargetServiceTest`、
  `CourseOverviewAssessmentServiceTest` 与 `CourseLibraryControllerTest` 共 13 个测试通过。
- `mvn clean verify -B`：后端全量 583 个测试通过；ArchUnit 5 个规则、生产 / 测试双
  Checkstyle、SpotBugs 和 JaCoCo 门禁全部通过。

### 遗留问题

- 总览继续按请求读取并在内存聚合课程事实；本轮不新增进度副本，也不改写既有查询数量、时间边界或
  目标优先级，性能优化仍需以真实数据证据为前提。

## Round 280：拆分试卷学习上下文职责

### 完成内容

- 新增 94 行的 `ExamPaperLearningContextService`，集中承担试卷存在性与可见性、发布状态、课程绑定、
  个人课程库关系、题目开放状态与课程归属校验，并按既有顺序读取试卷题目和选项。
- `ExamPaperLearningService` 从 392 行收敛为 320 行，直接依赖从 12 个降为 8 个；主服务继续负责
  会话创建 / 恢复、逐题判分、完成状态及错题、复习、课程事件和缓存写事务。
- 将课程库访问校验迁为上下文服务直接测试，并补齐私有试卷隔离、跨课程题目拒绝与完整读取路径；
  URL、DTO、数据库、事务、查询顺序、答案脱敏和判分契约不变。

### 验证

- `ExamPaperLearningContextServiceTest`、`ExamPaperLearningServiceTest`、
  `ExamLearningAiServiceTest` 与 `ExamLearningControllerTest` 共 24 个测试通过。
- `mvn clean verify -B`：后端全量 583 个测试通过；ArchUnit 5 个规则、生产 / 测试双
  Checkstyle、SpotBugs 和 JaCoCo 门禁全部通过。

### 遗留问题

- 会话展示继续在试卷题目整体校验后逐题重新读取题目，这是拆分前的既有查询顺序；本轮不同时改写
  查询次数和结果组装，后续只有在性能证据与等价测试充分时再处理。

## Round 279：拆分 AI 用量总览与运营报告职责

### 完成内容

- 新增 296 行的 `AiUsageReportService`，集中承担等长周期统计对比、异常提醒推导与持久化、
  新提醒通知、未确认提醒查询和管理员确认事务。
- `AiUsageService` 从 497 行收敛为 232 行，直接依赖从 4 个降为 2 个，只保留全局、功能、模型、
  每日、用户和失败调用等管理端总览聚合。
- `AdminAiUsageController` 按端点直接委托总览、运营报告和学习效果服务；报告与提醒测试迁到新边界，
  并补充控制器委托测试。URL、DTO、数据库、阈值、通知和事务契约不变。

### 验证

- `AiUsageServiceTest`、`AiUsageReportServiceTest` 与 `AdminAiUsageControllerTest` 共 19 个测试通过。
- `mvn clean verify -B`：后端全量 580 个测试通过；ArchUnit 5 个规则、生产 / 测试双
  Checkstyle、SpotBugs 和 JaCoCo 门禁全部通过。

### 遗留问题

- 总览和报告继续在读取周期日志后进行内存聚合；本轮只拆分业务职责，没有在缺少性能证据时同时改写
  SQL 聚合、时间边界或指标公式。

## Round 278：拆分题目学习资产上下文职责

### 完成内容

- 新增 122 行的 `QuestionAssetContextService`，集中读取题目、按顺序加载选项、知识点与课程，并按
  既有顺序组装 AI 上下文；题型中文标签、正确答案标记、可选段落省略和缺题异常语义保持不变。
- `QuestionLearningAssetService` 从 393 行收敛为 307 行，直接依赖从 11 个降为 8 个，移除选项、
  题目—知识点、知识点和课程四个上下文专用 Mapper，只消费完成的上下文字符串。
- 原服务的上下文测试迁为新服务直接测试，并补齐全部题型标签、完整文本与换行顺序及缺题异常；
  原生成服务继续覆盖同步 / 流式 Provider、缓存、审计、变式题和反馈路径。

### 验证

- `QuestionAssetContextServiceTest`、`QuestionAssetPromptFactoryTest`、
  `QuestionLearningAssetServiceTest`、`AiControllerTest` 与 `AiAssetViewControllerTest` 共
  43 个测试通过。
- `mvn clean verify -B`：后端全量 579 个测试通过；ArchUnit 5 个规则、生产 / 测试双
  Checkstyle、SpotBugs 和 JaCoCo 门禁全部通过。

### 遗留问题

- 访问校验与上下文加载继续各自读取一次题目，这是拆分前的既有查询顺序；本轮不把职责迁移和缓存或
  查询性能改写混合，后续只有在有性能证据和结果等价测试时再优化。
