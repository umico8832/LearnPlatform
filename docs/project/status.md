# 项目状态

> 当前项目状态的唯一权威来源。重大历史见[项目里程碑](history.md)，长期规划见[产品路线图](../product/roadmap.md)。

## Agent 执行状态

- Execution Mode: NORMAL（用户 2026-09-26 恢复开发；本轮会话复盘记忆已完成模块验收，提交后暂停）。
- 当前任务范围和操作授权只以用户最新要求为准。本页记录的历史完成状态不构成后续任务的 push、重写历史或其他远端操作授权。
- 模式变更遵循[工作流](../development/workflow.md#6-执行模式与阶段边界)；执行模式不扩大当前任务范围。

## 当前阶段

- Phase 23、24 已结束；[Tutor Agent 升级](../product/roadmap.md#tutor-agent-升级)的模型与治理底座及首个可控执行循环已在本地实现。
- Phase 24 演示截图按用户指示跳过，保留截图脚本；阶段结果见[项目里程碑](history.md)。
- Tutor RAG 已接通受治理的 Embedding、版本化知识导入、人工审核、Qdrant 索引与课程检索工具；默认关闭。
- 理解检查模块已 LOCALLY_VERIFIED：检查动作随对话恢复，用户显式作答，服务端首次判分与学习事件一致，Agent 读取真实结果；聚焦回归、MySQL 与隔离浏览器验收通过。
- 分级提示模块已 LOCALLY_VERIFIED：用户逐次请求，服务端按完整历史控制 1–3 级，失败不推进，刷新恢复一致；提示写回与首次作答共享会话行锁，拒绝迟到提示。聚焦回归、全量质量门禁与隔离浏览器验收通过。
- 已审查变式训练模块已 LOCALLY_VERIFIED：推荐限定本节已批准的正式题，用户显式作答；首次题面与结果快照可恢复，与真实练习、错题及课程事件同事务保存。并发、回滚、权限、改题后恢复与隔离浏览器闭环通过。
- 学习计划确认模块已 LOCALLY_VERIFIED：基于本人课程记录提出最多三步建议，用户显式确认并恢复原步骤及确认时间；确认不写学习事实。审核撤回、过期目标、并发及越权限制已验证，真实前后端闭环通过。
- 课程偏好与目标记忆已 LOCALLY_VERIFIED：用户显式保存、纠正或删除，跨会话使用；版本保护拒绝过期覆盖，每轮新提问读取最新快照，用户自述与学习事实分离。MySQL、前后端及隔离浏览器验证通过。
- 会话复盘记忆已 LOCALLY_VERIFIED：本人小结可分页查看、纠正和删除，关联原会话的真实理解检查；源撤回后隐藏证据且仍可删除文字，每轮最多读取五条可用小结。权限、并发、前后端及浏览器闭环通过。
- 当前模块无 Remaining Required；会话复盘不自动生成摘要、不包含变式或测评证据。后续进入记忆基线/消融评估及可复现演示，Fine-tuning 尚未开始。
- 外部验收待办：配置真实云模型并完成检索质量/来源忠实度/成本验证，人工核对首个知识版本及使用许可；现有 408 快照仍待审。
- Stop Condition：本轮会话复盘记忆已达到模块验收，本地提交后结束。缺少云配置或内容未审核时不启用检索、不声称已完成真实教学验收。

## 社区共建迁移

- 用户已确认迁入 AiStu 社区体验，以现有 Web 技术栈实现真实共享数据，并补齐评论、回复与点赞。
- 已实现社区列表、考试科目筛选、学校分区、资料投稿与真实附件、审核审计、权限下载，以及衔接既有单题投稿的整理入口。
- 新增表已通过空库迁移及旧版本升级验证；本地后端已重启并应用迁移，健康检查通过。
- 已达到 LOCALLY_VERIFIED：隔离浏览器完成真实登录、发帖、回复、点赞、刷新恢复、上传、审核、整理题目投稿与下载；1440 × 900 桌面截图已检查。社区导航高亮修复有 Red/Green 回归测试。
- 隔离环境已清理，日常前后端保持运行；已本地提交，未推送，未运行远端 CI。
- 业务契约见[社区 API](../reference/api/community.md)；没有迁入示例帖子或虚构学校，没有自动解析附件或自动写入知识库。

## 当前前端视口范围

- 当前阶段只开发和验收桌面 Web，默认支持宽度不低于 `1280px` 的桌面浏览器，并以 `1440 × 900` 作为主要视觉检查尺寸。
- 除非用户在具体任务中另有要求，不为手机或平板新增适配，不把移动端截图、布局检查或浏览器回归纳入完成条件。
- 保留已有响应式实现，不主动删除或破坏现有移动端能力；项目进入移动端阶段后再由用户明确调整本节范围。

## 当前目标与边界

- 已完成：修复本轮审查确认的高优先级问题，包括共享 AI 学习资产越权删除、课程与知识点引用完整性、间隔复习并发写入、学习事实事务原子性、缓存键失配、统计查询 N+1、公开契约嵌套、SSE 代理遗漏，以及前端不安全 HTML、非语义交互和虚构首页指标。
- 管理端课程查询与学习端课程可见性已分离；禁用课程只对管理员可见，删除操作会拒绝仍被下游业务引用的课程或知识点。
- AI / Tutor RAG 后端工程切片已本地提交；RAG 评测与工具顺序修复、知识快照审核页面及独立检索评测入口已 LOCALLY_VERIFIED 并纳入本地提交。未推送或重启用户日常服务，真实云教学效果尚未验收。

## 已具备能力

- 408 数据结构课程库、课程空间、已审查 Tutor 教学与受限互动课件，连接练习、错题、间隔复习和阶段测评。
- 2026 数据结构真题的逐题学习、限时考试、服务端判分、主观题人工批阅与可信来源复盘。
- 有限 Markdown/文本、文本型 PDF 与 DOCX 私有试卷导入，具备所有者隔离、草稿人工复核、原文件配额与受引用保护删除。
- 独立学习端与管理端入口、路由和构建；后端统一执行权限与判分，管理端审查后发布 AI 生成题。
- 课程概览提供课程总体事实计数、最近测评摘要，以及按知识点汇总作答、正确、错题与到期复习的事实面板。
- AI 关键生成路径具备固定离线评测；模型协议与调用审计已升级，试卷主观答案仍保持未判分语义。
- Tutor Agent 已接通结构化理解检查入口、真实首次判分反馈和刷新后的答案恢复；展示检查不代替用户作答，用户再次请求才触发后续模型指导。
- Tutor Agent 提供逐次请求的分级提示，服务端保存每条回复的提示等级并限制同一运行最多三级；等级是教学展示进度，不计作学习、掌握或作答事实。
- Tutor Agent 可推荐本节已批准变式题，显式作答复用正式练习判分，保留首次题面、答案和解析快照；按最新推荐读取真实结果，未作答与推荐行为不生成学习事实。
- Tutor Agent 可提出课程学习安排，保存服务端选择的最多三步目标；用户显式确认后可恢复并逐步打开，模型读取真实确认状态，确认不表示完成学习。
- Tutor Agent 支持本人课程目标与讲解偏好的显式记忆，保存、纠正和删除均受版本保护；后续新提问读取最新设置，已有对话与真实学习记录保留。
- 会话复盘保存用户明确整理的小结，动态关联原会话的理解检查结果；模型每轮读取最近五条可用复盘，自述不改变判分，删除不改变学习计数。
- Tutor Agent 支持已审查课程工具、受限循环、逐次调用审计、用户追问和失败重试；10 分钟执行租约支持进程中断后恢复，独立执行标识阻止旧请求覆盖新轮次，首次响应或本地标识丢失时可按本人会话找回最近运行；默认配置关闭。
- 版本化知识导入支持完整性校验、并发幂等和整包回滚；索引支持人工审核、租约恢复和撤回清理，检索受课程权限与版本状态约束。
- 管理员可分页查看知识版本、原文片段、来源元数据和索引状态，填写说明后批准或撤回；页面按纯文本显示资料，审核不自动创建索引或清理向量。
- 固定 AI Evaluation 已覆盖 Tutor Agent 工具顺序、学习证据、检查动作、真实判分反馈及 RAG 有结果/空结果/资料注入；报告保留工具原文、最终输出和逐调用成本，明确区分固定检索夹具与真实生成模型。
- 独立检索评测可用真实 Embedding 和临时 Qdrant 记录 Recall@K、MRR@K、排名、耗时与调用成本；原创中文样本与脚本向量明确标注，不冒充真实课程或教学质量验收。
- Google OIDC 可按后端配置开放；登录使用短期单次交换票据，已有邮箱账号不会自动合并第三方身份。

能力入口见 [README](../../README.md)，精确业务边界见 [API 参考](../reference/api/index.md)。

## 最新验证基线

以下是最近已记录的各类验证，历史通过不代表后续改动自动通过；本地验证与远端 CI 分开记录。

| 范围 | 最近已记录验证 | 适用范围与缺口 |
|---|---|---|
| 社区共建 | 2026-09-23，8 项真实 MySQL 社区/迁移测试及17项相关后端测试通过，Checkstyle、SpotBugs、JaCoCo 通过；前端41项聚焦测试、类型检查、定向 lint、格式检查和双入口构建通过 | 隔离浏览器社区完整流程1项通过；真实接口覆盖权限、附件配额、审核撤回、并发点赞与既有投稿入库。 |
| 知识审核后端 | 2026-09-22，查询/导入/快照/状态/架构 32 项聚焦测试及新增浏览器夹具加载测试通过，Checkstyle、SpotBugs、JaCoCo 通过 | 新增查询不暴露索引运行凭据，数据库角色复核先于内容读取；无需新迁移。 |
| 独立检索评测 | 2026-09-22，指标/评测/既有 AI 回归 53 项及真实临时 Qdrant 1 项通过，质量门禁通过 | Qdrant 测试使用脚本向量；当前终端缺少必要 Embedding 配置，未运行真实云检索质量评测。 |
| 后端单测与质量门禁 | 2026-09-26，会话复盘相关 126 项聚焦验证通过，含 15 项真实 MySQL / 迁移；最终 Prompt 调整后 99 项复验及 Checkstyle、SpotBugs、JaCoCo 通过 | V104 空库及 V103 升级、动态检查证据、来源撤回过滤、分页、并发纠正/删除和版本墓碑通过；有效 Red/Green 覆盖本轮记忆集合。固定评测扩至 v8；此前同日全量单测零失败（1 项跳过），本模块未重复全量。 |
| Agent 教学闭环前端 | 2026-09-26，会话复盘、原有记忆、Agent 对话与 API 共 39 项测试、类型检查、定向 lint/格式通过 | 覆盖历史会话重读、失败后同步、检查结果更新且保留草稿、切换与卸载隔离、迟到列表响应、删除末页后回退；真实请求拦截器契约通过，关键竞态有有效 Red/Green。 |
| 知识审核前端 | 2026-09-22，11 项聚焦测试、类型检查、定向 ESLint 及双入口生产构建通过 | 覆盖分页、审核确认期间切换版本、取消、失败后状态刷新与迟到响应隔离；本次未重跑全量。 |
| 前端全量回归 | 2026-09-20，95 个文件、438 项测试、类型检查、学习端与管理端生产构建、lint、format 通过；最近一次 npm 全依赖审计为 0 个漏洞 | 新增覆盖 Agent 提问、失败保留与刷新恢复；构建仍提示大分块和上游注解告警。 |
| MySQL / Qdrant 集成测试 | 2026-09-22，知识快照 MySQL 集成 9 项通过；此前索引状态 MySQL 2 项、真实 Qdrant 2 项通过 | 最新验证覆盖导入并发、回滚、管理员查询权限、分页、版本隔离及索引状态 DTO；其他集成套件未重复运行。 |
| Docker 应用与 Playwright E2E | 2026-09-26，目标偏好与会话复盘隔离用例 2/2 通过，完整截图调整后复盘用例再次通过；双入口构建通过 | 跨会话恢复、真实检查更新、纠正与删除后模型读取最新上下文通过；只有实际检查增加一次作答。1440 × 900 桌面截图已检查，测试资源已清理。上游固定夹具 14 项通过，不能代替云教学验收。 |
| 仓库工具 | 2026-09-22，73 项通过；知识快照、代码结构、仓库布局、文档与 Nginx SSE 配置检查通过 | 覆盖公开契约边界、目录规则、知识快照、文档链接和全部流式接口代理配置。 |
| 远端 CI | 2026-09-15，`c4284ab` 的 [CI](https://github.com/umico8832/LearnPlatform/actions/runs/34947281643) 全部通过 | 仅适用于该提交；本轮 Tutor Agent 改动只授权本地提交，尚未推送或运行远端 CI。 |

## 当前遗留问题

- 未运行真实 Google OIDC、真实 Cloudflare Turnstile 凭证闭环或真实云模型评测；2026-09-26 执行环境未注入聊天与 Embedding 必需配置，首批知识内容仍待人工审核；日常后端未由本轮重启应用 V99/V100/V101/V102/V103/V104。
- Spring Boot 3.2.5 与 Knife4j 4.4.0 的主版本升级需要单独兼容性迁移；当前已加入 Dependabot 和全依赖审计，不能把自动升级等同于兼容性验证。
- 前端仍有较集中的样式与组件维护债务：若干单文件组件接近结构门禁上限，`HomeView.css` 仍较大，原始颜色/颜色函数尚未完全迁移到设计令牌。
- 课程和知识点引用保护目前由服务校验与事务提供；在缺少数据库外键或一致锁策略时，极端并发下“校验后新增引用”的竞态仍需后续治理。
- 真实学习样本不足以支持课程或用户基础分层结论；现有学习效果指标仅表达观察性关联。
- 学习端与管理端共享依赖工程、基础 API/鉴权及静态镜像，这是 [ADR-0006](../architecture/decisions/0006-separate-learner-admin-frontends.md) 的既定边界。
- 扫描件 OCR、复杂版式、更多课程、画像和桌面端同步尚未实现，均不阻止维护任务完成。
- PRD 的自动审查 Agent 仍是产品目标；当前正式题发布由管理员批准，不据文档目标改变实现权限。

## 下一步

1. 进入记忆基线/消融评估与可复现演示，围绕已完成的目标偏好和会话复盘验证实际收益；自动摘要与更多证据类型需依据评估再扩展，不把当前固定夹具视为效果结论。
2. 真实云模型配置和内容许可审核具备后，运行在线教学及独立检索评测，核对工具遵循率、教学质量、来源忠实度、成本与延迟；通过前保持默认关闭。
3. Agent 主线稳定后再处理 Spring Boot / Knife4j 兼容升级与现有前端维护债务；未授权 push 前不视为 DELIVERED。

最后整理日期：2026-09-26。
