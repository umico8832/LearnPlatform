# ADR-0002：使用 Flyway 管理数据库结构

- 状态：Accepted
- 记录日期：2026-07-30
- 类型：回溯记录

## 背景

项目数据库结构持续演进，需要本地、Docker、测试和 CI 使用同一套可追溯迁移。

## 决策

所有数据库结构变化通过版本化 Flyway 迁移完成。数据库参考文档描述当前结果，但迁移脚本和真实数据库约束是执行事实来源。

## 影响

- 不使用独立、长期漂移的手工建表流程。
- 已发布迁移原则上不回写；修正通过新迁移完成。
- 关键约束使用真实 MySQL Testcontainers 验证。
